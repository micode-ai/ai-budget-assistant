# Бэклог рекламных кампаний

Что уже отрекламировано, что нет, и в каком порядке идём. Инвентаризация
сделана 2026-08-06 по состоянию `docs/marketing/` + релиз-ноуты 1.13.0–1.16.1.

> `docs/marketing/` в `.gitignore`. По сложившейся практике `copy/`, `creatives/`
> и `scripts/` так и остаются локальными — все предыдущие рил-скрипты не в гите;
> принудительно (`git add -f`) добавляются только `landing/site`, `seo/site` и
> `help/site`, которые забирает `web-deploy.yml`. Этот файл тоже локальный.

---

## Текущий шаг

**Пункт 5 — Fat Finder + Spending Story.** Взят 2026-08-06.

- **Fat Finder — готово** (2026-08-06): 6 скриншотов в `creatives/fat-finder/`,
  копирайт `copy/feature-fat-finder-reel-pl.md`, генераторы
  `scripts/build_fat_finder_reel.py` + `scripts/build_fat_finder_story.py`,
  рендеры в `creatives/fat-finder/renders/pl/` — рил 9:16 и 4:5 **и** статичная
  Story 9:16 и 4:5 (это она несёт link-стикер на Google Play). Осталось: решить,
  публиковать ли реальные суммы (см. «Uwagi produkcyjne» в копирайте) и выложить.
- **Spending Story — ждёт скриншотов.** Отдельный reel, не склейка с Fat Finder:
  другой хук («как прошёл месяц» против «где утекают деньги»).

Что помнить по обеим фичам:
- Обе **Pro** (`SubscriptionTierGuard` + `@RequireTier('pro')` на `POST /insights/fat-finder`
  и `POST /insights/story`) — в подписи называть честно, а не подавать как бесплатное.
- Валюта отчётов починена только в 1.16.1 (ABA-386/387) — снимать на версии ≥ 1.16.1,
  иначе на скриншоте может оказаться отчёт «в долларах» по злотовому счёту.
- Суммы в отчёте — **потенциальная** экономия: формулировка «możesz zaoszczędzić»,
  никогда «zaoszczędzisz».

---

## Уже отрекламировано

**Посты (22, `copy/feature-posts.md`)**
AI-чат · OCR чеков · Telegram-бот · семейный бюджет · офлайн + мультивалютность ·
аналитика и инсайты · геймификация · виджеты · WhatsApp-бот · импорт Wise ·
менеджер подписок · веб-версия · Slack-бот · новый сайт + переезд веб-приложения ·
Google sign-in · импорт польских банков · аномалии и умные алерты · долги ·
цели накоплений · симулятор сценариев · инвестиции · шифрование данных.

**Reels / stories (17 файлов копирайта + 21 папка креативов)**
ai-chat · analytics-spend-history · antinflation (щит от инфляции) · budgets ·
financial-month · gamification · goals · inflation (личная инфляция) · maps (карта расходов) ·
purchase-list (список покупок + сравнение цен + карта магазинов) · referral ·
split (разделение чека по ссылке) · theme (акцентный цвет) · trip (поездки) ·
wrapped (итоги года) · micode-site · web-site · family-feed · purchase-requests ·
google-signin · bank-import.

**Блог**: 17 тем × 9 языков (`seo/content-plan.md`).

---

## Не рекламировали — очередь

| # | Фича | Хук | Готовность | Статус |
|---|---|---|---|---|
| 1 | **Safe-to-Spend** — «сколько можно потратить сегодня» + вопрос ИИ «могу я себе позволить X?» | Одно число вместо таблицы; честное «подожди до зарплаты» | В проде, **бесплатно**, виджет + главный экран. Скриншота Safe-to-Spend в ассетах нет | todo |
| 2 | **Чек-контроль** (проверка цен по чеку) | «Касса ещё рядом — проверь чек»: позиция дороже, чем ты обычно платил в **этом же** магазине | Вышло в 1.15.0, работает безусловно на экране сканирования и во всех 3 ботах. Аналогов у конкурентов нет | todo |
| 3 | **Авто-захват банковских push (Android)** | Расходы пишутся сами в момент оплаты, без CSV; ~43 банка, 8 стран, разбор **на устройстве** | В проде, точность усилена в 1.16.1. Не путать с уже рекламированным импортом выписок | todo |
| 4 | **Голосовой ввод в приложении** | «Сказал — записалось», расход и доход | Есть только внутри постов про ботов, отдельного reel нет | todo |
| 5 | **Fat Finder + Spending Story** | «Аудит: где утекают деньги» и «твой месяц как история» | **Pro.** Валюта починена в 1.16.1 | Fat Finder — **done**, Story — todo |
| 6 | **Financial Health Score** | 0–100 по 4 компонентам; шкала — очень «снимаемый» визуал | Есть в Play-листинге, в кампаниях ни разу | todo |
| 7 | **Проекты и теги** | «Сколько реально стоил ремонт / свадьба / отпуск» | Бытовой сценарий, ни разу | todo |
| 8 | **Обучение категориям по магазину** | «Исправил один раз — дальше само»: правила магазин→категория + слияние продавцов | Классический «app learns» | todo |
| 9 | **Кошелёк: балансы, история по месяцам, обмен и переводы** | «Все валюты в одном месте, видно, куда ушёл месяц» | Мультивалютность рекламировали общо, сам кошелёк — нет | todo |
| 10 | **Отчёты / экспорт + резервные копии** | Для фрилансеров и «отдать бухгалтеру» | Упомянуто вскользь в одном посте | todo |
| 11 | **Роли на общем счёте (только просмотр)** | B2B-угол: доступ бухгалтеру без права правки | Семейный счёт рекламировали, роли — нет | todo |
| 12 | **Календарь продлений подписок** | Новый визуал к уже рекламированному менеджеру подписок | Появился после поста №11 | todo |

**Не фича, но лучший конверсионный повод:** цена Pro снижена до **$4.99/мес и $29.99/год**
(ABA-320) — анонса не было ни одного. Готовая промо-кампания.

**Дёшево и вовлекающе:**
- «Нет вашего банка? Напишите — добавим» (в приложении есть форма запроса банка).
- «Приложение на 9 языках» — мета-пост для DE / NL / UA / BY.

---

## Что сейчас рекламировать нельзя

- **Карта цен сообщества (Pro)** — чтение за kill-switch `COMMUNITY_PRICE_READ_ENABLED`
  (по умолчанию OFF), анти-Sybil не закрыт (`docs/superpowers/community-price-antisybil.md`).
  В релиз-ноутах 1.13.0 её уже анонсировали — **перед кампанией проверить флаг в прод-env**,
  иначе человек придёт на пустой экран.
- **Счётчик «найдено переплат» в аналитике и алерт-карточка чек-контроля** — за
  `RECEIPT_CHECK_ALERTS_ENABLED` (по умолчанию OFF, `docs/ops/receipt-price-check-rollout.md`).
  Рекламировать только предупреждение на экране сканирования.
- **Формулировки чек-контроля**: только «дороже, чем обычно — проверь чек».
  Нельзя «тебя обманули / промо не сработало» — данных, что промо было, у нас нет.
- **iOS не обещать** — App Store ID всё ещё placeholder, ссылки везде на Google Play.

---

## Форматы, которые ещё не пробовали

Все 17 reels сделаны в разрезе «одна фича». Не использовано:
- **Сценарные reels по персонам**: фрилансер, семья с детьми, студент, поездка вчетвером —
  одна история, 3–4 фичи внутри.
- **«Первые 3 минуты в приложении»** — онбординг-reel, лучший формат для холодного трафика.
- **Сравнение с Excel-таблицей** — статья `best-apps` есть, видео нет.
- **Карусели из блога** — 17 статей × 9 языков уже написаны, переиспользовать в
  10-слайдовые карусели почти бесплатно.

---

## Как выпускается одна кампания

1. Скриншоты: положить в `creatives/<name>/` (источники — как есть с телефона).
2. Копирайт: `copy/feature-<name>-reel-pl.md` — сториборд (таблица: # / время / что показываем /
   текст на экране), подпись к посту, хэштеги, короткий вариант для Stories. **На польском**
   (см. `copy/feature-antinflation-reel-pl.md` как эталон).
3. Генератор: `scripts/build_<name>_reel.py` по образцу `build_purchase_list_reel.py` /
   `build_antinflation_reel.py`; рендер в `creatives/<name>/renders/pl/`.
4. Прогон: `python docs/marketing/scripts/build_<name>_reel.py both` и `... both 4:5`
   (9:16 для Reels/Stories, 4:5 для фида).
5. Отметить пункт в таблице выше как `done`.
