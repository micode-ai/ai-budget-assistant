# Release notes for admin → App Versions — 1.9.0

Store / in-app `UpdatePrompt` release notes for version **1.9.0**, one block per
locale. Paste each into the matching field in the admin **App Versions** form
(EN is required; the other 8 are optional). Locales: `en`, `ru`, `ua`, `pl`,
`es`, `fr`, `de`, `be`, `nl`.

---

## en

```
New in 1.9.0:
• Desktop web layout — sidebar navigation, top bar, and two-column Dashboard grid at 1024 px+.
• Tracking gap reminder — get a nudge if you haven't logged expenses for 3+ days. Toggle in Settings → Notifications.
• Fix: Sign in with Google now works on the web app.
```

## ru

```
Новое в 1.9.0:
• Десктопный макет веб-приложения — боковое меню, верхняя панель и двухколоночная Главная при ширине от 1024 px.
• Напоминание о пропуске трекинга — сигнал, если не записывали расходы 3+ дней. Настройки → Уведомления.
• Исправление: вход через Google теперь работает в веб-приложении.
```

## ua

```
Нове у 1.9.0:
• Десктопний макет веб-застосунку — бічне меню, верхня панель і двоколонкова Головна при ширині від 1024 px.
• Нагадування про відсутність трекінгу — сигнал, якщо не записували витрати 3+ дні. Налаштування → Сповіщення.
• Виправлення: вхід через Google тепер працює у веб-застосунку.
```

## pl

```
Nowości w 1.9.0:
• Desktopowy układ aplikacji webowej — boczna nawigacja, górny pasek i siatka dwukolumnowa na Pulpicie od 1024 px.
• Przypomnienie o przerwie w śledzeniu — powiadomienie, jeśli nie zapisywałeś wydatków przez 3+ dni. Ustawienia → Powiadomienia.
• Poprawka: logowanie przez Google działa teraz w aplikacji webowej.
```

## es

```
Novedades en 1.9.0:
• Diseño de escritorio en la web — barra lateral, barra superior y panel de inicio en dos columnas a partir de 1024 px.
• Recordatorio de seguimiento — aviso si no has registrado gastos en 3+ días. Ajustes → Notificaciones.
• Corrección: el inicio de sesión con Google ahora funciona en la aplicación web.
```

## fr

```
Nouveautés de la 1.9.0 :
• Mise en page bureau sur le web — barre latérale, barre supérieure et tableau de bord en deux colonnes à partir de 1024 px.
• Rappel de suivi — notification si vous n'avez pas enregistré de dépenses depuis 3+ jours. Paramètres → Notifications.
• Correction : la connexion avec Google fonctionne désormais sur l'application web.
```

## de

```
Neu in 1.9.0:
• Desktop-Layout im Web — Seitenleiste, obere Leiste und zweispaltiges Dashboard ab 1024 px.
• Erinnerung bei Tracking-Lücke — Benachrichtigung, wenn du seit 3+ Tagen keine Ausgaben erfasst hast. Einstellungen → Benachrichtigungen.
• Korrektur: Die Google-Anmeldung funktioniert jetzt in der Web-App.
```

## be

```
Новае ў 1.9.0:
• Дэсктопны макет вэб-праграмы — бакавое меню, верхняя панэль і двухкалонкавая Галоўная пры шырыні ад 1024 px.
• Нагадванне пра прапуск трэкінгу — сігнал, калі не запісвалі выдаткі 3+ дні. Налады → Апавяшчэнні.
• Выпраўленне: уваход праз Google цяпер працуе ў вэб-праграме.
```

## nl

```
Nieuw in 1.9.0:
• Desktopindeling in de webapp — zijbalk, topbalk en tweekoloms Dashboard vanaf 1024 px.
• Herinnering bij trackinggat — melding als je 3+ dagen geen uitgaven hebt geregistreerd. Instellingen → Meldingen.
• Oplossing: inloggen met Google werkt nu in de webapplicatie.
```
