# Release notes for admin → App Versions — 1.9.0

Store / in-app `UpdatePrompt` release notes for version **1.9.0**, one block per
locale. Paste each into the matching field in the admin **App Versions** form
(EN is required; the other 8 are optional). Locales: `en`, `ru`, `ua`, `pl`,
`es`, `fr`, `de`, `be`, `nl`.

---

## en

```
New in 1.9.0:
• Tracking gap reminder — get a nudge if you haven't logged expenses for 3+ days. Toggle in Settings → Notifications.
• Fix: Sign in with Google now works on the web app.
```

## ru

```
Новое в 1.9.0:
• Напоминание о пропуске трекинга — получайте мягкий сигнал, если не записывали расходы 3+ дней. Отключить в Настройки → Уведомления.
• Исправление: вход через Google теперь работает в веб-приложении.
```

## ua

```
Нове у 1.9.0:
• Нагадування про відсутність трекінгу — отримуйте сигнал, якщо не записували витрати 3+ дні. Вимкнути в Налаштування → Сповіщення.
• Виправлення: вхід через Google тепер працює у веб-застосунку.
```

## pl

```
Nowości w 1.9.0:
• Przypomnienie o przerwie w śledzeniu — dostaniesz powiadomienie, jeśli nie zapisywałeś wydatków przez 3+ dni. Wyłącz w Ustawienia → Powiadomienia.
• Poprawka: logowanie przez Google działa teraz w aplikacji webowej.
```

## es

```
Novedades en 1.9.0:
• Recordatorio de seguimiento — recibirás un aviso si no has registrado gastos en 3+ días. Desactívalo en Ajustes → Notificaciones.
• Corrección: el inicio de sesión con Google ahora funciona en la aplicación web.
```

## fr

```
Nouveautés de la 1.9.0 :
• Rappel de suivi — recevez une notification si vous n'avez pas enregistré de dépenses depuis 3 jours ou plus. Désactivez dans Paramètres → Notifications.
• Correction : la connexion avec Google fonctionne désormais sur l'application web.
```

## de

```
Neu in 1.9.0:
• Erinnerung bei Tracking-Lücke — erhalte eine Benachrichtigung, wenn du seit 3+ Tagen keine Ausgaben erfasst hast. Deaktivieren unter Einstellungen → Benachrichtigungen.
• Korrektur: Die Google-Anmeldung funktioniert jetzt in der Web-App.
```

## be

```
Новае ў 1.9.0:
• Нагадванне пра прапуск трэкінгу — атрымлівайце сігнал, калі не запісвалі выдаткі 3+ дні. Адключыць у Налады → Апавяшчэнні.
• Выпраўленне: уваход праз Google цяпер працуе ў вэб-праграме.
```

## nl

```
Nieuw in 1.9.0:
• Herinnering bij trackinggat — ontvang een melding als je 3+ dagen geen uitgaven hebt geregistreerd. Uitschakelen via Instellingen → Meldingen.
• Oplossing: inloggen met Google werkt nu in de webapplicatie.
```
