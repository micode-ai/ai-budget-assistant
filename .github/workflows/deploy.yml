name: Deploy to VPS

on:
  push:
    branches: [development]
    paths:
      - 'apps/api/**'
      - 'apps/admin/**'
      - 'packages/shared-types/**'
      - 'packages/shared-utils/**'
      - 'docker/**'
      - 'docker-compose.prod.yml'
      - 'scripts/deploy.sh'
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Hetzner VPS
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Block deploy if apps/api runtime-imports @budget/shared-utils — the API has
      # no build step for workspace packages, so it loads the shared-utils TS barrel
      # and the prod Node ESM runtime crash-loops on ERR_UNSUPPORTED_DIR_IMPORT
      # (ABA-317; before that ABA-252/253 + the safe-to-spend regression).
      - name: Guard — no runtime @budget/shared-utils import in apps/api
        run: bash scripts/check-no-shared-utils-runtime-import.sh

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          command_timeout: 10m
          script: |
            cd /opt/ai-budget
            bash scripts/deploy.sh

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            set -e
            HEALTH_URL="${HEALTH_URL:-https://api.ai-budget.pl/api/v1/health}"
            echo "Waiting for API to become healthy at $HEALTH_URL (up to 120s)..."
            for i in $(seq 1 40); do
              code=$(curl -sf -o /dev/null -w "%{http_code}" --max-time 5 "$HEALTH_URL" || echo "000")
              if [ "$code" = "200" ]; then
                echo "API healthy on attempt $i"
                curl -s --max-time 5 "$HEALTH_URL"
                docker ps --filter name=budget
                exit 0
              fi
              echo "  ($i/40) $HEALTH_URL -> $code, retrying in 3s..."
              sleep 3
            done
            echo "ERROR: API did not become healthy in 120s" >&2
            docker ps -a --filter name=budget
            docker logs --tail 100 budget-api-prod || true
            exit 1
