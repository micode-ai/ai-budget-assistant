# AI Budget Assistant

## This file is the schema; the knowledge lives in `docs/wiki/`

Start at **[`docs/wiki/index.md`](docs/wiki/index.md)** — a hub per domain, a page per feature,
one shape per page whose `Invariants` section states what must not break and why.

This file keeps only what belongs to no single feature: repo-wide rules and conventions,
cross-cutting patterns, environment variables, deploy and release procedure. **Do not append a new
feature's description here** — it goes on a wiki page. When you touch a feature that is still
described in this file, move it to its page as part of that task and leave a short pointer behind
(design decision 2: the migration runs one feature per task, never as a big-bang pass).

Three rituals keep it alive, one per operation:

- **Ingest** — the `finish-aba-task` skill: issue → wiki page → one line in
  [`docs/wiki/log.md`](docs/wiki/log.md) → this file only if a repo-wide rule changed → user docs.
- **Query** — the `wiki-query` skill: **read the wiki before the code**, answer with the page path
  cited, and file the finding back when the investigation cost real effort — *even if no code
  changed*. A diagnosis ending in "this is working correctly" leaves no commit and no issue behind
  it, so without this it leaves nothing at all and gets re-investigated.
- **Lint** — `python scripts/wiki-lint.py` (links, cited paths, orphans) and
  `scripts/wiki-staleness.py` (pages the code has moved past) run weekly via `wiki-audit.yml` and
  comment on the **Wiki audit** issue. They run no model — this project's Claude Code is on a
  subscription, so CI has no key. The reading half is the `wiki-audit` skill, run in a session.

Why: this file reached 77k words (~104k tokens) loaded whole into every session, while
`docs/wiki/` — bootstrapped once in May and never ingested into again — had drifted into stating
"11 AI functions" when there were 18. The artifact was never the problem; the missing loop was.
Design: [`docs/superpowers/specs/2026-09-22-llm-wiki-design.md`](docs/superpowers/specs/2026-09-22-llm-wiki-design.md).

## Architecture

Turborepo monorepo with 5 packages:

| Package | Tech | Purpose |
|---|---|---|
| `apps/api` | NestJS 10 + Prisma 5 + PostgreSQL + Redis | REST API backend |
| `apps/mobile` | Expo 54 + React Native 0.81 + Zustand + SQLite/Drizzle | Mobile app (iOS/Android/Web) |
| `apps/admin` | Next.js 16 + React 19 + TailwindCSS 4 + shadcn/ui + Recharts | Admin web dashboard (port 3001) |
| `packages/shared-types` | TypeScript interfaces | Entities and DTOs shared between api and mobile |
| `packages/shared-utils` | Zod schemas + formatting + constants | Validation and utilities shared between api and mobile |

## Key Patterns

### API (NestJS)
- **Module structure**: `modules/<feature>/` contains `module.ts`, `controller.ts`, `service.ts`, `dto/index.ts`, `guards/`
- **Auth**: JWT via `@UseGuards(JwtAuthGuard)`. Request type: `AuthenticatedRequest` from `common/types/index.ts`. Password reset via 6-digit email code (`POST /auth/forgot-password`, `POST /auth/reset-password`) with in-memory rate limiting and bcrypt-hashed codes. Email change also via 6-digit code (`POST /auth/change-email/request`, `POST /auth/change-email/confirm`) — JWT-guarded, requires current password on request, issues new tokens on confirm. **Google sign-in (ABA-282)**: `POST /auth/google` (public) takes a Google **ID token** from the client (mobile + web via `expo-auth-session`, no native module), verified server-side by `GoogleTokenVerifier` (`google-auth-library`, audiences from `GOOGLE_OAUTH_CLIENT_IDS`, requires `email_verified`). Resolves by `googleId` → **auto-link by verified email** (sets `googleId`; rejects + does not link a deactivated account) → else creates a verified, passwordless user (name/email from Google, language from client, currency default USD) + default account. Returns the same shape as `/auth/login`. `User.googleId String? @unique` + `passwordHash` now **nullable** (migration `20260621000000_add_google_auth`); `login()` and `changeEmailRequest()` reject null-passwordHash accounts ("use Google sign-in"). Mobile: `authStore.googleLogin`, `useGoogleAuth` hook, "Continue with Google" on `(auth)/login.tsx`+`register.tsx`. **Web nonce (ABA-291)**: the web path uses the **generic** `expo-auth-session` `useAuthRequest` with `responseType: IdToken`, which (unlike `providers/Google`) does NOT add a `nonce` — Google's OpenID implicit flow requires one, so without it web sign-in failed with `Error 400: invalid_request / GeneralOAuthFlow` (native was fine — `signInNative()` builds its own URL with a manual nonce). Fix: `useGoogleAuth` generates a `nonce` once (`useMemo(() => rnd(), [])`) and passes `extraParams: { nonce }` to the web `useAuthRequest`. **Prerequisite**: Google Cloud OAuth client IDs (`GOOGLE_OAUTH_CLIENT_IDS` on API; `EXPO_PUBLIC_GOOGLE_WEB/IOS/ANDROID_CLIENT_ID` on client)
- **Restore credentials / Android session restore (ABA-464 server, ABA-465 client)**: a real WebAuthn credential so a signed-in session survives an Android device transfer, for Google Play's April-2027 requirement. **Moved to the wiki** — `docs/wiki/features/restore-credentials.md`. Carried here: registration fires on EVERY authenticated launch, not only after sign-in — `JWT_EXPIRES_IN` is 7 days and the app otherwise restores its session from local storage indefinitely, so the launch path is the only one that reaches the installed base; and the assetlinks deploy guard currently **blocks the whole `web-deploy.yml` job**, not just the apex rsync, until the release fingerprint is filled in.
- **Last-active tracking (ABA-389)**: `User.lastSyncAt` is the **last-activity** stamp — it is what the admin Users table shows (labelled **"Last Active"**, not "Last Login"), what `AdminAnalyticsService` counts active-today/active-this-week from (`admin-analytics.service.ts:160-161`), and what `ReferralsService` uses to qualify a referral (`referrals.service.ts:152-154`). It is written by `JwtStrategy.validate()`, fire-and-forget, on **every** authenticated request, via the leaf `LastActiveService.touch(userId)` (`modules/users/last-active.service.ts`, provided+exported by `UsersModule`) — throttled to at most one write per user per `LAST_ACTIVE_THROTTLE_SEC` (15 min) through the new atomic `CacheService.setIfAbsent(key, ttlSec)` (`SET … EX … NX`; returns `false` when Redis is down, so an outage skips the stamp rather than writing on every request). The public `login`/`googleLogin`/`refreshToken` stamps stay (those routes never reach `JwtStrategy`); `GET /users/me` no longer stamps explicitly (redundant + unthrottled). **Do not re-add a per-route `updateLastSync` call.** Why it moved: previously only those four routes wrote it, and the ordinary registration path hits none of them — `register()`/`verifyEmail()` return tokens directly (never `/auth/login`), `authStore.register` doesn't call `getProfile()` (unlike `login`/`googleLogin`), `authStore.initialize()` restores the session from local storage, and `JWT_EXPIRES_IN` is `7d` so no refresh for a week — so an active user read as "Never" indefinitely, undercounting the dashboard KPIs and silently withholding referral bonuses. The 5th writer, `POST /sync/push` (`sync.service.ts:75`), is unreachable from mobile (`pushChanges` in `subscriptions.api.ts:59` has zero call sites). Not covered: bot traffic (Telegram/WhatsApp/Slack doesn't authenticate via JWT); no backfill for users already stuck at `NULL`.
- **Account scoping**: `AccountContextGuard` (a **guard**, `implements CanActivate`, despite living in `common/middleware/account-context.middleware.ts` — there is no `AccountContextMiddleware` class) reads the `X-Account-Id` header, resolves membership, adds `accountId` and `accountRole` to request
- **Role-based access**: `AccountRoleGuard` with `@RequireRole('owner'|'editor')` decorator (DI-based, needs `AccountsModule`). `ViewerBlockGuard` (ABA-155) — zero-dependency guard in same file; use as `@UseGuards(new ViewerBlockGuard())` on any write method without importing `AccountsModule`. Reads `req.accountRole` set by `AccountContextGuard`. Apply to all POST/PATCH/PUT/DELETE endpoints that mutate account-scoped data. AI chat: viewer write-actions blocked in `chat.service.ts` before a pending action is queued. Bots: `TelegramUserState` and `WhatsAppUserState` include `accountRole`; link services join `AccountMember` on `getLink()`; write handlers check the role before executing.
- **Service signature**: `(accountId, userId, dto)` as parameters, all Prisma queries filter by `accountId`
- **Transaction attribution**: `Expense.userId` and `Income.userId` already store the creator's id (set from `request.userId` at create time). Both services include `user: { select: { name: true } }` in all Prisma queries and call a `toExpenseResponse()`/`toIncomeResponse()` helper to flatten it into `createdByUserName: string | null` on the returned object. No migration needed — just always pass the user include and map the result. **Incomes typed (ABA-359)**: `incomes.service.ts`'s `toIncomeResponse`/list-filter `where` are typed via a shared `Prisma.IncomeInclude`-derived `incomeInclude` const + `IncomeWithRelations`/`IncomeResponse`/`Prisma.IncomeWhereInput` (was `any`/`any`, the `expenses.service.ts` side of this — `toExpenseResponse(expense: any)` — is still open, tracked by tech-debt `expense-service-response-typed-any`). Gotcha: `Prisma.IncomeWhereInput['date']` isn't always an object (can be a bare `Date`), so `where.date = { ...where.date, gte/lte }` fails to typecheck once `where` is properly typed — build the date range in a separate local `Prisma.DateTimeFilter` instead of spreading `where.date`. `incomes.service.spec.ts` covers `create` (all `resolveCategoryId` branches, tags, gamification/cache/family-feed fire-and-forgets), `findAll` (filter building + pagination), `findOne`, `update`, `remove`, `getByClientId`.
- **Database**: Prisma ORM. Schema at `apps/api/prisma/schema.prisma`. Uses `@map("snake_case")` for column names. Prod `DATABASE_URL` includes `connection_limit=10` (see `docker-compose.prod.yml`) to cap the connection pool. **A `schema.prisma` field change MUST land with its migration in the same commit — nothing in the pipeline catches a missing one (ABA-558)**: lint, typecheck, tests and the deploy all pass, `prisma generate` happily builds a client that selects the new column, `prisma migrate deploy` has nothing to apply, and the first query to touch it fails at runtime in production. ABA-556 added `ExpenseItem.lineDiscount` without a migration and broke `ExpensesService.findAll` — the whole transactions list, not just the receipt-split feature it shipped — for everyone. **Monitoring cannot see this**: `/health` runs `SELECT 1` and never touches the table, so the container reported `healthy` and `uptime-check.yml` stayed green throughout; Sentry was the only signal. To recover, author the migration and apply it through the **standard `migrator` service** (`docker compose -f docker-compose.prod.yml --env-file .env.production --profile migrate run --rm migrator`, the same command `scripts/deploy.sh` runs) after copying the migration folder to `/opt/ai-budget` — never by hand-editing the database, which leaves `_prisma_migrations` without the row and makes the next deploy re-apply it. To audit for drift without a shadow DB, diff the `@map`ped columns in `schema.prisma` against `information_schema.columns` on prod; note a `String[]` field has no `@db` column type of its own and will read as a false positive.
- **API modules** (the list is the count — do not restate it as a numeral, it has been wrong at 44, 45 and 47): `accounts`, `account-transfers`, `admin`, `ai`, `analytics`, `anomaly`, `app-versions`, `auth`, `backups`, `budgets`, `categories`, `community-prices`, `currency-exchange`, `debts`, `encryption`, `exchange-rate-alerts`, `expenses`, `family-feed`, `gamification`, `health`, `import-bank`, `import-batches`, `import-wise`, `incomes`, `insights`, `investments`, `mail`, `merchant-rules`, `notifications`, `price-history`, `projects`, `purchase-requests`, `receipt-split`, `referrals`, `reports`, `restore-credentials`, `shopping-list`, `slack`, `subscriptions`, `sync`, `tags`, `telegram`, `telemetry`, `trip-settle-up`, `user-subscriptions`, `users`, `wallet`, `whatsapp`
- **Merchant-to-category auto-learning** (ABA-261): `modules/merchant-rules/` — learns from manual expense category corrections and applies them at import time. Table `merchant_category_rules` (`migration 20260615000000_add_merchant_category_rules`), `@@unique([accountId, merchantNormalized])`. `merchantNormalized = merchant.trim().toLowerCase()`. `MerchantRulesService` exported to `ExpensesModule`, `ImportWiseModule`, `ImportBankModule`. **Learning**: `ExpensesService.update()` fires-and-forgets `upsertRule(accountId, merchantNormalized, resolvedCategoryId)` after a successful category change on an expense with a non-empty merchant. **Applying**: both import commits (`import-wise` + `import-bank`) call `getRulesMap(accountId)` **before** the `$transaction`, then apply the user rule as a higher-priority override over static `MERCHANT_CATEGORY_HINTS` / `suggestCategoryFromMerchantPL`. Endpoints: `GET /merchant-rules` (list with category name/icon), `DELETE /merchant-rules/:id` (ViewerBlockGuard). Rules are cascade-deleted when their category is deleted. Mobile: `merchantRulesStore.ts` (in-memory, loads from API on demand), "Category rules" section in `app/settings/merchants.tsx`. 4 i18n keys (`categoryRules`, `noRules`, `ruleDeleteConfirm`, `ruleDeleted`) in all 9 locales. No rule migration on merchant rename/merge (import CSVs produce the same raw merchant names regardless).
- **Admin module** (ABA-175): `modules/admin/` has four focused services — `AdminService` (user management + audit log + system health; injects `PrismaService`, `CacheService`), `AdminAnalyticsService` (dashboard KPIs, analytics overview, AI usage trends, subscription stats; injects `PrismaService`), `AdminNotificationService` (push/email/broadcast delivery, scheduled notifications cron, notification history; injects `PrismaService`, `MailService`, `NotificationsService`), `AdminInvestorMetricsService` (ABA-340 investor-metrics endpoint IO + Redis cache + assembly + PL/other language segments; injects `PrismaService`, `CacheService`). `AdminGateway` injects both `AdminService` and `AdminAnalyticsService`. Controller injects all four directly — no facade layer. `estimateCost()` and cost constants are exported from `admin-analytics.service.ts` and imported by `admin.service.ts` for `getUserDetail` and by `admin-investor-metrics.service.ts` for the AI-COGS gross margin. The `PRO_MONTHLY_USD`/`BUSINESS_MONTHLY_USD` cents pair that file used to export is **gone** (ABA-433) — every MRR figure in the admin now prices through `MRR_MONTHLY_USD` in `admin-metrics.util.ts`.
- **Comped (admin-granted) tiers vs real revenue (ABA-433)**: an admin can hand a user Pro/Business from the admin panel with no payment behind it, and every revenue figure used to count those users as paying — on prod all 7 active `business` subscriptions are grants and there are zero Stripe-backed paid subs, so the dashboard reported $139.93/mo of revenue that does not exist. **Detection needs no column and no migration**: `stripeSubscriptionId` is written by exactly ONE place in the codebase (the Stripe webhook, `SubscriptionsService.handleSubscriptionCreated`) while `AdminService.changeSubscriptionTier` sets only `tier` + `status`, so "active paid tier with no Stripe subscription id" identifies the manually-granted set exactly, **retroactively** for grants already made, and self-heals if the user later pays; `handleSubscriptionDeleted` nulls the id **and** resets `tier` to free, so a churned ex-payer can never read as a grant. `modules/admin/admin-comped.util.ts` holds `isComplimentarySub`/`isStripePaidSub` **and** the matching `COMPED_SUB_WHERE`/`PAID_SUB_WHERE` Prisma literals in one file on purpose — a counting query and an in-memory filter that disagree is the failure mode it guards. The predicates are **nullish-tolerant, not `=== null`**: an absent field (a hand-built row, a narrowed select) would otherwise read as *paying*, i.e. fail in the direction that inflates revenue; the `where` literals are deliberately NOT `as const`, since Prisma's generated types reject the readonly array a const assertion produces. Pure `toMrrRows(subs, predicate)` (`admin-metrics.util.ts`, beside `normalizeMrr`) builds both the paying and the comped set through ONE mapper so they stay comparable — do not add a second mapper. **Excluded everywhere**: investor metrics (`payingUsers`/`mrrUsd`/`arppuUsd`/`freeToPaidConversion` + per-language segment MRR + `trialToPaidConversion`, which must not count a post-trial grant as a conversion) plus new `MonetizationBlock.compedUsers`/`compedMrrUsd`; and both legacy calculations, `getAnalyticsOverview` and `getSubscriptionStats`, which switched from `count()` to rows through `normalizeMrr` — fixing the comped inflation, the stale hardcoded $9.99 Pro price (real price since ABA-320 is $4.99) and the ignored yearly interval in one move. `conversionRate` counts only real payers. **Users list**: `GET /admin/users` returns a computed `isComplimentary` per row and takes `billing=paying|comped` as a **separate axis that composes with `tier`** (`tier=business&billing=comped` = "business tiers I gave away"); the raw `stripeSubscriptionId` is collapsed to that one boolean before it leaves the service. Admin UI: `TierBadge` `complimentary` prop (amber "Comped" badge), Billing filter + explanatory panel on the user pages, Comped cards with `InfoHint` on `/metrics` and `/subscriptions`, an amber sub-line under the dashboard MRR card so the corrected number is explained rather than silently lower, and a note in `ChangeTierDialog` that granting a tier there charges nothing. **Known limitation**: someone who paid for Pro via Stripe and was then hand-upgraded to Business keeps a non-null `stripeSubscriptionId` and still counts as paying at the Business price — closing that needs an explicit flag. Tests: `admin-comped.util.spec.ts`, `admin.service.spec.ts` (billing-filter composition + no id leakage), `toMrrRows` + three comped scenarios in the existing metrics specs.
- **Acquisition dashboard (ABA-452)**: closes the read-surface gap left by ABA-436 — the `User.acquisition{Source,Location,Language,Plan}` columns (captured from the `?src=&loc=&lang=&plan=` link tags on the landing/blog/help sites, see the ABA-436 entry under **Mobile** below) had no admin UI. `AdminAnalyticsService.getAcquisitionBreakdown(days)` groups signups in a day window by each of the four columns via `Prisma.groupBy` (on the existing `@@index([acquisitionSource])`) — a `null` in any dimension is explicitly bucketed as `'direct'`, never dropped, since NULL there is a legitimate "unknown" (pre-migration user, native install, no query param). `GET /admin/analytics/acquisition?days=30` (clamped 1–365) on the class-level `JwtAuthGuard + AdminGuard`, no extra caching (a few lightweight indexed `groupBy`s). Admin page `apps/admin/src/app/acquisition/page.tsx` (+ `use-acquisition.ts` hook) — 4 stat cards + 4 breakdown tables (by source/location/language/plan) with a 7/30/90-day toggle, same `Stat`/`InfoHint` pattern as `/metrics`; sidebar nav entry "Acquisition" (Megaphone icon) sits under Investor Metrics. New shared types `AdminAcquisitionBreakdownResponse`/`Row` in `packages/shared-types/src/dto/admin.ts`. No schema change (columns/index already existed).
- **Investor metrics (ABA-340)**: `GET /admin/metrics/investor?months&weeks&activationDays` (`JwtAuthGuard + AdminGuard` inherited from the class; params clamped, defaults 6/12/3; Redis cache `admin:investor-metrics:{months}:{weeks}:{activationDays}` TTL 1h) returns the metrics an investor asks for during due diligence — all from existing tables, **no migration**, admin-only, no mobile changes. Blocks: weekly cohort **retention** triangle + pooled W1/W4/W8 headline; **activation** (logged first transaction within a 3-day window); **DAU/WAU/MAU** + stickiness; MoM **growth**; **honest MRR** (interval inferred from `currentPeriodEnd−currentPeriodStart` >45d ⇒ yearly÷12, USD-normalized with an `mrrApproximate` flag when any paying sub is non-USD); ARPU/ARPPU; free→paid + trial→paid **conversion**; **churn**; AI-COGS **gross margin**; and **PL/other** language segments. Correctness lives in the pure, unit-tested `modules/admin/admin-metrics.util.ts` (`now` always injected; `MRR_MONTHLY_USD` is a **4th copy** of the pricing table — keep in sync — and since ABA-433 it is the pricing source for the legacy dashboard/subscription MRR too); `AdminInvestorMetricsService` does the Prisma IO + assembly. **Definitions**: `active` = created ≥1 expense/income that **UTC** day (deduped by `userId`); trials **excluded** from MRR/`payingUsers`; `totalUsers` and the `freeToPaidConversion` denominator = **all registered users** (matches the signup universe feeding retention/growth/segments). **Churn**: detected by `status='canceled' && canceledAt >= monthStart` (count-based, tier-independent) because `SubscriptionsService.handleSubscriptionDeleted` resets `tier→free` + `stripePriceId→null` on cancel, discarding the pre-cancel tier — so **logo churn is exact but revenue churn is `null` in v1** (Phase-2 needs a pre-cancel-tier snapshot). `normalizeMrr` returns the **exact** monthly-equivalent sum; cents rounding happens at the service assembly boundary (no double-rounding; ARPU/ARPPU derive from the exact value first). Mobile-facing: none. Admin `/metrics` page (`apps/admin/src/app/metrics/page.tsx` + `use-investor-metrics.ts` hook + sidebar nav): headline cards, cohort-retention heatmap, growth bar chart, monetization cards, PL/Other segment toggle (the admin `formatPercent` prefixes `+` for deltas — the page uses a local plain formatter for non-delta stats). Every metric card + section header + the segment toggle carry a hover **tooltip** (`InfoHint` → shadcn `Tooltip`, aliased `UITooltip` to avoid the recharts `Tooltip` name clash; descriptions in a `HINTS` map, English to match the admin UI) explaining what each metric means (ABA-341). **Follow-ups**: revenue churn via pre-cancel tier snapshot; CAC/LTV:CAC (need marketing-spend, not in DB); referral K-factor; the legacy dashboard `getAnalyticsOverview()` MRR was fixed in ABA-433 (was hardcoded $9.99 and ignored yearly).
- **Caching layer** (ABA-151): `common/cache/cache.service.ts` — global `@Global()` ioredis wrapper; `delByPrefix` uses cursor-based `SCAN` (not blocking `KEYS`). `common/cache/redis-throttler-storage.ts` — `RedisThrottlerStorage` (`ThrottlerStorage` v5 interface, INCR+PEXPIRE NX+PTTL pipeline, `keyPrefix: 'throttle:'`); registered via `ThrottlerModule.forRootAsync` in `app.module.ts` so rate limits survive restarts. **`CacheService.incrementWindow(key, windowMs)`** (ABA-416) is the general-purpose version of the same INCR+PEXPIRE-NX pattern for any security-sensitive rate limit that needs to live outside `ThrottlerModule` — unlike every other `CacheService` method it does NOT swallow Redis errors (callers must fail closed, not silently allow unlimited attempts on a Redis outage). First consumer: `EncryptionService.checkRecoveryRateLimit` (`enc:recovery:{email}`, 5/15min) — replaced a plain in-memory `Map` that reset on every deploy, the same defect class as the still-open `auth.service.ts` rate-limit Maps (tech-debt `auth-rate-limit-in-memory-maps`, not yet fixed).
- **Fire-and-forget logging convention (ABA-461)**: any `void someCall().catch(...)` guarding a non-critical side effect (Family Feed events, anomaly checks, gamification, renewal/reminder pushes, wallet-currency backfills, community-price contributions, etc.) MUST log the rejection, never swallow it with `.catch(() => {})` — a silent failure there means the secondary effect never happened and there's no way to diagnose it. Use `logFireAndForget(this.logger, 'ClassName.whatItDoes')` from `common/utils/fire-and-forget.ts`, which returns a `.catch()` handler that calls `Logger.warn` (never `.error` — mirrors the mobile ABA-157 convention that a fire-and-forget failure is expected/transient, not exceptional). Every call site across the API (`subscriptions`, `expenses`, `incomes`, `wallet`, `budgets`, `purchase-requests`, `import-bank`, `import-wise`, `sync`, `referrals`, `telegram`, `community-prices`, `insights`, `shopping-list`, `trip-settle-up`, `debts`, `gamification`, `user-subscriptions`) was migrated to this helper; a new fire-and-forget call site should use it too, not a bare empty `.catch()`.
- **Daily cron pagination** (ABA-457): `common/utils/paginate.ts` — `paginateById(fetchPage, batchSize=500)`, a generic async generator that streams a Prisma `findMany`-shaped query in `id`-ordered batches (`orderBy:{id:'asc'}` + `cursor`/`skip`) instead of loading the whole eligible-row set in one call. Consumed by all 5 daily notification crons that used to `findMany` an entire table (tech-debt `daily-crons-full-table-scan`): `tracking-gap-reminder.cron.ts`, `debt-reminder.cron.ts` (both methods — the per-batch repayment lookup is scoped to that batch's debt ids, not the whole day's set), `expense-recurring.cron.ts` (see below — its recurringId-dedup switched from "first-seen wins" under a global `orderBy:date desc` to "latest-date wins" compared per row, since pagination is now by `id`), `subscription-renewal.cron.ts` (both crons), `trial-reminder.cron.ts`. New cron/batch-job code that scans an unbounded table should use this util rather than a bare `findMany`. Deliberately NOT changed: `NotificationsService.sendToUser`'s per-user `findUnique` re-fetch (N+1) — that method has many other callers and its signature was out of scope for this fix.
- **Debt reminders**: `modules/debts/debt-reminder.cron.ts` — `@Cron('0 9 * * *')` daily cron that finds active lent/borrowed debts due in 3 days (upcoming) or overdue yesterday, batches repayment queries to compute remaining balance, and calls `NotificationsService.sendToUser(..., 'debt_reminder')`. Respects `user.notifyDebtReminders` preference (default `true`). User preference toggle in `app/settings/notifications.tsx`. `NotificationType` includes `'debt_reminder'`; `NotificationPreferencesResponse` includes `debtReminders: boolean`.
- **Recurring expenses**: `modules/expenses/expense-recurring.cron.ts` — `@Cron('0 8 * * *')` daily cron that groups all active recurring expenses by `recurringId`, takes the most recent per series, computes `nextDue = lastDate + period` (weekly/monthly/yearly), and if `nextDue <= today` clones the expense with today's date (same `recurringId`) and sends a push notification. Respects `user.notifyRecurringExpenses` preference (default `true`). Mobile new-expense form has a "Repeat" toggle + period chip picker that sets `isRecurring: true`, assigns a `recurringId` UUID, and sets `recurringPeriod`. Expense detail screen shows a "Part of a recurring series" banner with period label and an inline "Stop Recurring" button (`PATCH /expenses/:id/stop-recurring` sets `isRecurring: false`; history preserved). User preference toggle in `app/settings/notifications.tsx`.
- **Tracking gap reminder** (ABA-292): `modules/gamification/tracking-gap-reminder.cron.ts` — `@Cron('0 10 * * *')` daily cron that queries `UserStreak` (type `daily_tracking`) grouped by userId, finds the most recent `lastActivityDate` across all accounts, and fires a `tracking_gap_reminder` push when `diffDays >= 3 && diffDays % 3 === 0` (sends on day 3, 6, 9…; skips days 4, 5, 7, 8). New users with no `UserStreak` row are skipped. Column `user.notifyTrackingGap Boolean @default(true)` (migration `20260627000000_add_tracking_gap_notification`); preference gate in `NotificationsService.sendToUser`; `GET/PATCH /users/me/notification-preferences` includes `trackingGap: boolean`. Mobile toggle "Tracking Reminder" in `app/settings/notifications.tsx`; `trackingReminder`/`trackingReminderDesc` i18n keys in all 9 locales.
- **Gamification tests (ABA-426)**: `gamification.service.spec.ts` (28 tests total with `streak.service.spec.ts`) covers every `checkAchievements()` switch branch via an in-memory `Map`-backed `userAchievement` double (mirrors real upsert/update/findUnique semantics) rather than per-call arg mocking — one scenario call exercises all 16 `ACHIEVEMENT_DEFINITIONS` at once. Pins the `update`-vs-`upsert` split (existing row → `update`; no row → `upsert`, including the no-write case when already complete at ≥ new progress) and the `referrals_*` `achievementAccountId` redirect to `user.defaultAccountId`. `budget_3months_no_exceed`'s "Simplified" 33%-cap-forever behavior (see the `checkAchievements` source comment) is asserted explicitly as a documented known limitation, not left as a silent surprise. `streak.service.spec.ts` covers `updateStreak`'s same-day/consecutive-day/streak-break day math and a timezone case showing one wall-clock instant yields opposite verdicts (continue vs. break) depending on `user.timezone`. `gamification.controller.ts` (pure delegation) and `tracking-gap-reminder.cron.ts` remain untested — no branching logic to pin yet.
- **Purchase requests** (ABA-298): `modules/purchase-requests/` — group purchase approval for shared accounts. Any member (including viewers) can propose a purchase; other members vote via app or bots; approval creates a planned expense. **Data model**: `purchase_requests` + `purchase_request_votes` tables (migrations `20260630000000_add_purchase_requests`); `Account.purchaseApprovalRule ApprovalRule @default(MAJORITY)` (migration `20260630000001_add_account_approval_rule`); `Expense.isPlanned Boolean @default(false)` + `@@index([accountId, isPlanned])` (migration `20260630000002_add_expense_is_planned`). **Approval rules**: `MAJORITY` (approve/reject count > 50% of total members); `UNANIMOUS` (all must approve; one reject kills it); `OWNER_ONLY` (only the account owner's vote counts). ABSTAIN excluded from denominator so non-participating members don't block decisions. Voting is upsert on `[requestId, userId]` so members can change their vote. **9 endpoints** (all `JwtAuthGuard + AccountContextGuard`): `GET/POST /purchase-requests`, `GET/POST /purchase-requests/:id`, `POST /purchase-requests/:id/vote` (any member incl. viewers — intentionally NOT behind `ViewerBlockGuard`), `POST /purchase-requests/:id/convert` (ViewerBlockGuard — creates planned `Expense`, sets `plannedExpenseId`), `POST /purchase-requests/:id/mark-purchased` (ViewerBlockGuard — sets `status = PURCHASED`), `PATCH /purchase-requests/:id` (ABA-302 — edit PENDING request, ViewerBlockGuard, creator or owner only), `DELETE /purchase-requests/:id` (creator or owner only), `PATCH /purchase-requests/settings/approval-rule` (owner only, declared first to avoid `:id` shadow). Route order matters: `settings/approval-rule` → `pending-count` → `/` → `/:id` → `/:id/vote` → `/:id/convert` → `/:id/mark-purchased` → `PATCH /:id` → `DELETE /:id`. **Edit/Delete (ABA-302)**: creator or account owner can edit (title/amount/currency/merchant/description) or delete (= set status REJECTED + fire family-feed rejected event) a PENDING request. `[id].tsx` shows Edit + Delete buttons only for PENDING requests; Edit navigates to `new.tsx?editId=` which prefills fields and calls `updateRequest` on save; viewers cannot edit. **Notifications**: 4 push types — `purchase_request_created` (all members except creator), `purchase_request_voted` (creator), `purchase_request_approved` (all members), `purchase_request_rejected` (creator). User preference `notifyPurchaseRequests Boolean @default(true)` gated in `NotificationsService.sendToUser`. **Telegram bot**: `PurchaseRequestHandler` in `modules/telegram/handlers/purchase-request.handler.ts`, callbacks `pr_approve:{id}` / `pr_reject:{id}` (`:` separator). **WhatsApp bot**: same via `sendButtons`, callbacks `pr_approve--{id}` / `pr_reject--{id}` (`--` separator). **Planned expense**: `loadAllExpenses` in `expenseRepository.ts` filters `AND (is_planned IS NULL OR is_planned = 0)` so planned expenses never inflate spend totals. `expense/[id].tsx` shows a "Planned · Mark as purchased" banner when `isPlanned: true`; tap: optimistic `updateExpense({ isPlanned: false })` FIRST, then `markAsPurchased(linkedPR.id)`, catch → rollback + alert. Mobile: `purchaseRequestStore.ts` (server-only, not offline-first — voting requires cross-member consistency); 3 screens `app/purchase-requests/index.tsx` (3 tabs Active/Approved/History), `new.tsx`, `[id].tsx`; entry via Settings hub row with pending-count badge. 34 `purchaseRequests.*` i18n keys in all 9 locales. Tests: 27/27 (service 15, controller 12). **Phase 2 deferred**: expiry cron, comments thread, Family Feed integration, push image to bots.
- **Family Feed** (ABA-299): `modules/family-feed/` — Instagram-style activity feed for shared accounts. Events written fire-and-forget from `ExpensesService.create()`, `IncomesService.create()`, and `PurchaseRequestsService` (4 call sites: created/approved/purchased/cancelled→rejected); all use `@Optional() private readonly familyFeed?: FamilyFeedService` + `void this.familyFeed?.recordEvent(...).catch(() => {})` so existing tests pass without modification. `recordEvent()` no-ops when `account.type === 'personal'`. **Data model**: `family_feed_events` (`id`, `accountId`, `userId`, `type FeedEventType`, `entityId`, `metadata Json`, `createdAt`; `@@index([accountId, createdAt DESC])`); `feed_reactions` (`@@unique([eventId, userId])`); both `ON DELETE CASCADE`; migration `20260629214117_add_family_feed`. **Grouping**: `groupEvents(events, callerUserId)` groups `EXPENSE_ADDED`/`INCOME_ADDED` by `(userId, calendarDay)` into one `FeedGroup`; `PURCHASE_REQUEST_*` events always individual cards. `FeedGroup.eventIds` carries `event.entityId` (the expense/income PK) for deep-links — NOT `event.id` (the feed-event PK). **PR deduplication**: `groupEvents()` uses a `seenPrIds` Set (events are DESC) to keep only the most recent event per PR, so approve/reject after CREATED doesn't show two cards. **Live PR status**: `getFeed()` batch-queries `purchase_requests.status` from DB and passes a `prStatusMap` to `groupEvents()` — live status overrides the inferred type, so a PR cancelled years ago shows REJECTED even though its feed event was PURCHASE_REQUEST_CREATED. `FeedEventType` enum includes `PURCHASE_REQUEST_REJECTED` (migration `20260630100000_add_feed_event_rejected`). **3 endpoints** (class-level `JwtAuthGuard + AccountContextGuard`, no `ViewerBlockGuard` — viewers CAN react): `GET /family-feed` (default limit 100, NaN-safe clamp), `POST /family-feed/:eventId/react` (upsert on `[eventId, userId]`, 6 emoji allowlist), `DELETE /family-feed/:eventId/react` (204). Mobile: `familyFeedStore.ts` (in-memory, server-only, optimistic react/removeReaction with `prev` rollback, `console.warn` on failure); `family-feed.api.ts`; `EmojiReactionBar.tsx` + `FeedGroupCard.tsx` in `src/components/feed/`; full screen `app/family-feed/index.tsx` (FlatList, `useFocusEffect` reload, pull-to-refresh); `FamilyFeedWidget` (last 3 groups, reloads on `currentAccountId` change — prevents stale cross-account UI). `'familyFeed'` added to `WIDGET_KEYS` after `'safeToSpend'`; widget hidden when `currentAccountType === 'personal'`. 15 `familyFeed.*` i18n keys in all 9 locales. Tests: 17/17 (service 14, controller 3). **Feed retention (ABA-303)**: configurable `familyFeedRetentionDays` stored in `system_config` (key-value table via `SystemConfigService.get/set`); `FamilyFeedService` daily `@Cron('0 3 * * *')` `cleanupOldEvents()` reads the setting and deletes `family_feed_events` older than that many days (default 5). Exposed in the admin panel **Settings → System Health card** via `GET/PATCH /admin/config`. **Vote rejection cleanup (ABA-303)**: when `PurchaseRequestsService.vote()` resolves to `REJECTED`, it fire-and-forgets `prisma.familyFeedEvent.deleteMany({ where: { entityId: id, accountId } })` so the rejected PR card disappears from the feed immediately. **Phase 2 deferred**: budget/goal/anomaly events in feed, reaction push notifications, comments thread, Slavic plural i18n variants.
- **Group Trip Wallet** (ABA-305): `AccountType.trip` — a temporary shared account with multi-way splitting and a settle-up flow. **Moved to the wiki** — `docs/wiki/features/trip-wallet.md`. Carried here: `paidByUserId` must be set on BOTH the upsert's `create:` and `update:` branches (the update branch was missing once and silently dropped payer edits on offline retry); and settle-up writes use guard-derived `req.accountId`/`req.user.id`, never a raw `:id` param or a client-supplied identity field — an early draft's IDOR came from exactly that.
- **Anomaly alerts** (ABA-242): rule-based proactive alerts with no LLM cost, fired **on expense write**, not by a cron. **Moved to the wiki** — `docs/wiki/features/anomaly-alerts.md`. Carried here: `@@unique([accountId, dedupKey])` IS the dedup (insert + catch P2002 = already alerted); push `data` must use the key `anomalyType`, because a plain `type` is overwritten by `NotificationsService`; and an alert's `expenseId` is the SERVER PK, so every consumer resolves an expense by `id` OR `serverId` OR `clientId` OR `localId`.
- **Anomaly service split** (`anomaly-service-god-class` tech-debt, closed): `anomaly.service.ts` had grown to 689 lines mixing alert CRUD with all 6 independently-evolving detectors. Split into `anomaly-helpers.util.ts` (pure, dependency-free: `expensePayee`, `normalizeMerchant`, `monthKey`, `detectCycle`, `DAY_MS`/`DUP_DAY_MS`, `PRICE_INCREASE_FACTOR`, `SPIKE_THRESHOLD_PERCENT` — `expense-created-hooks.service.ts`/`import-bank-dedup.service.ts` import `expensePayee`/`DUP_DAY_MS` from here now, not from `anomaly.service.ts`), `anomaly.types.ts` (`CreateAlertInput`/`DetectorExpense`), `anomaly-alert-writer.service.ts` (`AnomalyAlertWriterService.createAlert` — the one shared insert+dedupKey-via-P2002+daily-push-cap mechanism all 6 detectors write through), and `anomaly-detectors.service.ts` (`AnomalyDetectorsService`, all 6 `detect*` methods incl. the `receiptCheckAlertsEnabled()` gate). `AnomalyService` is now the thin orchestrator — `checkExpense`/`checkExpenseBatch` fan out to `AnomalyDetectorsService`, plus the alert CRUD/read surface (`findAll`, `getPriceCheckSummary`, `markRead`, `markAllRead`, `dismiss`, `dismissForExpense`) it already owned. No public-API change: the controller and every external caller (`ExpensesService`, `ExpenseCrossAccountService`, `ExpenseCreatedHooksService`, `ImportBankService`, `ImportWiseService`) still depend on `AnomalyService` unchanged.
- **Safe-to-Spend engine (ABA-293)**: `modules/insights/safe-to-spend.service.ts` — single source of truth for the home hero number (#1) and `check_affordability` AI function (#3). `GET /insights/safe-to-spend` (JwtAuth + AccountContext, no tier guard — free). Inputs: wallet summary (`WalletService.getSummary`), upcoming subscription charges (`userSubscription` where `isActive`, projected forward within horizon), upcoming recurring expenses (mirrors `expense-recurring.cron.ts` grouping: latest per `recurringId`, next due date), on-track goal contributions (linear pace to deadline), inferred monthly income (income history 90d, 25–35d cadence heuristic — same detector as anomaly `recurring_suggestion`). FX via `getRatesSafe`/`convertAmount` with `fxApproximate` flag (same pattern as `ai-tools.service.ts`). Formula: `safeToSpendToday = max(0, (walletBalance + expectedIncome − obligations − buffer) / daysRemaining)`; `buffer = 0` (v1). Horizon = min(end-of-month, nextExpectedIncomeDate); clamps to 1 day minimum. Redis cache `sts:{accountId}:{baseCurrency}` TTL 300s. Pure `computeSafeToSpend(inputs)` formula helper is a **duplicated pair, same convention as `financial-month.ts`/`receipt-category-split.ts`**: the service imports its own canonical copy at `apps/api/src/modules/insights/safe-to-spend.util.ts`, and a mirror in `packages/shared-utils` is what the mobile offline fallback (`useSafeToSpend.ts`) actually imports — it is **not** a single shared function, and the API never imports `@budget/shared-utils` at runtime (no build step there — see the `check-no-shared-utils-runtime-import.sh` deploy guard under Production). `safe-to-spend.service.spec.ts` tests the shared-utils copy, not the file the service calls at runtime — harmless only because the two are kept identical by hand; this wording ("lives in shared-utils") was previously stale here and nearly caused a repeat of that same outage. **NO migration** — computable entirely from existing tables. `SafeToSpendResponse` + `SafeToSpendBreakdown` + `AffordabilityVerdict` DTOs in `packages/shared-types/src/dto/insights.ts`. Mobile: `useSafeToSpend` hook (`src/features/insights/useSafeToSpend.ts`) + `insightsStore.loadSafeToSpend()` (MMKV-cached) + home hero number (tap → breakdown bottom-sheet) + `'safeToSpend'` `WidgetKey`/widget + `check_affordability` `ActionResultCard` chip (yes/no + `reasonCode`).
- **Financial Wrapped (ABA-336)**: `modules/insights/wrapped.service.ts` (thin IO wrapper) + pure `wrapped.util.ts` `assembleWrapped` (unit-tested, mirrors `safe-to-spend.util.ts`) — a Spotify-Wrapped-style year-in-review assembled ENTIRELY from existing data (**NO migration**). `GET /insights/wrapped?year=YYYY` (JwtAuth + AccountContext, **no tier guard — FREE** for shareability, same precedent as safe-to-spend; clamps year to `[2000, currentYear]`). `WrappedResponse` = ordered discriminated-union `WrappedCard[]` (`packages/shared-types/src/dto/insights.ts`) — only cards WITH data are included; `hasEnoughData:false` (+ empty `cards`) below 5 tracked rows or on tier-2 full-encryption. Cards: `intro`, `total_tracked`, `top_merchant` (visits, tie-break spend), `biggest_month`, `top_category`, `category_mix`, `receipts_scanned` (`source in ('ocr','notification')`), `savings` (net + rate + savedVsLastYear — pulls prior year in the same query), `personal_inflation` (reuses `PriceHistoryService.getPriceHistory('12m').inflationIndex` — rolling-12m proxy, only for current/prev year; **`PriceHistoryService` is now exported from `PriceHistoryModule`**), `streak` (`StreakService.getStreak`). All amounts FX-converted to `user.currencyCode` via `getRatesSafe`/`convertAmount` (unknown rate → amount excluded from sum, `fxApproximate` flag). Redis cache `wrapped:{accountId}:{baseCurrency}:{year}` TTL 3600s. `InsightsModule` imports `PriceHistoryModule` + `GamificationModule`. **Mobile**: `api.getWrapped(year)` (`analytics.api.ts`), `useWrapped` hook (`src/features/insights/`), full-screen swipeable gradient card-deck `app/wrapped/index.tsx` (**zero new native modules** — paging `ScrollView` + `expo-linear-gradient`; progress dots; text share via `Share.share({message})` composed from `wrapped.share*` i18n + amount-hiding toggle), route `fullScreenModal` in `_layout.tsx`, entry banner in the Analytics tab. 41 `wrapped.*` i18n keys × 9 locales. **Shareable IMAGE (M3)**: `src/components/wrapped/WrappedShareCard.tsx` is a thin wrapper (gradient + `wrapped-<year>.png` file prefix + payload mapping) over the generic `src/components/share/ShareImageCard.tsx` (ABA-353, extracted from the earlier byte-faithful duplicate with `InflationShieldShareCard` — see **Share image mechanism** below). `buildSharePayload` mirrors `buildShareMessage` + reuses `money()` so `hideAmounts` masking is identical; no new i18n keys. **Deferred**: M4 seasonal home widget.
- **Inflation Shield (ABA-346)**: forecasts each tracked product's price from receipt history and recommends what to stock up on now. Deterministic, no LLM cost, free for every tier. **Moved to the wiki** — `docs/wiki/features/inflation-shield.md`. Carried here: `projectedSaving` is **halved** (a linear-ramp avoided-cost model, not the full end-gap) and all copy must frame it as an estimate; and a new `WidgetKey` needs three entries — `WIDGET_KEYS`, the `HomeWidgetSwitch` case, and `settings/widgets.tsx`'s exhaustive label map.
- **Fat Finder audit currency (ABA-386)**: `modules/insights/fat-finder.service.ts` — `generateReport(accountId, language?, forceRegenerate?, userId?, month?, year?, baseCurrency = 'USD')`. The report is labelled AND computed in the **caller's display currency** (`insights.controller.ts` passes `req.user.currencyCode || 'USD'`, same as safe-to-spend/wrapped/inflation-shield); it is **never** inferred from an expense row. The old `const currencyCode = expenses[0].currencyCode` took the currency of whichever row was *newest* (`orderBy date desc`), so a PLN account with one small USD charge produced a whole audit "in dollars" with PLN+EUR+USD summed blind — do not reintroduce that. Every expense is FX-converted (`getRatesSafe`/`convertAmount`, mirroring `SafeToSpendService`) into a single `analysis[]` array **before** any aggregation, so subscriptions/large-one-offs/category-trends/totals all read one currency; rates are only fetched when the account actually mixes currencies. A row with an unknown rate (or every non-base row when the provider is down) is **excluded, not mislabelled**, setting `fxApproximate` (wrapped/safe-to-spend convention); `fxConverted` marks that some amount was converted. Both flags ride on the response (optional fields on `FatFinderResponse`) — the *user-facing* "converted figures are approximate" wording is produced by the LLM from a prompt instruction, so there are **no new i18n keys**. The prompt states every amount is already in the report currency and must be written in it. **Cache**: the DB row is keyed `(accountId, periodStart, periodEnd)` only, so a cached report whose `currencyCode !== baseCurrency` is **regenerated instead of served** — that is what lets already-stored wrong-currency rows self-heal instead of persisting for their 30-day TTL. 
- **Spending Story audit currency + shared FX helpers (ABA-387)**: `story.service.ts` had the same defect as ABA-386 — `currentExpenses[0]?.currencyCode || budgets[0]?.currencyCode || 'USD'`, and since that query is `orderBy: { amount: 'desc' }` the **largest single charge** decided the currency of the whole narrative. Now `getSpendingStory(..., baseCurrency = 'USD')` takes the caller's display currency from the controller (`req.user.currencyCode`) and every amount is converted before aggregation: current + previous expenses, incomes, and **both budget figures** (`BudgetsService.getProgress` reports in the *budget's* currency because it filters expenses by it — `percentUsed` is a ratio and must NOT be converted). The top-5 expense list is re-sorted by the **converted** amount (native `amount desc` is meaningless across currencies). Unknown-rate amounts are excluded + `fxApproximate`, mirroring fat-finder. New nullable `SpendingStory.currencyCode` (migration `20260803180000_add_spending_story_currency`) is part of the cache-validity check alongside the existing `periodLabel` match — a story cached in another currency (or a pre-migration `null`) is regenerated, at the cost of one extra 3-request generation per period. **Shared helpers**: `common/utils/fx.ts` (`getRatesSafe(provider, base)` + pure `convertAmount(amount, from, base, rates)`) is the canonical home of the `amount_in_base = amount / rates[from]` formula. Originally only fat-finder and story imported it while `ai-tools.service.ts`, `safe-to-spend.service.ts`, `wrapped.service.ts`, `inflation-shield.service.ts` and `trip-settle-up.service.ts` each carried their own private copy (tech-debt `fx-rate-conversion-helper-quintupled`); all five have since been migrated onto the shared util and their private copies deleted — `ai-tools.service.ts` keeps a thin `private getRatesSafe(base?: string)` wrapper only to preserve its own optional-base short-circuit, but it delegates to the shared implementation. **New FX-consuming code must import the util, not add a private copy.**
- **Auto-capture spend gate (ABA-387)**: the Android capture allow-list is per **app**, not per **notification**, so any push from an allow-listed bank app containing a decimal number + a currency token became an expense. That is how a Revolut-style crypto price alert ("…up 5.32% in the past 2 hours. It's now $59,123.45") was booked as **5.32 USD** with merchant "The Past 2 Hours. It's Now" (the percentage read as the amount, `$` as the currency, and the German/Dutch `in <merchant>` connector as the payee) — 8 such rows reached prod in one account. `notificationParser/generic.ts` now exports `maskPercentages` (a percentage is never an amount; masking applies to the **amount** lookup only — currency/merchant still read the original text), `hasSpendIntent`/`isDeclined`/`looksLikeSpendNotification` (a push must carry spend wording in one of the 9 supported languages — deliberately including the same keywords the per-bank templates anchor on, so gating never rejects a push a template was written for — **or** an explicit debit sign on the amount; declined/rejected/insufficient-funds is always rejected), and `extractAmountSigned`. `notificationParser/index.ts` applies the gate to **BOTH** parse paths and masks percentages before the template amount match: six of the eleven templates (Pekao, Santander, Alior, BNP, Credit Agricole, Nest) use the bare `AMOUNT_PATTERN_PL`, which matches any decimal in the text, so a loan ad's "RRSO 8,99%" was equally capturable. Two recall bugs fixed in the same pass: `SYMBOL_TO_ISO` was missing the ISO code **`EUR`** (only `€`), so every "12,34 EUR" push from a non-PL bank was dropped as "no currency detected"; and `CURRENCY_RE` now word-bounds ASCII codes so `Br` no longer matches the "br" inside "brutto" → BYN (the boundary-less regex is still why NOK/SEK/RON stay out of `DETECTABLE_UNSUPPORTED`). Tests: `notificationParser/__tests__/{generic,parseNotification}.test.ts` carry the real production strings.
- **Share image mechanism (ABA-353)**: `src/components/share/ShareImageCard.tsx` — the shared hidden-`react-native-webview` → HTML/canvas (1080×1920 story card) → `canvas.toDataURL('image/png')` → `expo-file-system` write → `expo-sharing` `shareAsync` mechanism, extracted from what was a ~540-line byte-faithful duplicate between `WrappedShareCard` and `InflationShieldShareCard` (each previously re-implemented the same webview bridge, 8s timeout, single-in-flight guard, and never-throws error handling). `ShareImageCard` takes `renderFnName` (the `window.<name>` bridge the injected JS calls — must be unique per mounted card), `gradientFrom`/`gradientTo`, and `fileNamePrefix` as props, plus a `ShareCardPayload { fileTag, title, lines, footer }` at `share()`-time; `forwardRef` `share(payload): Promise<boolean>`, never throws. `ShareImageCard.web.tsx` is the shared no-op (keeps `react-native-webview`/`expo-file-system` out of the web bundle). `WrappedShareCard`/`InflationShieldShareCard` are now thin wrappers owning only their gradient/file-prefix/payload-shape; their own `.web.tsx` no-ops are unchanged (kept standalone — each is a required, separately-typed Metro platform-extension file, not part of the duplication). A future third shareable-card feature should add a new thin wrapper over `ShareImageCard`, not a fourth copy of the mechanism. No component-level test was added (this codebase's WebView-bridge components, e.g. `ExpenseMapView`, only unit-test their pure helper functions — there's no `react-test-renderer`/`@testing-library/react-native` dependency to render `ShareImageCard` itself).
- **Bank-notification auto-capture (ABA-294)**: Android-only native `NotificationListenerService` (`apps/mobile/android/…/notifications/BankNotificationListenerService.kt`) extending `android.service.notification.NotificationListenerService`. Implemented as a **legacy Old-Arch `ReactContextBaseJavaModule`** (no TurboModule/codegen — avoids Windows MAX_PATH issue identical to keyboard-controller). `NotificationCaptureModule.kt` + `NotificationCapturePackage.kt` registered manually in `MainApplication.kt:getPackages()` (documented seam). `MainActivity.kt` untouched. AndroidManifest: service block with `BIND_NOTIFICATION_LISTENER_SERVICE` permission + intent-filter. JS bridge: `notificationCapture.ts` subscribes to `onBankNotification` DeviceEventEmitter event (payload: `packageName`, `title`, `text`, `postedAt`). Allow-list-first filter; on-device parsing only (text never leaves the device). **New-Arch event emission (ABA-297)**: MUST use `reactContext.emitDeviceEvent(EVENT_NAME, params)` — `getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java).emit()` is silently swallowed in RN 0.81 New Architecture (caught by `catch(_: Exception){}` with no log, so events never reach JS). `BankNotificationListenerService.emitEvent` accepts `ReactApplicationContext?`; when null/not-ready, stores the event in `pendingEvent: WritableMap?` (one-slot volatile queue) and `NotificationCaptureModule.init` flushes it via `runOnJSQueueThread`. **Subscription timing**: `subscribeToCapture()` in `_layout.tsx` uses an empty-deps `useEffect` (runs on mount) — NOT gated on `isAuthenticated`/`fontsLoaded`/`isInitializing`; gating causes a startup race where Kotlin emits before JS registers its listener. The Kotlin gate (SharedPreferences flag) is the source of truth. **PKO BP template**: `amountRegex` must include `Obci[ąa][żz]enie(?:\s+karty?)?` — PKO BP card debits use "Obciążenie kartą", not "Płatność" (ABA-297 fix, `templates.pl.ts`). **externalRef**: `expenseStore.addExpense` passes `externalRef` to `api.createExpense` so the server `@@unique([accountId, externalRef])` can deduplicate across devices. **Multi-country (ABA-295)**: allow-list expanded to ~43 verified bank packages across 8 European markets (PL/DE/AT/ES/FR/NL/UA/RU/BY — kept in sync between the Kotlin `BANK_PACKAGES` set and JS `BANK_PACKAGES_DISPLAY`, which carries a `country` field; opt-in screen groups banks by country). JS pipeline: `notificationParser/index.ts` tries the per-bank PL template (`templates.pl.ts`) first, then falls back to a **generic country-agnostic parser** (`notificationParser/generic.ts`: amount in EU comma-decimal + Anglo dot-decimal; currency by symbol/ISO mapped to the supported `Currency` union — unsupported (CHF/CZK/SEK…) returns null + `console.warn`, never a wrong-currency expense; merchant via language-aware connectors `bei`/`chez`/`à`/`en`/`bij`/`at`/`в`/`у`/`w`). `normalizeMerchantWithPLOverride` applies the PL canonical brand map on top of the generic `normalizeMerchant`, so a PL brand in a non-PL bank's push still canonicalizes; category suggestion stays PL-only (`suggestCategoryFromMerchantPL`), non-PL merchants land uncategorized and `merchantRulesStore` learns over time. **CHF not yet in `Currency`** — Swiss captures are skipped until `'CHF'` is added to `primitives.ts`/`SUPPORTED_CURRENCIES`. Then: `merchantRulesStore` → dedup check (`externalRef = 'notif:<sha256(pkg|amt|merchant|date).slice(0,16)>'` against `@@unique([accountId, externalRef])`) → `expenseStore.addExpense({ source: 'notification', externalRef, … })` offline-first. Failures logged `console.warn`, never `console.error`. UX feedback: local toast "Captured X · Merchant — tap to review" deep-linking to `expense/[id]`. Opt-in screen `app/settings/auto-capture.tsx` (Android-only; linked from import hub). iOS/web: `NotificationCapture.ios.ts` no-op stub (`isPermissionGranted()=false`). `ExpenseSource` += `'notification'` (additive plain-string column, no migration). Play Store note: `BIND_NOTIFICATION_LISTENER_SERVICE` is a sensitive permission requiring justification; may delay review.
- **Receipt item regeneration + autocapture/import merge suggestion (ABA-568)**: two gaps when an expense exists BEFORE its receipt. (1) Regeneration: `ReceiptSection` ("Extract items") re-runs `POST /ai/scan-receipt` (photo or PDF) over the attached receipt and applies the lines to the existing expense (replace-with-confirm, never append); `ExpenseItemsSection` now renders for any expense with an attached receipt (`hasReceipt` via `onReceiptLoaded`), not just `source:'ocr'` — mirrored in desktop `ExpenseDialog` (same shared sections, same condition) and tracked in its mirror comment. New `expenseDetail.extract*` keys × 9 locales; one OCR request per regeneration (existing `@TrackAiUsage('ocr', 2.0)`). (2) Merge suggestion: when an auto-captured/imported expense is later scanned as a separate OCR expense, `detectDuplicateCharge` marks that pair with `params.suggestMerge` (one side `notification`/`import`, other side `ocr`; no new alert type, same `dup:` dedup, same push text) and both surfaces route it to `/expense/merge` — mobile `app/alerts` and desktop via the shared `isMergeableDuplicate()` + `alertAction()` in `features/dashboard/attentionActions.ts` (used by `useAlertTapThrough`; unit-tested in `attentionActions.test.ts`; `mergeTargets()` covers both alert shapes). `mergeExpenses` (server) now also re-points the merged row's line items to the survivor when the survivor has none (image was already gap-filled). Origin badges: `ExpenseListItem` (mobile) + `TransactionTable` (desktop) show `expenseDetail.sourceImport`/`sourceNotification` for `import`/`notification` rows; detail screens gained the `notification` label/icon. Attach and anomaly are deliberately separate: saving a receipt image fires NO anomaly check.
- **Notification-capture dedup (ABA-296)**: two-tier cross-source reconciliation for `source:'notification'` stubs. Shared predicate: `expensePayee()` (merchant || description, lowercased/trimmed) and `DUP_DAY_MS` exported from `anomaly.service.ts` — single source of truth shared with `duplicate_charge` detector. **Tier 1 auto-dedup (same currency, `P`):** Case A = `ExpensesService.reconcileNotificationStub` — when a non-`notification` expense is created (`isNew`, `source !== 'notification'`), soft-deletes a matching `source:'notification'` stub (same amount/currency/date ±1d/payee); runs BEFORE the fire-and-forget `checkExpense` so the suppressed stub is never a `duplicate_charge` candidate; only notification stubs are deletion candidates — two genuine non-notification expenses are never deleted. Case B = `captureService` `contentMatch.ts` — before `expenseStore.addExpense`, content-matches the parsed push against local `expenseStore.expenses` via predicate `P`; skips the create entirely if a match exists (placing the skip on the client avoids client/server row divergence). `@@index([accountId, source])` (migration `add_expense_source_index`) keeps the stub lookup cheap. **Tier 2 suggest-merge (cross-currency, `Q`):** `P` and `Q` are mutually exclusive — `P` requires same currency, `Q` requires different currency, so a pair is auto-deduped XOR suggested, never both. `AnomalyService.detectPossibleMerge` fires after `detectDuplicateCharge` from `checkExpense`: finds a `Q`-matching expense (same payee + date ±1d, currency differs), inserts a `possible_merge` alert (dedupKey `merge:{sortedId1}:{sortedId2}` is order-independent → once per pair ever). `ImportBankService.flagPossibleMerges` adds a pass after `flagContentDuplicates` in `buildPreviewResponse`, marking `possibleMerge: true` / `mergeCandidateIds` on eligible rows without changing import/skip counts — a `possibleMerge` row stays importable by default. `POST /expenses/merge` (ViewerBlockGuard, clientId resolution via `OR:[{id},{clientId}]`) merges two expenses: gap-fill carried fields, tag union, project carry via `project_expenses` join, soft-delete secondary + `syncVersion` bump on both rows (propagates via standard sync pull). Mobile: `app/expense/merge.tsx` — survivor picker (default: import/settlement-currency row) + field choices; entry via `possible_merge` alert tap (`aId`/`bId` params) or import-preview row; `expenseStore.mergeExpenses` (optimistic + offline-first, fire-and-forget server call with `console.warn` on failure); `api.mergeExpenses` in `expenses.api.ts`. `possible_merge` is a plain string in `anomaly_alerts.type` (no migration); `AnomalyAlertType` += `'possible_merge'`; new `MergeExpensesDto`. i18n: `alerts.mergeTitle/mergeBody`, `expenses.merge.*`, `bankImport.possibleMerge/mergeWithExisting/importAsNew` (all 9 locales). Tier 2 never auto-acts — only `POST /expenses/merge` mutates.
- **Personal Inflation Index (ABA-307)**: `modules/price-history/` — tracks price history per product per store from OCR receipt line items and computes a personal Laspeyres inflation index. **Data model**: `expense_items.canonical_name TEXT` (populated by OCR, NULL for manual items; migration `20260702160001_add_expense_item_canonical_name`); `product_aliases` table (`@@unique([accountId, rawName])`, stores user overrides; migration `20260702160002_add_product_aliases`). **OCR extension**: `ReceiptItem.canonicalName` added to prompt — per `ocr.service.ts`'s `canonicalName rules` block, the model KEEPS per-unit weight/volume, fat/alcohol percentage, and flavour/variant descriptors, and STRIPS only pack-quantity multipliers, product codes, and PLU numbers: "MLEKO 3,2% ŁACIATE 1L 6SZT" → "Mleko Łaciate 3,2% 1L" (size is never stripped — do not assume canonicalName is size-agnostic) + fallback `buildCanonicalNameFallback(description)` (up to 3 meaningful tokens ≥3 chars, not purely numeric/unit — e.g. "JOGURT ACTIVIA TRUSKAWKOWY" instead of just "JOGURT"; backfill migrations `20260702160003` v1 + `20260702160004` v2 re-backfill existing rows; single-word entries from v1 are overwritten by v2, multi-word LLM-generated names preserved); write path: `ExpensesService.create()` + `SyncService` both persist `canonicalName`. **Discount line folding (ABA-343)**: some receipts (Polish Biedronka/Lidl/Żabka) print each discount on its own line and the OCR model sometimes emits those as negative line items ("OPUST PIWO HEINEKEN -8,00", "Lidl Plus kupon -6,87"). `OcrService.validateAndNormalizeReceipt` calls pure `extractReceiptDiscounts` (`modules/ai/utils/receipt-discount.ts`, unit-tested): a line is a discount when its amount is **negative AND** carries a discount label (`isDiscountLine`, regex across pl/en/de/es/it/ua/ru — the negative gate is the safety net so a positive product with a label-like brand name is never pulled). Discount lines are removed from `items` and their value folded into `parsed.discount` (`max(existingDiscount, Σ pulled lines)` — no double count). It only cleans the item list + the informational discount; the paid total (`amount` = OCR `total`) is **never** touched. Forward-only (new scans); existing rows unaffected. **Inflation formula**: Laspeyres weighted index — `inflationIndex = Σ(weight_i × priceChangePct_i) / Σ(weight_i)` where `weight_i = baseAvgPrice × purchaseCount_in_base`; `null` when `productCount < 3`; base period = `[now - period*2 .. now - period]`, current = `[now - period .. now]` using calendar-month boundaries. Multi-currency: majority currency by row count. **5 endpoints** (class-level `JwtAuthGuard + AccountContextGuard`, no `@RequireTier` — free): `GET /price-history?period=3m|6m|12m` (Redis cache `ph:{accountId}:{period}` TTL 300s), `GET /price-history/products`, `PATCH /price-history/products/alias` (ViewerBlockGuard), `DELETE /price-history/products/alias/:rawName` (ViewerBlockGuard), `POST /price-history/products/merge` (ViewerBlockGuard), `POST /price-history/products/backfill-ai` (ABA-308, ViewerBlockGuard — re-generates canonical names for NULL/single-word entries using GPT-4o-mini; deduplicates descriptions, batches 50 at a time, max 500 unique per call; user aliases never overwritten; ~$0.001/1000 descriptions; mobile "Re-analyze with AI" button in products screen). Route order: static paths before `/:rawName`. **Name resolution**: `COALESCE(alias.canonicalName, item.canonicalName)` — alias overrides LLM value; merging = two rawNames share the same alias canonicalName. `PriceHistoryProduct.rawName` carries the original `expense_items.canonical_name` key (pre-alias) so mobile rename calls always update the correct alias row. **Mobile**: `priceHistoryStore.ts` (server-only, same pattern as `familyFeedStore`); `priceHistory.api.ts`; `InflationIndexSection.tsx` in Analytics tab (period chips, product list top-3 + expand, per-product bottom sheet: `InteractiveLineChart` + cheapest-first store comparison + rename — `canEdit`-gated); `app/settings/products.tsx` management screen (multi-select merge, rename alias, delete alias, `canEdit`-gated); linked from Settings → Reference Data hub. SQLite: `ALTER TABLE expense_items ADD COLUMN canonical_name TEXT` in `client.native.ts`. 29 `priceHistory.*` i18n keys (19 original + 6 products-screen UX keys + 4 AI-backfill keys) + `settingsNav.products/productsDesc` in all 9 locales. `priceHistoryStore.isLoadingProducts` flag drives ActivityIndicator in products screen while initial load runs.
- **Community Price Map (ABA-335)**: a crowdsourced, k-anonymized “where's cheapest” database built from all accounts' OCR'd line items. **Moved to the wiki** — `docs/wiki/features/community-prices.md`. Carried here: reads are Pro-gated AND behind `COMMUNITY_PRICE_READ_ENABLED`, which defaults **OFF** — the surface is dark in production pending anti-Sybil work, and that also keeps the community fallback in the receipt price check unwired; and the observation table holds no accountId, userId or user coordinates, only a one-way `contributorKey` that never leaves the server.
- **Receipt price check / чек-контроль (ABA-373)**: each scanned line compared against the median of what this user previously paid for that product at that store. **Moved to the wiki** — `docs/wiki/features/receipt-price-check.md`. Carried here, and binding on all copy in all languages: the data cannot prove a promotion failed to apply, so nothing may say "overcharged" or "promo not applied" — only "more expensive than usual, worth checking". `RECEIPT_CHECK_ALERTS_ENABLED` is still OFF in production.
- **Receipt category auto-split (ABA-398)**: splits a scanned receipt's own line items across expense categories at scan time — no new endpoint, no LLM in any write path. **Moved to the wiki** — `docs/wiki/features/receipt-category-split.md`. Carried here because they bite outside the feature: the model is never shown or asked for an amount, only line indexes and category NAMES; `buildCategorySplits` is a deliberately duplicated pair (`apps/api/src/common/utils/` canonical, `packages/shared-utils` mirror) for the same reason as `financial-month.ts`; `Expense.depositAmount` must be written at all three `ExpensesService` write sites AND listed in `findAll`'s explicit `select`; and its mobile i18n namespace is `receiptCategorySplit`, never `receiptSplit` (that belongs to guest-link bill splitting).
- **Receipt splitting / guest links**: the payer splits a shared bill among friends who do not have the app; each gets a public, unauthenticated guest link. **Moved to the wiki** — `docs/wiki/features/receipt-split.md` (guest surface, split-receivable accounting, payment-method resolution) and `docs/wiki/features/receipt-split-item-shares.md` (per-line claims, shared lines, explicit percentages, discount scaling, disputes, reassignment). The two invariants worth carrying here: `GuestController` is the app's ONLY unauthenticated surface and its whole `s` subtree is excluded from the `/api/v1` prefix by one wildcard in `global-prefix-exclusions.ts`; and split rows are excluded from spend totals by `isSplitReceivable`, **never** by `isDebt` — for a standalone cash loan the debt row IS the outflow.
- **Smart Shopping List (ABA-330)**: shared, offline-first lists plus a Pro-gated basket comparison, restock predictions and deal detection. **Moved to the wiki** — `docs/wiki/features/shopping-list.md`. Carried here: this module does NOT use the generic `/sync` machinery — it has its own SQLite mirror and REST pull-merge, the local row `id` stays the `clientId` permanently, and rows are marked synced ONLY on the server ack.
- **Receipt-scan reconciliation (ABA-531, ABA-545)**: scanning a receipt auto-checks matching shopping-list items. **Moved to the wiki** — `docs/wiki/features/shopping-list.md`. Carried here: matching is **exact-after-normalization, never fuzzy** (a false-positive auto-check is worse than a missed one), and the two implementations are deliberately separate — the app's own scans and every bot both use `source: 'ocr'` with nothing to tell them apart, so wiring it into the shared create path would double-run it.
- **Store Arrival card + Shopping Mode live on `feature/shopping-mode`, NOT on `development` (ABA-438)**: the pair (ABA-404 home card, ABA-408 foreground-service location session, ABA-437 branch clustering) was lifted off the mainline because `FOREGROUND_SERVICE_LOCATION` in `AndroidManifest.xml` makes Google Play refuse the **track update** for every submit until the Foreground service permissions declaration is filled in Play Console — so one unreleased feature froze the release pipeline for the whole app. Removed from `development` by a single forward commit (no history rewrite): the feature-only files, the `storeArrival` `WidgetKey` and its `HomeWidgetSwitch` case, the location-task registration in `index.js`, the sweep/list-sync hooks in `app/_layout.tsx`, the sign-out stop in `authStore`, the button on `app/shopping-list/index.tsx`, both manifest permissions, the `shoppingMode`/`storeArrival` i18n objects in all 9 locales, and the Shopping Mode section of `user_docs/*/38-shopping-list.md`. `expo-location`/`expo-task-manager` stay in `package.json` — `locationCapture` still needs the former and the branch needs both back. To re-land: fill the Play declaration first, then merge the branch and revert that removal commit; do NOT cherry-pick the 40 original commits, they are interleaved with unrelated ABA work.
- **Expense geo-location & map (ABA-310)**: expenses carry `location: {lat, lng, name?}`, shown on a map with **no native map module** (WebView + inlined Leaflet). **Moved to the wiki** — `docs/wiki/features/expense-location-and-map.md`. Carried here: `buildLocationColumns` must be spread into ALL THREE Prisma write sites (create, the create-upsert update branch, and `update()`) — the update branch was missing once and silently dropped location on offline retry; `(0,0)` means "no location" everywhere (it is the zeroed plaintext of an undecryptable E2EE row); and `GeocodingService` must stay ONE DI instance, because its Nominatim throttle is instance-level state.
- **App version gate**: `modules/app-versions/` — public `GET /app-versions/check?platform=ios|android&version=x.y.z` (no auth, mobile calls before login) returns `{latestVersion, minSupportedVersion, isUpdateAvailable, isUpdateRequired, releaseNotes, storeUrl}`. Admin CRUD under `/admin/app-versions` (behind `JwtAuthGuard + AdminGuard`). Latest row per platform = `ORDER BY publishedAt DESC LIMIT 1`. Service enforces `latestVersion >= minSupportedVersion` via `utils/semver.ts`. Default Play Store URL hardcoded; iOS storeUrl is a placeholder until App Store ID is assigned (admin overrides per row).
- **Subscription pricing (ABA-320, consolidated in tech-debt `pricing-table-triple-copy`)**: current Pro = **$4.99/mo, $29.99/yr** (cut from $9.99/$95.88 to improve free→paid conversion); Business unchanged at $19.99/mo, $191.88/yr. **Single source of truth**: `apps/api/src/modules/subscriptions/pricing-data.json` (amounts in minor units + display symbol, per currency). Three consumers import/read it directly — no more hand-synced copies: `subscriptions.service.ts` (`import * as pricingData from './pricing-data.json'` — needs `resolveJsonModule: true` in `apps/api/tsconfig.json`; use a **namespace import**, not a default import, since the API has no `esModuleInterop` and a default import compiles to `.default` which is `undefined` for a plain `require()`'d JSON module) drives `getPlans()`, i.e. what the app displays; `apps/api/src/modules/subscriptions/scripts/setup-stripe-products.ts` derives its lowercase-currency-keyed `CURRENCIES` from the same file (used only when actually re-provisioning Stripe); `docs/marketing/landing/build_landing.py` `json.load()`s the same file directly (converting minor→major units) to build `CURRENCY_PRICING`, which drives the static `/pricing/` marketing page — regenerate with `LANDING_BASE= ROBOTS="index,follow,max-image-preview:large" python docs/marketing/landing/build_landing.py` (the **default env-less invocation builds a `noindex` preview variant** that will silently overwrite the committed production site with wrong robots/base-path if you forget the env vars). A price-amount change now touches exactly one file. `subscriptions.service.spec.ts` has a `pricing-data.json` describe-block asserting the JSON's shape and that `getPlans()` reads through it (fails fast on a malformed edit instead of shipping quietly broken pricing). **The displayed price and the price Stripe actually charges remain two independent systems** — `getPlans()`'s amounts are cosmetic; the real charge comes from a Stripe Price object whose ID is read from an env var (`STRIPE_{PRO,BUSINESS}_{MONTHLY,YEARLY}_PRICE_ID_{CURRENCY}`) in `resolvePriceId()`/`createCheckoutSession` — this was deliberately NOT folded into `pricing-data.json` (env-driven, per-environment test/live IDs, not a build-time constant). Stripe Prices are immutable — changing a real live price requires (1) running `setup-stripe-products.ts` (creates brand-new Products + Prices every run, NOT idempotent — do not re-run casually) with a live `STRIPE_SECRET_KEY`, ideally executed **inside the prod API container** (`docker cp` the script in, `docker exec -w /app/apps/api budget-api-prod node script.js`) so the live secret never has to leave the server; (2) archiving the old Products/Prices (`active:false` — Stripe has no real delete; archiving only blocks *new* checkouts, existing subscriptions keep renewing at the old price untouched); (3) updating all 24 `STRIPE_*_PRICE_ID_*` lines in `.env.production` (back it up first) with the new IDs; (4) `docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate api`; (5) editing `pricing-data.json`'s amounts so the displayed price catches up with what's actually being charged — until that deploy lands there's a window where Stripe already charges the new price but the app still displays the old one. A stray un-tagged legacy Stripe product from an earlier pricing iteration (no `tier` metadata, incomplete currency coverage) was found during the ABA-320 migration and deliberately left alone rather than guessed at — when hunting for "the old Pro/Business product" to archive, match on `metadata.tier` AND verify the price amounts against the pricing table you're retiring, since `products.list().find()` can grab the wrong same-tier product if more than one exists. The yearly-discount **badge text must not hardcode a percentage** (landing `toggle_save` key, mobile `subscription.tsx`) — Pro and Business now have different yearly discount ratios (~69% vs 20%), so the landing badge is fixed non-numeric copy ("Save with yearly") and the mobile badge computes `Math.round((1 - plan.yearly.amount/(plan.monthly.amount*12))*100)` live from `getPlans()`'s response instead.
- **Invite existing users by search (ABA-309)**: lets an account owner search for an already-registered user by name/email and invite them directly, instead of only by email/code/link. **Data model**: `AccountInvitation.invitedUserId String?` — plain field mirroring `invitedBy`/`acceptedBy` (no Prisma `@relation`), migration `20260703122200_add_invitation_invited_user_id`. "My Invitations" (the invitee's view) matches any pending invitation where `invitedUserId = currentUserId` OR `invitedEmail = currentUser.email` — folds in existing email-based invites too, when the typed email happens to match a registered user. **Endpoints**: `GET /users/search?q=<term>` (`UsersController`) — case-insensitive substring match on name/email, min 2 chars, capped at 20, excludes requester + deactivated users, returns only `{id, name, email}`; guarded by `ThrottlerGuard` + `@Throttle({default:{limit:20,ttl:60000}})` — the first route in this codebase to actually consume the globally-configured-but-previously-unused `ThrottlerModule`. `CreateInvitationDto.invitedUserId` — when set, `AccountsService.createInvitation` validates the user isn't already a member, creates the invitation, and sends a **push** (not an email) via `NotificationsService.sendToUser(..., 'account_invitation')`. `GET /accounts/invitations/mine` — current user's pending invitations, joined with inviter name + account name + role. `PATCH /accounts/invitations/:id/respond` (`{action:'accept'|'decline'}`) — verifies the invitation is addressed to the responder (`invitedUserId` or `invitedEmail` match) **before** any other logic (closes an IDOR where an invitation id could be guessed and hijacked), then enforces `expiresAt` the same way `acceptInvitation` already does (search-based invitations have no cron to expire them otherwise — the check must live in this handler too, or a past-expiry invitation stays acceptable forever). `getMyInvitations` must short-circuit `if (!me) return []` before building the `OR` filter — otherwise a stale/deleted-user JWT causes Prisma to silently drop the `undefined invitedEmail` clause, leaving an `OR` that matches every pending invitation system-wide. New `NotificationType = 'account_invitation'` is **always sent, with no per-type preference toggle** (unlike every other notification type) — a direct one-off action request, not a recurring background alert; `notification-i18n.ts` has `accountInvitationTitle`/`accountInvitationBody` in all 9 languages. **Mobile**: `invitationStore.ts` (server-only, optimistic accept/decline with rollback — mirrors `purchaseRequestStore`'s pattern); `app/alerts/index.tsx` gained a tab control (Alerts / Invitations) + new `InvitationCard` component (Accept/Decline buttons — a new interaction pattern for this screen, previously tap-to-navigate/dismiss-only); the bell badge sums `unreadCount + invitations.length`, and `loadInvitations()` runs alongside `loadAlerts()` in both real call sites of `useHomeScreenData.ts` (focus effect + pull-to-refresh) so the badge is populated even before the user opens the Alerts screen — do NOT reintroduce a badge sum that reads only `useAlertStore`. `account_invitation` push deep-links to `/alerts?tab=invitations`. `account/invite.tsx` gained a third "Find user" mode alongside Link/Email — debounced (~300ms) search, selecting a result reveals the existing role picker, success screen shows no invite code (nothing to share, already delivered via push). 13 i18n keys (7 `alerts.*`, 6 `accounts.*`) across all 9 locales.
- **Budgets (ABA-171, ABA-314, ABA-383, ABA-523, ABA-529, ABA-537)**: periods, the financial-month anchor, split-aware progress, the end-of-period projection, category threshold pushes, history, and the `budgetStore` composition. **Moved to the wiki** — `docs/wiki/features/budgets.md`. Carried here: a `Budget` has **no** `categoryId` — category filtering is only through `BudgetCategory` allocations; `financial-month.ts`, `budget-projection.ts` and `category-attribution.ts` are each a deliberately duplicated pair (canonical `apps/api/src/common/utils/`, mirror `packages/shared-utils`) — change both sides together; and the projection's rate excludes the single largest day while `projectedTotal` stays non-nullable (a declined projection falls back to `spent`).
- **Account transfers (ABA-388)**: moving money between two accounts the user belongs to, with its own offline write queue rather than the generic `/sync` path. **Moved to the wiki** — `docs/wiki/features/account-transfers.md`. Carried here: `GET /wallet/summaries` keeps the class-level `AccountContextGuard` but deliberately ignores `req.accountId`, enumerating membership from the user instead; and it shares `buildWalletBalanceRow` with `getSummary`, because a transfer form quoting a different balance than the wallet screen is worse than no balance at all.
- **Wallet currency visibility (ABA-431)**: the wallet shows a card for every currency an account actually holds money in, not only the ones somebody set an initial balance for. It used to render exactly one card per `wallet_balances` row, and that table is written **only** by the "set balance" screen — so a production shared account with **124 484 USD** of income showed just its PLN card, and 36 of the ~43 account+currency pairs holding money prod-wide had no row at all (the bug was the normal state, not an edge case). The same `.map()` over wallet rows existed in **three** places and all three are fixed: `WalletService.getSummary` (wallet screen), `WalletService.getSummariesForAccounts` (the transfer form's `Available:` line, ABA-388) and **`walletStore.computeWalletSummary` — the mobile app computes its own summary locally, so that copy is what the phone actually renders**. Meanwhile `getMonthlyBalanceHistory` had always derived its currency set from the transactions, which is why one screen could show USD in the monthly chart while having no USD balance card. **Rule**: visible = live rows ∪ currencies with movements that have **no row at all**. A currency whose only row is **soft-deleted stays hidden even with movements** — that row is the user having hidden it (`WalletService.remove`), and hiding must survive the next transaction in it; this is exactly why the set is not simply re-derived from movements on every read, and why the existence lookup must NOT filter `isDeleted`. Pure `resolveWalletCurrencies` is a **deliberately duplicated pair** (canonical `apps/api/src/common/utils/wallet-currencies.ts`, mirror `packages/shared-utils/src/formatting/wallet-currencies.ts` consumed by mobile's `features/wallet/walletSummary.ts`) — same convention and same reason as `financial-month.ts`: the API has no build step and must not import `@budget/shared-utils` at runtime. Same rule table on both sides. **Persistence**: `WalletCurrencyService.ensureCurrencies(accountId, userId | undefined, codes)` (`modules/wallet/wallet-currency.service.ts`) creates the missing row at `initialAmount 0`. Prisma-only leaf in its own tiny `WalletCurrencyModule` so the write paths can depend on it without a module cycle (the `InflationShieldTrackingService` shape); idempotent via `createMany({ skipDuplicates: true })`; **never throws** (every caller is fire-and-forget); **must be called OUTSIDE any `$transaction`** — a constraint violation aborts the whole Postgres transaction (ABA-313/401). `wallet_balances.user_id` is NOT NULL and the read path does not always know who is asking (`SafeToSpendService` calls `getSummary` with no user), so it falls back to the account owner — **filtered** on `role: 'owner'`, never `orderBy: { role }`, since alphabetically `editor` sorts ahead of `owner`. **Called from both sides on purpose**: on write from `ExpenseCreatedHooksService.onExpenseCreated` (the documented home for post-create hooks, ABA-425) and `IncomesService.create`, **and** fire-and-forget from the read path itself — so a write path nobody instrumented (import commits, the subscription auto-charge cron, debts, anything added later) still displays correctly instead of silently reintroducing this bug. Do NOT drop the read-side derivation on the grounds that the writes now cover it. Migration `20260821120000_backfill_wallet_currency_rows` backfills the pre-existing pairs (data-only, mirrors the six money sources `getSummary` aggregates, `NOT EXISTS` without an `isDeleted` filter so a hidden currency is not resurrected); verified against prod inside a rolled-back transaction as `INSERT 0 36`.
- **Exchange-rate watch alerts**: a personal, per-`userId` “notify me when this pair hits my target” watch. **Moved to the wiki** — `docs/wiki/features/exchange-rate-alerts.md`. Carried here: it is one-shot, so the row is marked inactive BEFORE the push and rolled back if the send fails — a lost push must not permanently lose an alert with no other surface reporting it; and the hourly cron groups watches by base currency before calling the **existing singleton** `ExchangeRateService` (do not provide a second one).
- **Wise CSV import**: `modules/import-wise/` — `POST /import/wise/preview` (multipart CSV ≤5 MB, `papaparse`, strips BOM) classifies rows as `expense` (Amount<0) / `income` (Amount>0) / `fx` (paired conversion rows sharing `Payment Reference + Date + opposite sign`, emitted as `CurrencyExchange` not `AccountTransfer` since Wise FX is in-wallet), folds `Total fees` into amount, suggests categories via `MERCHANT_CATEGORY_HINTS`. `POST /import/wise/commit` writes in one Prisma `$transaction` with `source: 'import'`, `externalRef = 'wise:<TransferWise ID>'`. **Dedup**: `Expense`/`Income`/`CurrencyExchange` each have `externalRef String?` with `@@unique([accountId, externalRef])` (NULLs distinct in Postgres). Mobile `app/settings/wise-import.tsx` (5-state machine, `expo-document-picker`); reached via the `/settings/import` hub.
- **Polish bank import** (ABA-126): `modules/import-bank/` — strategy-registry module for CSV/PDF statement imports. Banks: `mbank`, `pko`, `revolut`, `ing`, `millennium`, `pekao` + `universal` column-mapping fallback. 5 endpoints under `/import/bank/*` (`JwtAuthGuard + AccountContextGuard`): `POST preview` (multipart ≤5 MB), `POST commit`, `GET/POST mappings`, `DELETE mappings/:id`. `ImportBankService` flow: `decodeCsvBuffer` (UTF-8/Windows-1250 auto-detect, `iconv-lite`) → parser dispatch (mappingId → bankId → saved-fingerprint → auto-detect) → normalized rows → `pairFxRows` (same date, opposite sign, different currency) → `buildExternalRef` → dedup. Per-bank parsers `parsers/*.parser.ts` implement `BankParser { id, displayName, detect(), parse() }`; `universal.detect()` always returns `false`. Helpers in `utils/`: `polish-amount.ts`, `polish-date.ts`, `encoding.ts`, `header-fingerprint.ts`, `fx-pairing.ts`, `pdf-text.ts`. `merchants/merchants-pl.ts` ~40 brand→category hints (`MERCHANTS_PL` + `suggestCategoryFromMerchantPL`). **Merchant name normalization** (ABA-254): the same file also exports `MERCHANT_CANONICAL_PL` (brand UPPERCASE substring → canonical display name, e.g. `BIEDRONKA → Biedronka`) + `normalizeMerchantPL(name)` (longest-key-first `includes` match, idempotent, passthrough for unknown/undefined). Applied in `buildPreviewResponse` (visible in preview) and re-applied defensively at commit so `BIEDRONKA 1234 WARSZAWA` lands as one `Biedronka` seller. `NETTO` is deliberately NOT in the canonical map (collides with the PL accounting term "kwota netto") — it stays only in the category-hint map. **Dedup key**: `bank:<bankId>:<isoDate>:<signedAmountCents>:<sha256(normalize(desc)).slice(0,8)>`. **Two dedup layers** in `buildPreviewResponse`: (1) exact `externalRef` match (re-import of same file); (2) `flagContentDuplicates` — content match on `(date, signedAmountCents, currency)` against ALL account Expense/Income regardless of source, greedy 1-to-1 multiset, excludes FX. Matched rows → `alreadyImported: true`, auto-unchecked in preview. `Expense.date`/`Income.date` are `@db.Date` so date match is exact. **Commit dedup / crash fix (ABA-313)**: `commit()` calls `dropDuplicateRows(accountId, toImport)` **before** opening the `$transaction` — removes externalRefs that already exist in the DB (expense/income/currencyExchange) AND intra-batch repeats. This is REQUIRED, not just an optimization: Postgres aborts the WHOLE transaction on the first `@@unique([accountId, externalRef])` violation (subsequent statements fail with `25P02`), so the old catch-P2002-and-`continue` pattern crashed the entire import (`PrismaClientUnknownRequestError`). The per-row `catch` now rethrows ALL errors (a poisoned tx can't continue) → clean rollback + client retry. Covers the gaps `alreadyImported` misses (intra-file repeats; rows imported between preview and commit). **Saved mappings**: `csv_import_mappings` table (migration `20260523100000_add_csv_import_mappings`, `@@unique([accountId, headerFingerprint])`); commit `saveMapping` field persists column mapping for auto-application. **PDF statements**: `BankParser.format?: 'csv'|'pdf'`; orchestrator detects `%PDF` header (`isPdfBuffer`), extracts text via `pdf-parse` (`extractPdfText`), dispatches to PDF parsers — CSV header/mapping/fingerprint skipped. `erste.parser.ts` and `alior.parser.ts` are PDF parsers (line state machines over the bank's "Wyciąg" table). **Request-a-bank**: `POST /import/bank/request-bank` (multipart `file?` + `bankName`, `notes?`) → forwards to **ops Telegram chat** (`TELEGRAM_CHAT_ID`, never the user) via `TelegramService.sendMessage`/`sendDocument`. Mobile: Settings → "Import transactions" → `app/settings/import/` (`index.tsx` hub, `preview.tsx`, `mapper.tsx`, `request-bank.tsx`); shared `useImportStore`; `bankImport.*` i18n (8 locales); help section `27-bank-import`. **Visible banks**: Wise, mBank, PKO, Revolut, Erste (PDF), Alior (PDF), Other — ING/Millennium/Pekao are in the registry but hidden in `BANKS` (`app/settings/import/index.tsx`) until validated against real exports. mBank/PKO parsers were rewritten against real exports (mBank: metadata preamble before `#Data operacji…#Kwota`, amounts without ` PLN`; PKO: comma-delimited quoted, single signed `Kwota`, array-mode parse). **Revolut parser** (ABA-116): `revolut.parser.ts` — comma-delimited UTF-8, columns `Type,Product,Started Date,Completed Date,Description,Amount,Fee,Currency,State,Balance`. Detection: `Started Date` + `Completed Date` + `Balance` header combo. Only `State=COMPLETED` rows imported (DECLINED/REVERTED/PENDING skipped). Amount is signed per-row; Fee already folded in. Currency is per-row (supports multi-currency accounts). EXCHANGE rows paired by existing `pairFxRows()` utility.
- **Competitor app migration (ABA-401)**: importing Monefy / Wallet / Money Manager exports with the user's own categories. **Moved to the wiki** — `docs/wiki/features/competitor-app-migration.md`. Carried here: parser ids are permanent (`buildExternalRef` embeds them in the dedup key); do not reorder `PARSERS` around `pko`/`pekao` (the Pekao detector also matches a PKO export); and a parser the user PICKED skips `detect()`, so when it reads zero rows `parsePreview` must fall back to detection and AI mapping — a silent empty preview is what a real Money Manager export produced (ABA-581).
- **AI universal statement import (ABA-390)**: when no bank parser recognises an upload, an LLM infers the CSV/XLSX column MAPPING (the deterministic `UniversalParser` then does the parsing) or extracts PDF rows directly. **Moved to the wiki** — `docs/wiki/features/ai-statement-import.md`. Carried here: on the CSV path the model returns column NAMES only and any invented one rejects the whole response; `parser.id` stays `'universal'` there, because `buildExternalRef` embeds it in the dedup key and an AI-inferred import must dedup identically to a hand-mapped one; and the AI path never returns a 5xx — every failure degrades to the picker or the manual mapper.
- **Import-bank service split**: `import-bank.service.ts` had grown to 1,188 lines mixing request-a-bank forwarding, deterministic preview parsing, AI mapping/extraction, dedup, the transactional commit writer, and category resolution — larger than `ai-tools.service.ts`/`expenses.service.ts`/`sync.service.ts`, none of which stayed unsplit at that size. Split into: `ai-preview.service.ts` (`ImportBankAiPreviewService` — `tryAiMapping`, AI-import consent `resolveAiConsent`/`grantAiConsent`, daily inference quota, `isMappingCorrection` signature-bookkeeping compare, exports `parseInferenceQuotaEnv`), `ai-pdf.service.ts` (`ImportBankAiPdfService` — `tryAiExtraction`, Pro-gate + usage tracking + balance reconciliation), `import-bank-dedup.service.ts` (`ImportBankDedupService` — `buildPreviewResponse`, `flagContentDuplicates`, `flagPossibleMerges`, `dropDuplicateRows`), and `import-bank-category.util.ts` (plain exported functions, no DI — `resolveCategoryId`/`preloadCategories`/`categoryCacheKey`, same P2002-avoidance pattern as `ExpensesService`/`BudgetsService`). `ImportBankService` is now the 408-line thin orchestrator for `requestBank`/`parsePreview`/`parsePdfPreview`/`commit`, injecting the three new services; `SignatureService` stays a direct dependency of both `ImportBankService` (commit-time bookkeeping) and `ImportBankAiPreviewService` (mapping lookup) — a shared leaf, same Nest-DI singleton in both. `ImportBankController`'s `POST /import/bank/ai-consent` now injects `ImportBankAiPreviewService` directly rather than routing through `ImportBankService` (mirrors ABA-368's "controller injects the new service directly" precedent) — no route/guard/DTO change. Mirrors the `expenses.service.ts` (ABA-368) and `chat.service.ts` (ABA-119) splits — a new import-bank concern should extend one of these focused services, not regrow `ImportBankService`.
- **Import batch history** (ABA-130): `modules/import-batches/` — `GET /import/batches` (last 20, each with `canRollback`: `status === 'committed'` and within 30-day window); `DELETE /import/batches/:id` rolls back (sets `isDeleted=true` + `externalRef=null` on linked rows so the file can be re-imported, marks batch `rolled_back`). Both wise + bank commit endpoints create an `ImportBatch` in-transaction and stamp records with `importBatchId`. Table `import_batches` (migration `20260525144711_add_import_batches`); nullable `import_batch_id` FK on `expenses`/`incomes`/`currency_exchanges`. Mobile: "Past imports" section in `app/settings/import/index.tsx`. `ImportBatchesModule` imported by both `ImportWiseModule` and `ImportBankModule`.
- **Backups** (ABA-163): `modules/backups/` — `POST /backups/export` builds a full account snapshot, serializes it **once**, and streams the JSON string straight to the response body via `@Res()` (no Nest re-serialization — returning `data: backup` previously tripled peak memory and OOM-crashed the API). Filename travels in the `X-Backup-Filename` header (+ `Content-Disposition`). The backup object carries `version`/`data` at the **top level** so restore validates directly (no outer envelope). **`Expense.receiptImage` (binary blob) is stripped from the export** (set to `undefined` right after the query) — a Bytes field serializes to a per-byte integer array (~4-6x size) and was the main cause of >50 MB backups; restore never reads it back. (Query-level Prisma `omit` types as `never` in this client setup, so a JS strip is used.) Mobile calls the endpoint **once** (`reports.api.ts` `downloadBackupData()` → `{ blob, fileName }`); `reportStore.exportBackup()` returns a `BackupExportResult` status so the UI only claims success when the file is actually saved/shared (Android SAF folder picker → shows path; else system share sheet). `POST /backups/restore` dedups by `clientId`/name and **never reuses the source row `id`s** (they are global PKs — reusing them threw `Unique constraint failed on (id)` when restoring into any account that still held those rows); created rows get fresh DB-generated ids and `expense`/`income.categoryId` is remapped through an old→new category-id map built during category restore. **Atomic**: the whole restore runs in one interactive `$transaction` (timeout 180s) — any per-row error throws `RestoreAbort` to roll everything back (no partial import), and the caller returns the collected `errors` with zero counts. `GET /backups/history`. 50 MB cap. See also API-memory note under Production.
- **Subscription manager** (ABA-208): `modules/user-subscriptions/` — CRUD tracker for users' recurring charges (Netflix, gym, SaaS). Table `user_subscriptions` (`id`, `accountId`, `name`, `amount`, `currencyCode`, `billingCycle` monthly/yearly/quarterly/weekly, `nextRenewalDate` @db.Date, `categoryId?`, `notes?`, `detectedFrom?`, `isActive`). 4 endpoints behind `JwtAuthGuard + AccountContextGuard`: `GET /user-subscriptions` (list, active first), `POST` / `PATCH :id` / `DELETE :id` (guarded by `ViewerBlockGuard`). Service computes `monthlyEquivalent` (yearly÷12, quarterly÷3, weekly×4.33) and `daysUntilRenewal` at query time — not stored. `SubscriptionRenewalCron` has two daily crons: (1) **09:00 UTC reminder** — sends `subscription_renewal` push to all account members 3 days before `nextRenewalDate` (respects `user.notifySubscriptionRenewals` pref, new column default `true`); (2) **08:00 UTC auto-charge (ABA-211)** — `handleDueRenewals` finds active subscriptions whose `nextRenewalDate <= today`, and per subscription **creates one `Expense`** (dated on the renewal date, `source:'manual'`, amount/currency/category/name from the subscription, attributed to the account **owner** via `accountMember role:'owner'`) and **advances `nextRenewalDate` by one billing cycle** (`addCycle`: weekly+7d / monthly+1mo / quarterly+3mo / yearly+1yr) — both in one `$transaction` so a crash can't double-charge. At most one charge per subscription per run (a long-overdue sub catches up one period per day, no burst). Sends a `subscription_renewal` "charged" push after. Fat Finder shows a "Track this" button on `type:'subscription'` findings, pre-filling the `new.tsx` form via router params. Mobile: `app/subscriptions/index.tsx` (list + monthly-total banner + FAB), `app/subscriptions/new.tsx` (modal), `app/subscriptions/[id].tsx` (edit/toggle-active/delete). Store: `userSubscriptionStore.ts`. API client: `userSubscriptions.api.ts`. Migration: `20260606000000_add_user_subscriptions`. i18n: `subscriptionManager.*` keys (9 locales). Notification preference `subscriptionRenewals` added to `GET/PATCH /users/me/notification-preferences`. **Entry points (ABA-209)**: ABA-208 left `app/subscriptions/index.tsx` route-registered in `_layout.tsx` but with **no navigable entry** (only Fat Finder linked to `/subscriptions/new`). Now reachable via a Settings-hub row (`settings/index.tsx` → `/subscriptions`, `subscriptionManager.title` + `settingsSubtitle`) and a configurable `subscriptions` home quick action (added to `QUICK_ACTION_KEYS`, icon `repeat-outline`, default on). **Renewal calendar (ABA-259)**: `app/subscriptions/index.tsx` has a **List / Calendar toggle pill** below the monthly-total card. Calendar mode renders `SubscriptionCalendarView` (`src/components/subscriptions/`) — Mon-first 7-column day grid where each day with a renewal shows colored dots (active=primary, inactive=textDisabled) + first subscription name (9px); tapping a day expands an inline drill-down with name/amount/cycle/inactive badge. Month nav (← →) managed inline. Data from `useSubscriptionCalendar` hook (`src/hooks/`) — projects `nextRenewalDate` forward/back via `addCycle`/`subtractCycle` to find all renewal days for the displayed month; weekly subs may appear 4–5× per month. Inactive subs shown at `opacity 0.55`. `FlatList` replaced by `ScrollView`. 4 new i18n keys (`calendarView`, `listView`, `noRenewalsThisMonth`, `renewalsOnDay`) in all 9 locales. No new API endpoint.
- **AI module features**:
  - **Chat Q&A**: Natural language financial questions powered by GPT-4
  - **Natural Language Commands**: Execute actions via chat (create expenses/budgets, query data) using OpenAI function calling
  - **AI functions** (the list is the count — do not restate it as a numeral, it has drifted before): `create_expense`, `create_income`, `create_budget`, `create_category`, `get_expenses`, `get_budget_status`, `get_category_breakdown`, `record_debt_repayment`, `create_debt`, `get_debt_summary`, `update_goal_balance`, `check_affordability`, `add_to_shopping_list`, `remove_from_shopping_list`, `get_shopping_suggestions`, `get_inflation_shield`, `get_deposit_total`, `get_discount_total`
  - **Add / remove / query shopping list (ABA-348, extended ABA-360)**: `add_to_shopping_list({ items: string[] })` — the AI chat can put groceries on the user's shopping list. **Executes IMMEDIATELY** (no confirmation card, no read cache): `chat.service.ts` has a dedicated branch BEFORE the `isWriteAction` viewer/write check (item writes aren't `ViewerBlockGuard`-gated, so viewers may add too), routing to `handleShoppingListAdd` → `AiToolsService.executeAddToShoppingList` → `ShoppingListService.addItemsByName(accountId, userId, names)` (resolves the first non-archived list, else revives/creates `default-{accountId}`; one item per name with a fresh server clientId). Deterministic 9-lang confirmation via `PromptBuilder.getShoppingListAddText`; mobile renders a `ShoppingAddResult` `ActionResultCard` (`chat.actionAddShoppingList`). `AiModule` imports `ShoppingListModule`; `AiToolsService` injects `ShoppingListService`. The executor tolerates a lone `item` string as well as `items[]`. Targets the default/active list only (no add-to-named-list yet); the mobile store reflects the new item on its next pull. **`remove_from_shopping_list({ items: string[] })`** (ABA-360) is the mirror-image write — same immediate-execute treatment (not in `isWriteAction`, dedicated `chat.service.ts` branch before the confirmation check, `handleShoppingListRemove` → `AiToolsService.executeRemoveFromShoppingList` → new `ShoppingListService.removeItemsByName(accountId, names)`): case-insensitive match against `rawLabel` (fallback `canonicalName`) among **unchecked**, non-deleted items across ALL non-archived lists; first match per name is soft-deleted (`isDeleted` + `syncVersion` increment, same convention as the existing item-delete path); unmatched names come back in `notFoundLabels` (never an error — one bad name doesn't fail the rest). No "mark as bought" tool exists — removing via chat IS how "I already bought X, take it off" is handled; the one-tap checkbox on the shopping-list screen remains the only way to check an item off without removing it. Narrated via `PromptBuilder.getShoppingListRemoveText` (9 langs); mobile renders `ShoppingRemoveResult` (`chat.actionRemoveShoppingList` + `chat.actionShoppingNotFound`). **`get_shopping_suggestions()`** (ABA-360) is a **read** action — cached 10 min through the existing generic `executeWithCache`/`handleReadAction` path with NO dedicated `chat.service.ts` branch (same as `get_inflation_shield`/`check_affordability`); `AiToolsService.executeGetShoppingSuggestions` calls the existing `getRestockSuggestions`/`getDeals` in parallel, caps each at top 5. System prompt disambiguates it from `get_inflation_shield`: this tool is today's restock/deals, the shield is long-term price-rise forecasting for stocking up ahead. Mobile renders `ShoppingSuggestionsResult` (`chat.actionShoppingSuggestions` + `chat.shoppingSuggestionsRestock`/`chat.shoppingSuggestionsDeals` sub-sections). Deferred: quantity parsing, named-list (`listName`) targeting for any of the three tools.
  - **Spending questions — split-aware categories and line-item search (ABA-446, ABA-343)**: how `get_expenses`/`get_category_breakdown` answer "how much did I spend on X". **Moved to the wiki** — `docs/wiki/features/chat-spending-questions.md`. Carried here: `attributeToCategories` is the ONE place the split rule lives (splits decide; without splits the expense's own category takes the whole amount), and `categoryName` is deliberately NOT pushed into the SQL filter — a filtered row reports only its matching share; the semantic filter asks the model for integer line indices, never UUIDs, and on error degrades to substring-only, never "return everything".
  - **Deposit and discount totals (ABA-516, ABA-569, ABA-576)**: `get_deposit_total`, `get_discount_total` and the Analytics tab's tappable savings rows. **Moved to the wiki** — `docs/wiki/features/deposit-and-discount-totals.md`. Carried here: they read `Expense.depositAmount`/`discountAmount` **columns**, never a category; a new cached chat read tool must be added to `invalidateExpenseChatCache`, whose bust list is per-tool; and all copy says deposits **already paid**, never a refundable amount, in every language.
  - **Affordability Oracle (ABA-293)**: `check_affordability(amount, currencyCode?, description?)` — read action (no confirmation), deterministic `AffordabilityVerdict` from `SafeToSpendService`; verdict fields `affordable`/`reasonCode` forwarded verbatim to the LLM which narrates only (prompt rule mirrors the "report `overBy` verbatim" pattern); cached via `executeWithCache` (cache key includes `baseCurrency`); `reasonCode` values: `within_safe`, `within_available_tight`, `over_available`, `delays_goal`, `wait_until_income`.
  - **Debt commands**: `record_debt_repayment(debtId, amount, date?)` creates a linked repayment income/expense; `create_debt(contactName, amount, currencyCode, direction, dueDate?)` creates lent (Expense) or borrowed (Income) record with `isDebt=true`; `get_debt_summary()` returns active debts (read, cached). AI resolves contact names via `activeDebts` context (ids included) — ambiguous names trigger clarifying question.
  - **Goal command**: `update_goal_balance(goalId, newAmount)` calls `GoalPlannerService.updateGoal({ currentAmount }, { userId, note: 'AI update' })`. Resolves goal names via `savingsGoals` context (ids now included). Auto-completes goal if `currentAmount >= targetAmount`. Also inserts a `GoalContribution` row (see **Goal contribution log** below).
  - **Goal contribution log** (ABA-262): `goal_contributions` table (`migration 20260615100000_add_goal_contributions`). `GoalPlannerService.updateGoal()` accepts optional `contributionMeta: { userId, note? }` — when `newCurrentAmount > oldCurrentAmount`, delta amount is recorded in a `goalContribution.create` inside the same `$transaction`. `GET /ai/goals/:id/contributions` returns last 20 newest-first (no ViewerBlockGuard — read-only). Manual `PATCH /ai/goals/:id` passes `{ userId: req.user.id }` automatically. AI `update_goal_balance` passes `{ userId, note: 'AI update' }`. Mobile goal detail screen shows a "Contribution History" card below the progress bar; `goalStore.contributions` is a per-goalId in-memory map loaded via `loadContributions(id)`. No backfill for existing goals.
  - **Confirmation flow**: Write actions (`create_*`, `record_debt_repayment`, `update_goal_balance`) require user confirmation before execution; read actions (`get_*`) execute immediately (10-min cache)
  - **Language detection** (ABA-264): `PromptBuilder.detectUserLanguage(message, history, uiLanguage?)` picks the reply language in three tiers — (1) the language detected from the **current message's** script/unique chars, (2) the user's **app UI locale** (`user.language`, mapped via `localeToLanguageName`) for ambiguous/plain-ASCII text, (3) recent-assistant-message history (legacy fallback). `chat.service.ts` selects `user.language` and threads it through `buildSystemPrompt` as `uiLanguage`. `detectLanguage` decides French vs Spanish on **language-unique characters only** — the shared `é` (very common in FR: *dépense, café, été*) must NOT force Spanish (the original bug: FR UI → ES replies, then history locked it in). Supported: Russian, Ukrainian, Belarusian, German, Spanish, French, Polish, Dutch, English. Cyrillic/German/Polish (unique scripts) always override the UI locale.
  - **Currency mapping**: Supports currency symbol detection (₴→UAH, $→USD, €→EUR, zł→PLN, £→GBP, ₽→RUB)
  - **Currency labeling + display-currency conversion in answers** (ABA-263): chat answers are LLM-narrated free text, so currency labeling is enforced via prompt, not code. `prompt-builder.ts` carries a hard rule — every amount in tool results / context has its own `currencyCode`; the model must label each amount with that currency (ISO code or mapped symbol) and **never default to € for non-EUR amounts**. `chat.service.ts` selects `user.currencyCode` and passes it to `buildSystemPrompt` as `baseCurrency` (shown in the dynamic context) AND down to the read-action tools as the conversion target. **Conversion**: `AiToolsService` injects `ExchangeRateService` (from `CurrencyExchangeModule`; rates from open.er-api.com, `1 base = rates[X] X`, so `amount_in_base = amount / rates[from]`). all three read tools — `get_expenses`, `get_budget_status`, `get_category_breakdown` — convert every monetary field into the display currency and return them already in `baseCurrency` (rows/categories `currencyCode: base`, single base-currency total, plus `fxConverted/fxApproximate` flags); `getRatesSafe`/`convertAmount` helpers; **graceful fallback** to native amounts when the rate provider is unavailable or a currency has no rate. **`get_category_breakdown` computes from the raw expenses** (group + convert each), NOT `analyticsService.getSummary` — getSummary sums across currencies into one currency-blind number that can't be converted afterwards. The tool cache key (`buildToolCacheKey`) **includes `baseCurrency`** — it is per-user, so two members of one account with different display currencies must not share a cached (wrongly-converted) result. The read-action `tool` message tells the model: if `fxConverted`, present amounts as-is and add a short "approximate rate" note. (Not yet converted: the always-present UserContext summary numbers — `totalSpentThisMonth`/`topCategories` in the system prompt — are still native; the prompt tells the model to use tools for accurate amounts.) Mobile `ActionResultCard.tsx` `ExpensesResult` renders deterministically from the `get_expenses` tool output (`recentExpenses` + `totalsByCurrency`, per-currency totals with codes — now the display currency) — NOT `data.expenses`/`data.total` (those keys don't exist; the old card showed 0 items).
  - **Income voice & receipt capture** (ABA-191): `POST /ai/parse-income` — parallel to `POST /ai/parse-expense` but uses **income-type categories** for matching; guarded + tracked at 1.0. Mobile `app/income/voice.tsx` (record → Whisper → `parseIncome` → confirm form, `source:'voice'`) and `app/income/receipt.tsx` (camera/gallery/PDF → `scanReceipt` → confirm, `source:'ocr'`; total+date only, no line-items). Both accessible from home screen quick-action strip and via mic/camera icons in `income/new.tsx` footer. `Income.source` field (`IncomeSource = 'manual'|'voice'|'ocr'|'import'`) added to Prisma schema (migration `20260602000000_add_income_source`) and mobile SQLite (ALTER TABLE migration in `client.native.ts`).
  - **Endpoints** (all behind `JwtAuthGuard + AccountContextGuard` via the class-level decorator on `AiController`): `POST /ai/chat`, `POST /ai/chat/confirm`, `POST /ai/chat/reject`; `GET /ai/chat/conversations` (last 20, **account-scoped**: `accountId = X-Account-Id AND (isShared OR userId = me)`), `GET /ai/chat/conversations/:id/messages` (last 50, user+assistant roles only, same access predicate), `GET /ai/chat/conversations/:id/poll?since=<iso>` (newer messages + refreshes Redis presence), `PATCH /ai/chat/conversations/:id/shared` (owner-only sharing toggle). `confirm`/`reject` verify the conversation is in the caller's account AND that the caller initiated the `pending_action` (scoped by its `senderUserId`).
  - **Chat history**: `ChatConversation` (now `accountId` + `isShared`) + `ChatMessage` (now `senderUserId` + `mentionedUserIds`) stored server-side. Mobile loads history via `chatStore.loadConversations()` / `loadConversation(id)` with SQLite cache (`src/db/chatRepository.ts`, account-scoped local query). Chat tab shows a History bottom-sheet and a "New Conversation" button when browsing past conversations.
  - **Shared conversations**: per-conversation opt-in group chat for shared accounts. `ChatConversation.isShared` (default false; **any account member may share a conversation THEY created (ABA-334)** — `setConversationShared` checks `conversation.userId === caller` (NOT `accountRole`), and `chat()`'s `initialIsShared` is honored for any member; a member still cannot share someone else's conversation, even an account owner's) makes it visible to all members; private stays creator-only. A message that `@mentions` a member (mentions passed as `{userId}[]`, validated, self excluded) **silences the AI** and pushes a `chat_mention` notification (gated by `notifySharedActivity`) to each mentioned member **not currently present**; no mention → AI replies. Presence in Redis via `CacheService` key `chat:presence:{conversationId}:{userId}` (TTL 45s). AI history prefixes each member's message with sanitized `[Name]: `. Mobile polls `…/poll?since=` every 4s (only while a shared conversation is focused), reconciles optimistic id to server `userMessageId`, renders others' messages left-aligned with name label, `@`-mention suggestion bar, **creator-only** Shared/Private header toggle (mobile `chatStore` tracks `currentIsOwner`/`ownedConversationIds` from the API's per-conversation `isOwner`; `canToggleShared = hasOtherMembers && currentIsOwner`). **Sender-side dup fix (ABA-334)**: while `api.chat()` is in flight the 4s poll could return the just-sent message's server copy while the local optimistic copy still had its `tempId` (id mismatch → not deduped → appended twice, sender-only); the poll now dedupes against state read **after** its network await, and the `tempId→serverId` reconciliation drops duplicate ids. **Read-action userId (ABA-334)**: `handleReadAction` now threads the real `userId` into `executeWithCache` (was a hard-coded `''`), so read tools (e.g. `check_affordability`) execute with the actual user; the cache key (`accountId+args+baseCurrency`) is unchanged. **Deep-link**: tapping a `chat_mention` push → `handleNotificationResponse` switches `accountId` → `router.push('/(tabs)/chat', { conversationId })`; chat screen reads the param and `loadConversation` (falls back to `loadConversations()` when uncached). **Cold-start gate (ABA-264)**: `_layout.tsx` must NOT act on `getLastNotificationResponseAsync()` immediately — navigating while `RootNavigator` still returns `null` (init not done, `<Stack>` unmounted) wedges expo-router on a black screen, and `switchAccount` no-ops against the empty accounts list. The cold-start response is stored in state and flushed only once `!isInitializing && isAuthenticated && fontsLoaded` (same gate as the `Linking` deep-link handler). Keep the two deep-link paths gated symmetrically.
  - **AI service split** (ABA-119): `chat.service.ts` was split into a lean orchestrator with three focused helpers alongside it (it has since grown back to roughly twice the size it was split down to — audited 2026-09-22): `user-context-builder.service.ts` (builds `UserContext` from Prisma/domain services), `ai-tools.service.ts` (the OpenAI function schemas + `executeAction` dispatcher + cache wrapper — the list above is the count, do not restate it as a numeral here; this said 11 while the real number reached 18), `prompt-builder.service.ts` (system prompt construction, language detection, action i18n). All three are registered in `AiModule` as providers but not exported.
  - **UserContext caching** (ABA-151): `UserContextBuilder.build()` caches the result in Redis under key `uc:{accountId}` (TTL 60 s). `ExpensesService.invalidateChatCache()` and `IncomesService` mutation methods (`create`/`update`/`remove`) call `CacheService.del('uc:{accountId}')` after writing so the cache is refreshed promptly. The exported `ucKey(accountId)` helper in `user-context-builder.service.ts` is the canonical key formula. **Budget summary (ABA-344)**: `context.monthlyBudget` sums **ALL** active monthly budgets (a budget's `amount` is its total regardless of category allocations) — the old `categoryAllocations.length === 0` filter made the prompt show "Monthly budget: Not set" for users whose only budget is category-allocated, and the model then wrongly answered "you have no budget" without calling `get_budget_status`. The prompt line is annotated as a rough monthly-only total and instructs the model to ALWAYS call `get_budget_status` for budget questions.

- **Shared bot i18n (ABA-462)**: `common/bot-i18n/shared-messages.ts` holds the ~30-key, 9-language dictionary shared by all three bots (error messages, confirmations, menu labels, category-flow copy) plus a `createBotT(messages, { markup, defaultParams })` factory and the shared `buildCategorySplitLine`/`CategorySplitLineItem` — replaced three independently-copy-pasted `messages`/`t()` implementations (tech-debt `bot-i18n-triplication`, closed). Canonical markup is HTML (`<b>`, `<code>`, matching Telegram's own `parse_mode: HTML`); `createBotT(..., { markup: 'markdown' })` regex-converts those tags to WhatsApp/Slack's `*bold*`/`` `code` `` syntax at read time. `defaultParams` (e.g. `{ platform: 'Telegram' }`) is merged under any caller-supplied params, letting a "same template, only the brand word differs" message (`notLinked`) live once with a `{{platform}}` placeholder — no call-site changes needed. Each bot's own `modules/{telegram,whatsapp,slack}/helpers/i18n.ts` is now `{ ...sharedMessages, ...platformMessages }` → `createBotT(...)`, holding only genuinely platform-specific copy: `linkFirst`/`welcomeNew`/`unlinkSuccess`/`helpText` (all three — telegram uses `/slash` commands + HTML-entity escaping, whatsapp/slack don't), `welcomeBack`/`linkSuccess` (shared between whatsapp/slack only; telegram overrides both since it says "/help" not "help"), and Slack's own `thinking` placeholder key. **A new bot-facing string shared by 2+ platforms belongs in `shared-messages.ts`, not copy-pasted into each `helpers/i18n.ts`.**
- **Bot receipt line-item editing (ABA-482)**: typed corrections to a scanned receipt from Telegram/WhatsApp/Slack before it is saved. **Moved to the wiki** — `docs/wiki/features/bot-receipt-editing.md`. Carried here: the item-edit and date-edit typed-input modes are mutually exclusive and confirm/cancel clears both, or the next chat message is swallowed by the correction parser; and the internal `proposed:` category sentinel must never leave `receipt-item-edit.ts` as an id (the ABA-451 failure mode).
- **Telegram bot**: `modules/telegram/` — Telegraf-based bot with 7 handlers: `ChatHandler` (AI chat with usage tracking, 1.0/msg), `VoiceHandler` (Whisper transcription + chat, 2.0), `PhotoHandler` (OCR receipt scan, 2.0), `CommandHandler` (/start, /link, /help, /usage, /account, /newchat, /unlink), `ExpenseHandler`, `IncomeHandler`, `CategoryHandler`. All system messages localized via `helpers/i18n.ts` (9 languages, resolved from `user.language`). AI usage tracked and limits enforced — users get localized warning when limit reached. **Pending mid-flow state is Redis-backed (`CacheService`), not a module-level Map (tech-debt `telegram-handlers-in-memory-state`, closed)**: `ChatHandler`'s AI-confirmation data (`telegram:pa:{shortId}`, TTL 1800s) and `PhotoHandler`'s pending-receipt data + date-edit cursor (`telegram:receipt:{receiptId}` TTL 1800s, `telegram:awaiting_date:{telegramUserId}` TTL 600s) mirror the WhatsApp bot's `wa:pa:*`/`wa:receipt:*`/`wa:awaiting_date:*` keys — an in-memory Map was silently wiped on every deploy restart, orphaning any in-flight confirmation or receipt scan. Redis TTL replaces the old manual size-capped eviction loop. Both handlers inject `CacheService` (`@Global()`, no module change needed). **Gotcha fixed during the port**: `handleDateInput` mutates the pending-receipt object's `date` field in place — with a real Map that mutation persisted for free via object reference; with Redis a fetched value is a fresh JSON-deserialized copy, so the handler now explicitly writes the mutated object back via `cache.set(...)` before continuing, or the edited date would silently revert to the original on the next read.

- **WhatsApp bot**: `modules/whatsapp/` — `@Global()` module on the Meta Business Cloud API. Parallel to Telegram, same features + shared services (`ChatService`, `WhisperService`, `OcrService`, `ExpensesService`, `IncomesService`, `CategoriesService`, `SubscriptionsService`). Differences: (a) **webhook-only** `POST /whatsapp/webhook`, excluded from `/api/v1` prefix in `main.ts`; (b) **HMAC-SHA256** verification via `verifySignature` over `req.rawBody`; (c) **Redis state** not in-memory — `wa:msg:{id}` (idempotency 24h), `wa:pa:{shortId}` (pending actions 1800s), `wa:receipt:{shortId}` + `wa:awaiting_date:{phone}`, `wa:cat:{shortId}`; (d) callback IDs use `--` separator (UUIDs contain single `-`); (e) interactive via `WhatsAppClientService.sendButtons` (max 3×20char) / `sendList` (max 10 rows); (f) WA markdown (`*bold*`, `_italic_`) via `markdownToWhatsApp`. Linking via 6-hex code: mobile QR + `wa.me/{phone}?text=link%20{code}` in the **Chat bots** screen `app/settings/bots.tsx`; `CommandHandler.handleLink` is the only command from unlinked numbers. Endpoints on `UsersController` (`POST/GET/DELETE /users/me/whatsapp-link[-code]`). `helpers/i18n.ts` ports telegram's (33 keys × 9 languages).

- **Slack bot**: `modules/slack/` — Slack DM bot with full feature parity (AI chat, voice via Whisper, photo receipts via OCR, `expense`/`income`/`category` commands, confirm/reject via Block Kit buttons). **Webhook-only**: `POST /slack/events` (Events API, JSON) and `POST /slack/interactivity` (Block Kit button taps, `application/x-www-form-urlencoded` `payload` field) — both excluded from the `/api/v1` prefix in `main.ts`. `main.ts` also registers `express.urlencoded({ verify })` alongside the existing JSON body parser so that `rawBody` is captured on the interactivity endpoint for HMAC verification. **Security**: `v0=` HMAC-SHA256 over `v0:{timestamp}:{rawBody}` keyed by `SLACK_SIGNING_SECRET`, with a 5-minute replay window (`helpers/verify-signature.ts`). File downloads for voice/photo are fetched from `files.slack.com` over https only, with redirects refused (SSRF hardening). **Inbound message guard (ABA-256)**: `SlackBotService.handleEvent` drops bot/self messages and edit/delete/system subtypes, **but must let `subtype === 'file_share'` through** — Slack tags every uploaded/pasted file (image/voice/PDF) message with that subtype, and dropping it (the old `if (event.subtype) return;`) silently broke ALL receipt-photo / voice / PDF handling. Pattern: `if (event.bot_id) return; if (event.subtype && event.subtype !== 'file_share') return;`. Files are then dispatched from `dispatchMessage` via `event.files[0]` (audio→voice, image→`photoHandler.handleImage`, pdf→`handleDocument`). Regression test in `slack-bot.service.spec.ts`. **Redis state**: `slack:msg:{event_id}` (idempotency 24h; guards against Slack retries via `X-Slack-Retry-Num`), `slack:pa:{shortId}` (pending actions 1800s), `slack:receipt:{shortId}`, `slack:awaiting_date:{slackUserId}`, `slack:cat:{shortId}`. **Callback IDs** use a single `:` separator (`ca:`, `ra:`, `account:`, `cat_e:`, `cat_i:`, `cat_d:`, `receipt_*:`), parsed on the first `:`. **Outbound** via `@slack/web-api` `WebClient` (`chat.postMessage` with text + Block Kit buttons; `auth.test` on startup for the bot user id loop-guard). Button pickers replace `sendList` — account lists capped at 23 items, category lists at 20, with overflow text. **Linking**: 6-char hex code via `link <code>` DM (no OAuth). New tables `SlackLink` + `SlackLinkCode` (migration `20260604103259_add_slack_links`). Link endpoints on `UsersController` (`POST/GET/DELETE /users/me/slack-link[-code]`). Bot-created expenses use `source: 'slack'`. **Thinking indicator** (ABA-196): slow paths (AI chat, voice, photo OCR) immediately post a `💭` placeholder (`SlackClientService.postPlaceholder` → `ts`) and replace it in place with the final answer via `chat.update` (`updateText` passes NO `blocks` so the body renders — `blocks: []` would blank the body; `updateButtons` rebuilds the Block Kit via shared `buildButtonBlocks`; `replyText`/`replyButtons` update when a `ts` exists else send fresh). `ChatHandler.handleText(text, userState, placeholderTs?)` reuses a passed `ts`; voice turns its placeholder into the permanent `🎤 "transcript"` echo, then the chat reply gets its own placeholder. `thinking` i18n key in all 9 languages. **Mobile**: "Slack" section added to `app/settings/bots.tsx` (link code + status + unlink), `slackBot.*` i18n keys in all 9 locales, `api.generateSlackLinkCode/getSlackLinkStatus/unlinkSlack`. **Env**: `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_APP_ID`, `SLACK_BOT_USER` (bot is a no-op when unset). Prod webhook URL: `https://api.ai-budget.pl/slack/events`. **Multi-workspace OAuth** (ABA-200): `GET /slack/install` (issues single-use CSRF state in Redis `slack:oauth_state:{state}` TTL 600s → 302 to Slack `oauth/v2/authorize`) and `GET /slack/oauth/callback` (validates+consumes state → `oauth.v2.access` code exchange → upsert `SlackInstallation` with bot token **AES-256-GCM encrypted at rest** keyed by `SLACK_TOKEN_ENC_KEY`) — both excluded from `/api/v1` in `main.ts`; implemented in `SlackOAuthController` + `SlackOAuthService` + `SlackInstallationService`; helpers `token-crypto.ts` (AES-256-GCM) and `oauth-pages.ts` (HTML success page, team name HTML-escaped). `SlackClientService` resolves the bot token **per `teamId`** (looks up `SlackInstallation` first, falls back to `SLACK_BOT_TOKEN` env for the original workspace), caches `WebClient` per token; every outbound method takes a leading `teamId` parameter. New table `SlackInstallation` (migration `20260604230924_add_slack_installations`). New env vars: `SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`, `SLACK_OAUTH_REDIRECT_URL`, `SLACK_TOKEN_ENC_KEY`. Mobile: "Add to Slack" button in `app/settings/bots.tsx` opens `<API_ORIGIN>/slack/install` in-browser; `slackBot.addToSlack`/`addToSlackHint` i18n keys (9 locales).

### Mobile (React Native/Expo)
- **Platforms**: iOS, Android and **Web** (Expo web via `react-native-web` + Metro). The web build is a real product at `app.ai-budget.pl`, shipped on every push to `development`. **Moved to the wiki** — `docs/wiki/features/web-build-and-hosting.md` (platform splits, the two nginx containers, deploy traps), `docs/wiki/features/marketing-site.md` (the three generators and their regeneration commands), `docs/wiki/features/acquisition-tracking.md` (link tagging, first-touch capture, GA4), `docs/wiki/features/directory-badges.md`. Carried here: a platform split is a `.web.ts(x)` sibling, never a route-level file; SQLite is a no-op mock on web, so anything gating on a local-row count reads as an empty account there; and regenerating the marketing site needs `LANDING_BASE= ROBOTS="index,follow,max-image-preview:large"`, since the env-less default emits a `noindex` preview over production.
- **Desktop web layout** (ABA-289, refined ABA-290): on web at `width >= 1024px` the SPA gets a laptop layout — a **full-width top bar** over everything + a left sidebar nav + the content filling the area right of the sidebar — instead of a stretched full-width mobile screen. Native and narrow web (`<1024`) are **unchanged**. Single source of truth `src/components/webLayout.constants.ts` (`DESKTOP_MIN_WIDTH=1024`, `CONTENT_MAX_WIDTH=1080` (charts cap only — see below), `SIDEBAR_WIDTH=240`, `TOP_BAR_HEIGHT=56`, `COLUMN_HORIZONTAL_PADDING=16`; pure `isDesktopWeb(width)` + `useIsDesktopWeb()` — gate is `Platform.OS === 'web' && width >= 1024`, so native never qualifies). `WebShell` (`src/components/WebShell.tsx`) wraps the root `<Stack>` in `app/_layout.tsx` (single insertion; overlays `UpdatePrompt`/`UpgradeGate`/`StatusBar` stay outside it) — off-desktop it returns `<>{children}</>` (the byte-for-byte mobile guarantee); on desktop it renders a separate `DesktopShell` child. **Why the `DesktopShell` split**: it keeps the route/auth/theme hooks off the native render path AND stays Rules-of-Hooks-safe when a browser resizes across 1024 (moving those hooks after an early return in one component would crash on resize — hook count would change). `DesktopShell`: unauthenticated / `(auth)` routes → a centered 480px card, no chrome; otherwise → `WebTopBar` + a row of `WebSidebar` + a **full-width content area** (no centered column, so the screen's own scrollbar sits at the window's right edge — a "general" page scroll with no dead gutters). `WebTopBar` (`src/components/WebTopBar.tsx`) = brand + the five tab routes on the left (it renders `WebSidebar` itself, with `orientation="horizontal"`, so the nav and the sidebar's active-state matching are one component); on the right the account control, a divider, then the alerts bell and the settings gear as icon pills. **There is deliberately no section title** — ABA-499 dropped the `<Text>` that drew one and left the binding behind for weeks (a standing lint warning), and ABA-512 deleted the remains rather than restoring it: the horizontal nav already marks the active tab, `/settings` is named by the shell's own left pane, and a third statement of the section would undo ABA-507's tiering, which made the account control the one labelled element in this bar. The gear instead carries an **active wash** across the whole `/settings` area (`WebSidebar`'s own `rgba(255,255,255,0.18)` literal, matched deliberately so two active states 200px apart in one bar cannot diverge), on a **separate predicate** from the one that makes it no-op: it reads active anywhere under `/settings` but only no-ops at the root, because from a pane it is still the one navigation back to the overview and its sign-out button. `WebSidebar` (`src/components/WebSidebar.tsx`) = the 5 tab routes only (brand + alerts/settings live in the top bar), active-route via `usePathname()`. `(tabs)/_layout.tsx` on desktop web hides BOTH the bottom tab bar (`tabBarStyle: { display: 'none' }`) and the per-screen tab header (`headerShown: !isDesktopWeb`, since `WebTopBar` replaces it); native/narrow preserved. `(tabs)/index.tsx` on desktop hides the hero control row + divider (top bar carries them), neutralizes the quick-actions overlap margin, and **lays the home widgets out in two balanced columns** (`webTwoCol`/`webCol`; widgets collected into an array, split by index parity). Charts/measured components size via `useContentWidth()` (`src/hooks/useContentWidth.ts` — capped to `CONTENT_MAX_WIDTH` on desktop web, raw `window.width` otherwise so mobile is unchanged): `WalletMonthlyChart`, `InteractiveLineChart` (former module-scope `Dimensions.get()` moved into the component body), `InteractiveBarChart`, `InsightCarousel`. Analytics tab stays single-column (only the home dashboard is 2-column). Gate + width logic unit-tested.
- **Desktop transactions screen (ABA-499)**: the transactions list rebuilt for ≥1024px — the first screen in the desktop design language. **Moved to the wiki** — `docs/wiki/features/desktop-transactions-screen.md`. Carried here: the platform split is **component-level, never route-level** (one thin route, a `.web.tsx` component that decides on width, and the phone's JSX in exactly one definition) — every platform split in this repo works this way; and a desktop dialog HOSTS the existing screen component through a ref handle, never a second edit implementation.
- **Desktop transactions screen dialog split (ABA-538)**: `ExpensesDesktop.tsx` had grown to 921 lines (tech-debt `expenses-desktop-screen-god-file`, closed) — the row context menu (`RowContextMenu.tsx`) and the collapsed facet-rail dropdown (`FacetRailTrigger`/`FacetRail`) were already their own files, but the four overlay dialogs the screen can have open — `ExpenseDialog` (view/edit a row), `CreateDialog` (the "+ Add expense/income" dialog), and the bulk category/tag picker `Modal`s (previously two ~85-line inline blocks) — were still dispatched inline. Extracted into `ExpensesDesktopDialogs.tsx`, taking the already-resolved dialog state (`selectedRow`, `createKind`, `showBulkCategoryPicker`, `showBulkTagPicker` + their close callbacks) as props; `ExpensesDesktop.tsx` keeps all the underlying STATE (it's inseparable from the screen's own row list/selection/menu wiring), the new component only resolves it into JSX. `ExpensesDesktop.tsx` is now 787 lines. Mirrors the `AnalyticsDesktop.tsx` split (ABA-501/tech-debt `analytics-desktop-screen-god-file`) — a new desktop-transactions overlay should extend `ExpensesDesktopDialogs.tsx`, not grow `ExpensesDesktop.tsx` inline again.
- **Desktop keyboard shortcuts (ABA-533)**: a fixed desktop-web-only set of bindings on the transactions screen plus a `?` cheat sheet. **Moved to the wiki** — `docs/wiki/features/desktop-keyboard-shortcuts.md`. Carried here: the most recent registration wins, but an editable-target block is a hard stop, never a fall-through to an older registration; do NOT register an `Esc` binding (RN's `Modal` already owns it); and the cheat sheet reads the live registry, never a hardcoded list.
- **Navigation**: Expo Router. Screens in `app/`, tabs in `app/(tabs)/` — home, expenses, budgets, analytics, chat. `(tabs)/_layout.tsx` fires `Haptics.selectionAsync()` on every `tabPress` via `screenListeners` (`expo-haptics`, web no-op).
- **Root layout decomposition** (ABA-354): `app/_layout.tsx`'s `RootNavigator()` was the busiest file in the mobile app by commit count — every new push type/deep-link/cross-cutting feature added another `useEffect` to one component body. Decomposed into 7 focused hooks in `src/hooks/` (mirrors the `useHomeScreenData.ts` "Home screen decomposition" extraction): `useAppBootstrap` (fonts + db/language/auth init + splash-hide), `useColdStartGate` (pure `computeColdStartGate({isInitializing,isAuthenticated,fontsLoaded})` predicate + memoized hook — the single source of truth for the "app fully ready" gate, replacing a hand-copied boolean expression across two effects), `useAuthenticatedBootstrap` (subscription load + delayed push-registration/language-sync), `useBankNotificationCapture` (Android bank-notification capture subscribe), `useNotificationDeepLink` (cold-start push-notification response capture/flush), `useTripInviteDeepLink` (trip-invite universal-link auto-accept), `useGenericDeepLink` (`budget://`/`https://` deep links, deliberately gated WITHOUT `fontsLoaded` — do not "fix" that). `RootNavigator()` is now a thin composition of these hook calls plus the unchanged `<Stack.Screen>` route tree. Both deep-link flush effects (`useNotificationDeepLink`, `useTripInviteDeepLink`) consume the SAME `useColdStartGate()` value — the "keep the two deep-link paths gated symmetrically" invariant is now structural, not manual. `src/hooks/__tests__/useColdStartGate.test.ts` covers the gate predicate (previously zero test coverage on this logic). Any new cross-cutting mobile concern (new push type, new deep link, new bootstrap step) should get its own hook here, not another `useEffect` inline in `_layout.tsx`.
- **Header actions must be `textInverse` (ABA-450)**: the root stack paints every header `theme.colors.primary` (`headerStyle` in `app/_layout.tsx`) with `headerTintColor: theme.colors.textInverse`, so a `headerRight` painted `theme.colors.primary` is orange-on-orange (**1.0:1**) and one painted `theme.colors.danger` is `#FF6B6B` on `#E37F2B` (**1.04:1**) — both invisible, not merely low-contrast. Nine such actions across eight screens shipped that way and were recoloured to `textInverse`; a destructive one (the shopping list's "Clear checked", which soft-deletes every ticked item with no confirmation and no undo) was among them. **Do not colour a header action with a semantic token** — red does not read as destructive when it cannot be seen; the confirmation dialog carries that meaning. Also: `headerTitleAlign: 'center'` is global, so a centred title is constrained by the wider of the two side elements — a long `headerRight` **label** silently truncates the title (`Lista zaku...`), which is why the shopping list's action moved into the screen body rather than being recoloured in place. Prefer a compact icon in `headerRight`; put a long labelled action in the screen.
- **Desktop dashboard (ABA-505, ABA-507, ABA-517)**: the home tab at ≥1024 — a focus column beside a ~300px rail, the web first-run state, and the quick links. **Moved to the wiki** — `docs/wiki/features/desktop-dashboard.md`, which also collects the chart and layout traps that cost real time (gifted-charts' `height` is not the drawn height; a `Pressable` scrim always emits a `tabIndex` and so becomes the focus trap's first target). Carried here: **never draw unloaded data as fact** — with budgets landed and expenses still in flight the health score reported “Great, 100” about an account it knew nothing about.
- **Settings desktop shell (ABA-508, ABA-510, ABA-511, ABA-512)**: on web at ≥1024 settings becomes a two-pane layout driven by `settingsRegistry.ts`. **Moved to the wiki** — `docs/wiki/features/settings-desktop-shell.md`. Carried here: nothing under `src/` may import from `app/` (`@/*` maps only to `./src/*`), and extracting a screen out of `app/` also removes a phantom expo-router route; and a bottom-anchored `Modal` must add `insets.bottom` or its last row lands under the system nav bar, untappable on a three-button device.
- **Desktop chat screen (ABA-513)**: a 280px conversation rail beside a 760px reading column; the only screen with no page scroll. **Moved to the wiki** — `docs/wiki/features/desktop-chat-screen.md`. Carried here: the screen-data hook is called in BOTH platform `ChatView` files and passed down as one prop, never inside the platform child — a resize across 1024 swaps the child, and a hook mounted there would re-fire its telemetry and reset a half-typed draft.
- **Chat conversation management (ABA-514)**: rename, delete, pin, and a two-segment sharing control. **Moved to the wiki** — `docs/wiki/features/chat-conversation-management.md`. Carried here: a rename must write `updatedAt` back explicitly, since Prisma's `@updatedAt` bumps only when the field is absent from `data`; and the existence-non-disclosure order (`findFirst({id, accountId})` → 404, THEN the ownership check → 403) must not be tidied into one query.
- **Web data loading (ABA-498, ABA-506, ABA-518, ABA-519, ABA-520, ABA-522)**: how the web build gets its data with no local database, and six defects from code that assumed one. **Moved to the wiki** — `docs/wiki/features/web-data-loading.md`. Carried here: **a failed load and a successful empty load must never leave the same state** — a null `currentAccountId` makes `AccountContextGuard` fall back to the user's default account, and `isInitialized: true` on an empty category list disables every retry in the app; module-scope caches (sync throttles, seeded-accounts sets) are cleared from inside the store's `reset()`, and every account-scoped store needs one; and an opaque `TypeError: Failed to fetch` on web is the proxy rate limit until proven otherwise — an nginx-generated error carries no CORS headers (`docs/ops/api-rate-limit.md`).
- **Dashboard readiness (ABA-521)**: the per-widget loading gate for the web dashboard. **Moved to the wiki** — `docs/wiki/features/desktop-dashboard.md`. Carried here: native short-circuits to fully-ready (SQLite holds the account; dashes there would hide correct figures), and `readiness` on `HomeWidgetContext` is optional with absent meaning ready, so the phone is unchanged by construction.
- **First-run onboarding**: a brand-new user is routed once to `app/get-started.tsx` instead of an empty dashboard. **Moved to the wiki** — `docs/wiki/features/first-run-onboarding.md`. Carried here: count transactions from **SQLite**, never from the Zustand stores — they fill only after the cold-start gate opens, so an established user's in-memory list is empty long enough to route them into onboarding on top of their own data; and do NOT reintroduce `usePathname()` in `RootNavigator`, which re-renders 95 `<Stack.Screen>` elements on every navigation in the app.
- **Invite nudge (ABA-489)**: the one place the app asks anyone to use the referral programme. The loop had working economics and, after ABA-486, a working link — but nothing ever invited anyone into it; a row in Settings is not an ask. Pure `shouldOfferInvite` (`src/features/referral/`, mirrors `shouldAskForReview`) requires **a settled participant**, `canEdit`, at most one offer per 60 days, and goes quiet permanently after **2 dismissals**; `invitePromptStore` (MMKV `invite-prompt`) holds `lastShownAt`+`dismissals` behind pure `resolve*` readers (corrupt timestamp → never-shown, never `NaN`), and `markAccepted` **resets** the dismissal count because someone who shared has refused nothing. **The moment is the whole design**: it fires on a bill split once a friend has actually paid the user back through a page we served — the product has demonstrably worked for both people and the friend already knows what it is; the home screen would reach more users and mean nothing. It is an **inline card, never a modal** (`InviteFriendsCard`), and deliberately on a *different path* from the two interrupting prompts — the store-rating sheet (ABA-492) fires on receipt-scan and Wrapped-share, and stacking a second ask on the same success is how both get dismissed. `ParticipantStatusList` gained an optional `footer` slot rendered inside its scroll so the screen owns what goes there and the component keeps no opinion about referrals. `markShown` fires when the card actually renders, so the interval starts from a real sighting; the effect also lazily loads the referral code, since sharing without it would send a message with an empty code. A second surface should be justified by this one's results, not added on the assumption that more asking is more inviting.
- **PWA / installable web app (ABA-488)**: `app.ai-budget.pl` had no manifest, no icons and no iOS install tags, so it could not be added to a home screen — which matters more here than for a typical SPA, because there is **no native iOS app** and "Add to Home Screen" is the only way an iPhone gets an app-shaped AI Budget Assistant today. `apps/mobile/public/` holds `manifest.webmanifest` (+ icons) and is copied verbatim into `dist/` by `expo export --platform web`. `background_color: #000000` matches `expo-splash-screen.backgroundColor` in app.json so the launch doesn't flash; `orientation` is deliberately **omitted** because `useOrientationLock` already locks portrait on phones and unlocks on tablets from JS, and a manifest value would fight it. Icons are regenerated from `assets/icon.png` by `apps/mobile/scripts/generate-web-icons.py` — three consumers, three shapes: `icon-192`/`icon-512` for the manifest, `icon-maskable-512` scaled into the inner **80% safe zone** (a maskable slot is cropped to the launcher's shape, so a full-bleed icon loses the wallet off the top), and `apple-touch-icon.png` flattened opaque because **iOS reads none of the manifest** and without that `<link>` an installed shortcut takes a screenshot of the page as its icon. **`app/+html.tsx` does NOT work in this app and must not be re-added**: with `web.output: "single"` (the setting the nginx SPA fallback assumes) Expo SDK 54 renders its own built-in shell and ignores the file — verified by building with `--clear` and diffing, not assumed; switching to `output: "static"` to honour it would change the rendering mode of the whole app. The tags are injected instead by `apps/mobile/scripts/inject-pwa-tags.js`, called from `scripts/build-web.sh` (which is exactly what `web-deploy.yml` runs, so the deployed bundle gets them). It is idempotent and **fails the build** when `</head>` is absent, and `build-web.sh` greps the result afterwards — a silent no-op would ship an app nobody can install with nothing downstream noticing (the assetlinks-guard reasoning). `apple-mobile-web-app-status-bar-style` is `default`, never `black-translucent`: translucent puts content under the status bar and this layout has none of the native app's safe-area handling. **No service worker** — iOS installs from the manifest alone, but Chrome's automatic install prompt still wants one, so on Android/desktop this is "Add to Home screen" rather than an install banner; a naive shell-caching worker would risk serving a stale JS bundle and the web app has no offline story anyway (no SQLite on web).
- **Referral link (ABA-486)**: the referral share message carries a real link and the code pre-fills from it. `shareCode` used to send a hard-coded English sentence with a bare six-character code and **no link**, while the localized `referral.shareText` key sat unused in all nine locales — so the friend had to find the app themselves and type a code into a field they had never seen. `src/features/referral/referralLink.ts` (pure, unit-tested) builds `https://app.ai-budget.pl/?ref=<code>&src=referral&loc=share` — the **web app**, because that is where a link can pre-fill anything, tagged so the channel lands in the ABA-436 acquisition columns instead of reading as direct (`src=referral` is a new source, never a reused landing `loc`, so it cannot split the landing funnel). **The code stays visible in the message text as well as inside the link** — a friend who installs from Play gets no query string on the other side (that needs the Play Install Referrer API), so the printed code is their only way to claim the bonus; the URL sits on its own line after a blank one because messengers linkify a URL that ends a line. The message renders through the existing `referral.shareText` key, so this needed **zero i18n changes**. Capture **requires `src=referral` alongside `ref` (ABA-494)** — `ref` is a common parameter name and PeerPush links to us as `?utm_source=peerpush&ref=peerpush`, where `peerpush` satisfies the code shape exactly, so without the gate every visitor from that directory arrived with `PEERPUSH` pre-filled and first-touch-wins then kept it over a real friend's later link; `buildReferralUrl` always emits `src=referral`, so the gate costs nothing. Capture is `parseReferralCode` (pure, `attribution.types.ts`) plus `captureReferralCode`/`getReferralCode` across the `attribution.{ts,web,native}` trio, called from `index.js` beside `captureAcquisition` for the same hard reason — the query string belongs to the FIRST load, before expo-router owns the URL; native is a no-op. It is stored under its own `referralCode` key and deliberately **not** folded into `Acquisition`: a referral code is an argument to registration, not an acquisition column, and mixing it in would put it on a path toward the `acquisition*` DB fields. Client-side validation (`[A-Za-z0-9]{4,12}`, uppercased) is looser than the server's own no-0/O/1/I/L alphabet on purpose — the server is the authority, and a stricter client regex would silently drop a code the server would have honoured. `register.tsx` seeds its state from `getReferralCode()`. Known gap: the landing/blog generators don't forward `?ref=` through their CTAs, so a referral routed via ai-budget.pl still loses the code.
- **Guest split page CTA (ABA-487)**: the receipt-split guest page is the only surface where a **non-user** opens our page unprompted and often pays through it, and its whole acquisition affordance was a 12px grey footer whose iOS half pointed at `https://apps.apple.com/app/id000000000` — a placeholder that has never resolved, handed to plausibly half of every bill's recipients. Now a CTA card: `poweredBy` as its heading, a primary button to the **web app** (`?src=split&loc=guest`), Google Play kept as a secondary link. `STORE_URL_IOS` and the `getIos` string are deleted outright from the type and all nine locale blocks — a dead link is worse than none — and a new `ctaButton` string names the action the reader just experienced ("Split your own bill") rather than a generic "try the app". **The CTA must never use `.btn-primary`**: that class is the marker `guest.controller.spec.ts` uses to prove the pay affordance disappears once a share is claimed, and a CTA borrowing it silently defeated 7 of those assertions (the suite caught it on the first run). It has its own `.btn-cta`, and a test pins the separation. The group-picker and confirm pages have no footer and are untouched.
- **Store-rating prompt (ABA-492)**: the app asks for a Play rating at a success moment. It had never asked at all — 13 reviews against a 10+ install bucket — and rating volume is what Play ranks on, so this is the one ASO lever that is pure client code. **Pure decision** `src/features/review/shouldAskForReview.ts` (mirrors `shouldShowFirstRun`/`computeColdStartGate`) with **two throttles, neither implying the other**: never twice on the same app version (stops a second ask inside one release, e.g. a user scanning ten receipts in an evening) and never inside `MIN_DAYS_BETWEEN_ASKS` (90 days, roughly Play's own quota window — asking sooner is silently dropped), plus a `MIN_TRANSACTIONS` (5) floor. The count comes from SQLite via `countTransactions`, **never the in-memory stores** — they fill only after the cold-start gate opens, so an established user reads as zero for a moment on every launch (the same trap `useFirstRunOnboarding` documents). `reviewPromptStore` (MMKV `id: 'review-prompt'`) persists only `lastAskedAt`/`lastAskedVersion` behind pure `resolve*` readers (`firstRunStore` convention); a corrupted timestamp resolves to `null`, not `NaN`, which would compare false against every operand and silently disable the interval throttle. **Marked BEFORE the request fires**, because `requestStoreReview` resolves identically whether Play showed the sheet or dropped it for quota — marking on success would re-fire on every later save and spend the budget repeatedly for no signal. `src/services/storeReview.ts` never throws and `console.warn`s (ABA-157) — a prompt not appearing is the expected outcome on plenty of devices; web returns early. `maybeAskForReview` is a plain module function, not a hook, since every call site is an Alert callback rather than a render (`hydrateTransactions` shape). **Two call sites, both delight moments**: a saved receipt scan — only the `Done` branch of the success alert, **never `Scan another`**, since interrupting a batch-scanning run is how a prompt earns a one-star answer — and a successful Wrapped share. No i18n keys and no user docs: the sheet is system UI. Requires a native rebuild to take effect, and Play never reports whether it was shown, so the only verification is review count moving after the next release.
- **“What's New” spotlight** (ABA-470): a one-time, dismissible nudge for already-shipped features. **Moved to the wiki** — `docs/wiki/features/whats-new-spotlight.md`. Carried here: an entry's `id` is permanent once shipped (the persisted pointer and the i18n keys both reference it), and its copy lives in the nine locale files — a missing key renders the key string to the user, because i18next falls back to the key, not to English.
- **MainActivity must call `super.onCreate(null)`** (ABA-188): `apps/mobile/android/app/src/main/java/com/budget/assistant/MainActivity.kt` MUST pass `null` (NOT `savedInstanceState`) to `super.onCreate`. `react-native-screens` cannot restore its fragments — when Android restores the Activity after the OS kills the backgrounded process (common on Samsung deep-sleep), passing `savedInstanceState` throws `IllegalStateException: Screen fragments should never be restored` in native `MainActivity.onCreate`, **before any JS runs**, so the JS `ErrorBoundary` never catches it → app crashes on launch ("crashing frequently"). RN/Expo own screen state on the JS side. Do NOT let an `expo prebuild` regress this back to `savedInstanceState`. (Note: the mobile app has **no crash reporting** — `@sentry/react-native` is not a real dep, `ErrorBoundary.componentDidCatch` only `console.error`s — so native crashes are only visible via Play Console Android vitals / `adb logcat`.)
- **Keyboard handling**: **pure-JS, no native module** (builds with the local Windows Android toolchain — `react-native-keyboard-controller`'s Fabric codegen blows past the Windows 260-char MAX_PATH limit; do NOT reintroduce it or any keyboard native module). `src/hooks/useKeyboardHeight.ts` tracks height via RN `Keyboard` events (works under New Arch + Android edge-to-edge, where `KeyboardAvoidingView`/`adjustResize` are unreliable). Two wrappers in `src/components/`: `KeyboardAwareScreen` (drop-in for a form's root `ScrollView`: Android pads content by keyboard height, iOS `automaticallyAdjustKeyboardInsets`) and `KeyboardAvoidingScreen` (drop-in for `KeyboardAvoidingView` — pads bottom to lift docked inputs/footers; accepts+ignores `behavior`). Form screens import `KeyboardAvoidingScreen as KeyboardAvoidingView`.
- **A bottom-anchored `Modal` must clear the system navigation bar (ABA-483)**: the app is edge-to-edge, so the nav bar overlays the modal window — a sheet with `justifyContent: 'flex-end'` and a FIXED `paddingBottom` puts its last row underneath it, and on a three-button-nav device (~48dp) the row is not merely clipped but **untappable**. Every such sheet must add the inset: `useSafeAreaInsets()` and `paddingBottom: <base> + insets.bottom` as an inline override on the sheet view, with the base removed from the StyleSheet so there is one source for it (the pattern `AddDebtSheet`/`TransactionActionSheet`/`FinancialMonthSheet` already used). This was copy-pasted into **8** sheets before anyone hit it — `WhatsNewSpotlight` (the report: its two action buttons sat under the nav bar), `SafeToSpendSheet` (the prototype the others were copied from), `ChatHistorySheet`, `ItemCategorySheet`, `GroupQrModal`, `CreateCategoryModal`, `GoalAddFundsModal`, `UpgradeGate`. Sheets that take a `bottomInset` **prop** from their parent screen (`AddItemModal`, `ListNameModal`, `ListSwitcherModal`, `MergeProductsModal`, `RenameProductModal`) are already correct — don't add a second inset there. Note this cannot be caught by a test: there is no `react-test-renderer`/`@testing-library/react-native` in this repo, so nothing renders a component in CI and the only verification is on a device.
- **Metro crawl scoping on Windows** (ABA-255): Watchman has no Windows builds, so Metro falls back to its slower JS Node crawler. `apps/mobile/metro.config.js` keeps `watchFolders = [workspaceRoot]` (needed for hoisted deps + `packages/shared-*`) but extends `config.resolver.blockList` to exclude paths the mobile bundle never imports — `apps/api`, `apps/admin` (each with its own `node_modules`) and the `node_modules/@budget/(api|mobile)` + `node_modules/admin` workspace back-symlinks. `blockList` IS the file-map `ignorePattern` and the Node crawler applies it **at directory level** (skips the whole subtree before recursing), so this is what keeps the cold-start crawl from walking the entire ~150k-file monorepo and hanging the dev bundler before `index.bundle` can build. The default Expo `blockList` is preserved (combined into one RegExp with the default's flags to avoid Metro's `combine()` "different flags" error); `packages/shared-*` and `react-native` are NOT excluded (the app imports them); patterns use `[\\/]` so they match on Windows AND Linux/CI (web export). Do NOT widen `watchFolders` back to bare `workspaceRoot` without this blockList, and do NOT exclude `shared-types`/`shared-utils`.
- **State**: Zustand stores in `src/stores/` (no count — this one has been stale at 24 and 29; the list is the count) — `authStore`, `accountStore`, `alertStore`, `expenseStore`, `incomeStore`, `budgetStore`, `categoryStore`, `tagStore`, `projectStore`, `walletStore`, `chatStore`, `insightsStore`, `exchangeRateStore`, `subscriptionStore`, `themeStore`, `widgetVisibilityStore`, `quickActionStore`, `debtStore`, `encryptionStore`, `gamificationStore`, `goalStore`, `investmentStore`, `referralStore`, `reportStore`, `scenarioStore`, `userSubscriptionStore`, `merchantSuggestionStore`, `merchantRulesStore`, `upgradeStore`, `alertDialogStore`, `importStore`, `orderedVisibilityStore` (the shared factory, see ABA-456 below), `receiptSplitStore`, `receiptSplitParticipantIndex`, `restoreCredentialStore`, `whatsNewStore`
- **walletStore split** (ABA-447): `walletStore.ts` had grown to 765 lines mixing wallet-balance CRUD, currency-exchange CRUD, account-transfer CRUD, and read-side aggregation (`loadWallet`/`computeWalletSummary`/`loadAccountSummaries`/`loadMonthlyHistory`) — the same shape of problem `docs/tech-debt/expense-store-god-class.md` (closed) already fixed for `expenseStore.ts`. Mirroring the **actual** `expenseStore.ts`→`expenseSync.ts` extraction (function modules sharing the store's `set`/`get`, not literal separate Zustand stores — that would force edits across the ~19 files importing `useWalletStore`), the CRUD bodies moved out into `walletBalanceActions.ts` (`setInitialBalanceAction`/`updateInitialBalanceAction`/`removeBalanceAction`), `currencyExchangeActions.ts` (`addExchangeAction`/`updateExchangeAction`/`deleteExchangeAction`), `accountTransferActions.ts` (`addTransferAction`/`updateTransferAction`/`deleteTransferAction`), and the server-pull/merge half of `loadWallet` into `walletSync.ts` (`syncWalletFromServer`). `walletStore.ts` is now a 283-line composition: same `WalletState` shape, same `useWalletStore` public API (zero consumer changes), each action a one-line delegate into its module, keeping only the cross-cutting aggregation logic inline. A new wallet-adjacent CRUD feature should extend one of the three action files, not regrow `walletStore.ts`.
- **authStore split** (ABA-467): `authStore.ts` had grown to 826 lines with one flat `AuthState` mixing session lifecycle (`initialize`/`login`/`register`/`googleLogin`/`biometricLogin`/`logout`), password-recovery/verification (`forgotPassword`/`resetPassword`/`verifyEmail`/`resendVerification`), and user-preference setters (`setCurrency`/`setPaymentInfo`/`setPaymentMethods`) — the same shape the `walletStore` split above fixed. `AuthState` (plus `AuthStoreSet`/`AuthStoreGet` zustand-typed aliases) moved to `authStore.types.ts` so the two new action files can import the type without a runtime circular dependency on `authStore.ts` itself. Session-lifecycle bodies (+ the module-scope `persistSession`/`applyRestoredSession` helpers and the `isLoggingOut` guard) moved to `authSessionActions.ts` (`initializeAction`/`loginAction`/`registerAction`/`googleLoginAction`/`biometricLoginAction`/`logoutAction`); recovery bodies moved to `authRecoveryActions.ts` (`forgotPasswordAction`/`resetPasswordAction`/`verifyEmailAction`/`resendVerificationAction`). `authStore.ts` is now a ~110-line composition: same `AuthState` shape (re-exported from `authStore.types.ts`), same `useAuthStore` public API (zero consumer changes), each action a one-line delegate; `updateUser`/`setCurrency`/`setPaymentInfo`/`setPaymentMethods`/`setTokens`/`clearError` stay inline since they already delegate to `utils/currency.ts`/`utils/paymentInfo.ts`. A new auth-adjacent feature (2FA, another OAuth provider) should extend `authSessionActions.ts` (or a new sibling action file), not regrow `authStore.ts`.
- **`quickActionStore`/`widgetVisibilityStore` share one factory** (ABA-456): both are an MMKV-backed Zustand store holding a `Record<K, boolean>` visibility map + a `K[]` order array, with `toggle`/`setVisible`/`reorder`/`resetOrder` actions — the two had drifted into near-duplicate hand-written implementations with one undocumented behavioral difference. `src/stores/orderedVisibilityStore.ts` exports `createOrderedVisibilityStore<K>(keys, options)`, the shared implementation; `quickActionStore.ts` and `widgetVisibilityStore.ts` are now thin call sites (same public exports, zero consumer changes — `QUICK_ACTION_KEYS`/`DEFAULT_VISIBILITY`/`useQuickActionStore`/`resolveVisibility`/`resolveOrder` and `WIDGET_KEYS`/`useWidgetVisibilityStore`). The one real difference — a missing-from-persisted-order key is **appended** (quickActionStore) vs **inserted at its intended position** so a new high-priority widget like `familyFeed` surfaces at the top for existing users (widgetVisibilityStore) — is now an explicit, documented `insertMissingByPosition` factory option (default `false`), not a silent divergence. A third reorderable-visibility settings surface should call the factory, not copy either store file.
- **Monetization / upgrade paywall** (ABA-265/266): the mobile `Paywall` (`src/components/Paywall.tsx`) is surfaced app-wide via `upgradeStore` (Zustand: `visible`/`feature`/`requiredTier` + `show()`/`hide()`) and a single `<UpgradeGate>` modal mounted once in `app/_layout.tsx` — no per-screen modal duplication. Any `403` with `code: 'TIER_REQUIRED'` / `requiredTier` (or the monthly AI-limit 403 in `chatStore.sendMessage`) calls `useUpgradeStore.getState().show()`. Gated surfaces: AI chat monthly limit, Spending Story, Fat Finder, Analytics AI-Insights (compact "Pro" lock via `AiInsightsSection` `proGated` prop), and the member-invite limit (`account/invite.tsx`). **API tier gating**: `SubscriptionTierGuard` + `@RequireTier('pro')` gate `GET /insights/ai-charts`, `POST /insights/story`, `POST /insights/fat-finder` (business passes via numeric tier ranking); the guard throws `{ message, requiredTier, currentTier, code: 'TIER_REQUIRED' }`. **Subscription row upsert race (ABA-314)**: `SubscriptionsService.getOrCreateSubscription(userId)` is the funnel for `getCurrent`/`getUsageStats`/all AI-limit checks; Prisma `upsert` is NOT atomic under concurrency, so a new user's parallel startup requests could both INSERT and one hit P2002 on the unique `userId` (crashed `GET /subscriptions/usage`). It now catches P2002 and re-fetches the row the concurrent request created — apply the same catch-P2002-then-findUnique pattern to any hot-path `upsert` on a unique key. Trial free AI limit is `TRIAL_REQUEST_LIMITS.free = 50`; `trial-reminder.cron` sends both a T-3 and a T-1 reminder (`trialReminderIn3*` in `notification-i18n`, 9 langs). Plan feature bullets render from **local** i18n `subscription.features.*` keys, NOT the English `getPlans()` `features[]` strings. New i18n keys `subscription.limitReachedBody` + `subscription.startTrial` in all 9 locales. Note: Story/Fat Finder/AI-Insights were previously ungated (free) — this changes positioning to Pro-only; keep `user_docs/12-subscription.md` in sync.
- **Base/display currency** (ABA-187): the app-wide base currency is `user.currencyCode`. Change it via `authStore.setCurrency(code)`, which wraps the pure `src/utils/currency.ts` `applyCurrencyChange(next, deps)` helper — optimistic `updateUser({ currencyCode })` first (this is what `exchangeRateStore`'s subscription on `user.currencyCode` watches → it reloads rates and recomputes all converted totals), then fire-and-forget `api.updateProfile({ currencyCode })` (`console.warn` on failure, offline-tolerant). Both the `AccountSwitcher` currency chips and `settings/profile.tsx` route through `setCurrency` — do NOT duplicate the optimistic/persist logic.
- **Theme customization** (ABA-372): light/dark stay the built-in bases; on top, a user-chosen **accent color** recolors only brand tokens. **Persisted per-user in the DB** (`User.themeMode String @default("system")` + `User.accentColor String?` — null = default `#E37F2B`; migration `20260721000000_add_user_theme_prefs`), synced via `PATCH /users/me` (server validates `themeMode ∈ {light,dark,system}` and `accentColor` = `#RRGGBB`|null; both returned by `getProfile` + all 5 `auth.service.ts` response `user` blocks); `AuthResponse.user` + `User` entity carry both. Mirrors the `currencyCode` pattern: `themeStore` setters (`setMode`/`setAccent`) write MMKV then call the pure `utils/theme.ts` `applyThemePatch` (optimistic `authStore.updateUser` + fire-and-forget `api.updateProfile`, `console.warn` on failure); `setCustomAccent` is MMKV-only (last custom color, not synced). **Source-of-truth precedence** in `ThemeContext`: `mode = user?.themeMode ?? localMode ?? 'system'` (falls through to the MMKV mirror so a pre-feature explicit choice survives upgrade); `accent = user ? (user.accentColor ?? null) : (localAccent ?? null)` (user is sole source when logged in — do NOT let accent fall through to local, it would defeat an explicit clear). **Colors**: pure `theme/deriveAccent.ts` (`hexToHsl`/`hslToHex`/`relativeLuminance`/`readableOn`/`deriveAccentColors`) maps one accent → 10 brand tokens (`primary`/`primaryDark`/`primaryLight`/`secondary`/`accent`/`textLink`/`tabBarActive`/`messageBubbleUser` + on-accent `textInverse`/`messageBubbleUserText` chosen by luminance); `primaryDark` darkens in light mode, lightens in dark. `ThemeContext` merges `{ ...base, ...deriveAccentColors(base, accent, isDark) }` so `getTabBarTheme`/`getStackHeaderTheme` inherit the accent free. **`textInverse` overload gotcha**: it's the on-accent fg on `primary` surfaces (correctly accent-derived) AND the fg on fixed `success`/`danger` buttons (must NOT follow the accent) — so `colors.ts` has an always-white `onSemantic` token and semantic-colored confirm buttons use it (`income/*`, `goals/*`) — a full audit confirmed those were the ONLY `textInverse`-on-semantic sites (every other solid `success`/`danger`/`warning` button already uses `textPrimary`/hardcoded `#FFFFFF`/`onSemantic`, or is a tint/legend/progress-fill with no `textInverse` fg). **Quick-action icons** (`QuickActionIcon`) default their symbol + rounded-frame color to `theme.colors.primary` (accent) so the home quick-action strip follows the accent too — the baked orange `ICON_COLOR` in the inlined SVG strings is a render-time placeholder, replaced with the resolved color; the green income/invoice variants still pass an explicit `color`. The `ColorPicker` live preview shows a sample button + tab dot + chat bubble (spec §4). **Legacy migration**: `seedLegacyThemeFromLocal()` (themeStore, one-time via MMKV `themeSeededV1` flag, called from `useAuthenticatedBootstrap`) pushes a pre-feature explicit MMKV mode to the server so biometric-login (which overwrites `user.themeMode` from `getProfile`) doesn't reset it. **UI**: `settings/appearance.tsx` Accent section — preset swatches (`theme/presetAccents.ts`: `DEFAULT_ACCENT` + `PRESET_ACCENTS`) + a Custom swatch opening the in-house `components/ColorPicker.tsx` (hue slider + S/L square via `PanResponder` + hex input + live preview; NO native color-picker dep — `expo-linear-gradient` only) in a bottom-sheet. Accent is a personal preference → NOT `canEdit`-gated (available to viewers, like currency). 7 `settings.accent*`/`settings.customColor`/`settings.pickColor`/`settings.applyColor`/`settings.resetAccent`/`settings.invalidColor` i18n keys × 9 locales. Tests: `deriveAccent` (hex↔HSL, luminance on-accent pick, light/dark direction), `applyThemePatch` (optimistic + gated persist), API `users.controller.spec` (hex/mode validation).
- **Local DB**: SQLite via Drizzle ORM. Schema in `src/db/schema/index.ts`. 18 repositories in `src/db/*Repository.ts` use raw `executeSql()` — `account`, `accountTransfer`, `budget`, `budgetCategory`, `category`, `chat`, `currencyExchange`, `encryption`, `expense`, `expenseItem`, `gamification`, `income`, `investment`, `project`, `split`, `syncMetadata`, `tag`, `wallet`
- **API client**: `src/services/api.ts` — barrel that re-exports a composed `api` singleton. Base infrastructure lives in `src/services/http-client.ts` (`HttpClient` class: `request()`, `getAuthToken()`, `refreshToken()`, `setAccountIdGetter()`, `setLogoutHandler()`). Domain methods are split across one file per domain: `auth.api.ts`, `users.api.ts`, `expenses.api.ts`, `incomes.api.ts`, `budgets.api.ts`, `categories.api.ts`, `analytics.api.ts`, `ai.api.ts`, `accounts.api.ts`, `wallet.api.ts`, `investments.api.ts`, `encryption.api.ts`, `reports.api.ts`, `subscriptions.api.ts`, `import-bank.api.ts`, `userSubscriptions.api.ts`, `alerts.api.ts`, `merchantRules.api.ts`, `purchase-requests.api.ts`, `receiptSplit.api.ts`, `shoppingLists.api.ts`, `trip.api.ts`. All stores import `{ api }` from `api.ts` unchanged. Auto-injects `X-Account-Id` header, auto JWT refresh, 401 → logout.
- **Offline-first**: write to SQLite first, queue via `syncQueue`, push when online. **Moved to the wiki** — `docs/wiki/features/offline-first-sync.md` (the push convention, `SyncService`'s per-entity handlers, which entity types actually travel through the queue), `docs/wiki/features/client-id-resolution.md` (create idempotency, and the rule that a resolved server PK — never the raw route param — is what goes into a foreign key), and `docs/wiki/features/category-id-resolution.md` (the category-specific story). The two worth carrying here: a failed push while offline is `console.warn`, **never** `console.error` — it is expected, and RN's LogBox turns an error into a full-screen overlay users read as a crash; and a bug that reproduces ONLY on web is an id-resolution mismatch until proven otherwise, because web has no SQLite fallback to paper over a wrong-key server read.
- **Expense ↔ project association**: an expense's `projectId` is **not** an `expenses` column — it lives in the `project_expenses` join table (mirrored on server as `ProjectExpense`). The generic `updateExpense(id, updates)` store action canNOT manage it (the field isn't a column, and `undefined` is dropped from the JSON body so clearing is impossible). Use `expenseStore.setExpenseProject(expenseId, projectId | null)` instead — it updates in-memory state, the local SQLite join table (`addExpenseToProject`/`removeExpenseFromProject` in `projectRepository`), and the server via `api.updateExpense(id, { projectId })` (explicit `null` clears; `UpdateExpenseDto.projectId` accepts `string | null`, service soft-deletes the old row). Both `expense/new.tsx` (create) and `expense/[id].tsx` (edit) render the shared `ProjectPicker` component.
- **Expense merchant field** (ABA-140): `Expense.merchant?: string` is a plain free-text column (Prisma `merchant String?` + `@@index([accountId, merchant])`; mobile SQLite `merchant TEXT`). Auto-populated from receipt OCR (mobile `expense/receipt.tsx`, Telegram/WhatsApp `photo.handler.ts`) and bank/Wise import commit; manually editable via the shared `MerchantInput` component (free text + autocomplete suggestions from `expenseStore.getDistinctMerchants()`). Encrypted client-side **like `description`** — it's in `ENCRYPTION_FIELDS.expense.tier1`, so push paths (`addExpense`, `syncPendingExpenses`) run it through `maybeEncrypt` and the `loadExpenses` pull merge reads `decrypted.merchant`. Shown on the detail screen + as a secondary line on Expenses-tab rows. Filtering is **client-side only** (no `?merchant=` API param): `ExpenseFilters.merchants: string[]` (multi-select, default `[]`) — matches any of the selected merchants; the search box also matches merchant substring. The Expenses-tab category + merchant filters share **one filter row** (ABA-142): `[Category ▾] [Merchants N ▾]  ……  −total`. The merchant picker is **multi-select** (toggle stays open; "All merchants" clears; "Done" closes). The right-aligned total is `sumConverted(filteredItems, baseCurrency, rates)` from `src/utils/total.ts`. Income tab shows `[Category ▾] …… +total` (no merchant pill). Clearing on edit sends `''` (treated as absent everywhere). No backfill; income has no merchant field.
- **Date pickers on web — always go through `src/components/DatePicker.tsx` (ABA-381)**: `@react-native-community/datetimepicker` (8.4.4) ships **no web implementation** — the package has `datetimepicker.{android,ios,windows}.js` but no `.web.js`, so Metro resolves the fallback `src/datetimepicker.js`, whose whole body is `console.warn('DateTimePicker is not supported on: web'); return null`. Every date field in the web app was therefore **silently dead**: tapping the row flipped the `show…` flag and rendered nothing (reported as "дату не получается нажать"). Fix is a platform split following the repo's existing `.web.tsx` convention (`ExpenseMapView`, `ShareImageCard`): `DatePicker.tsx` wraps the native picker, `DatePicker.web.tsx` renders a real `<input type="date">` (raw DOM is fine in a `.web.tsx` — it is only bundled for web, under React DOM) that best-effort `showPicker()`s on mount so one tap opens the browser calendar, and treats **blur as dismissal** by calling `onChange(undefined)` — which the call sites' existing Android "close on change" branch already handles, so no extra prop. `onChange(date | undefined)` intentionally mirrors the underlying lib's contract so call sites keep owning their own close logic; `iosDisplay?: 'spinner' | 'inline'` exists solely to keep `goals/new`/`trip/new` iOS-identical. **All 8 pickers across 6 screens** now route through it (`expense/new` ×2, `income/new` ×2, `ExpenseDetailsCard`, `income/[id]`, `goals/new`, `trip/new`) and **no screen may import `@react-native-community/datetimepicker` directly again** — that import existing outside `DatePicker.tsx` is the bug. Several call sites closed on `Platform.OS === 'android'`, which left the web input stuck open; those are now `=== 'ios'` / `!== 'ios'`. Date↔`YYYY-MM-DD` conversion lives in pure, unit-tested `src/utils/dateInput.ts` (`toDateInputValue`/`fromDateInputValue`) and **must never go through `toISOString()`/`new Date(string)`**: those route via UTC and shift the calendar day for any non-zero offset (in UTC+2 a date picked at local midnight becomes the *previous* day). `fromDateInputValue` returns `null` for empty/partial/impossible input (e.g. `2026-02-31`, which JS would roll into March) so a half-typed value can't put an `Invalid Date` into a store. The same UTC bug was found and fixed in `goals/new.tsx`, which sent its `deadline` as `deadlineDate.toISOString().split('T')[0]`.
- **Transaction date is a create-form field (ABA-380)**: `expense/new.tsx` and `income/new.tsx` both hardcoded `date: new Date()` in their `addExpense`/`addIncome` payload with no date control anywhere on the form, so logging a transaction for a past day meant saving it today and then editing the date on the detail screen. Both now carry a `date` state defaulting to `new Date()` plus a `DateTimePicker` field (label `expenseNew.date`/`incomeNew.date`, placed after Description/Merchant, `display: spinner` on iOS mirroring every other picker in these files) — `@react-native-community/datetimepicker` was **already imported in both files** for the debt due-date picker, so no new dependency. Deliberately **not** clamped with `maximumDate`: `ExpenseDetailsCard`'s edit-mode picker accepts any date, and a create form that refuses what the edit form allows would be the inconsistency. The receipt/voice/bank-notification capture paths set their own dates and are untouched.
- **Report periods (ABA-409)**: the reports screen's range selection, generation and file export. **Moved to the wiki** — `docs/wiki/features/report-periods.md`. Carried here: report generation in all three formats is **free** — only scheduled e-mail delivery is paid; format every date boundary with `toDateInputValue`, never `toISOString()`, which shifts the calendar day on any non-zero UTC offset; and never call `expo-file-system` from a store — its `File`/`Paths` API has no web implementation and threw from three call sites, breaking backup creation as well as report export.
- **Expense currency is editable (ABA-379)**: `ExpenseDetailsCard`'s edit-mode amount row is `[amount input][currency chip]`, the chip expanding the same `SUPPORTED_CURRENCIES` list picker as `expense/new.tsx`; `editCurrencyCode` is reset by the existing `!isEditing` effect and sent as `currencyCode` in `triggerSave`'s `updateExpense()` call. Changing the currency **relabels** the expense — it never converts the amount (same rule the merge screen states as "the survivor keeps its own currency and amount"). Everything downstream already supported the field before this change and needed no edit: `updateExpenseInDb` writes `currency_code`, `UpdateExpenseDto.currencyCode` exists, `ExpensesService.update` persists it. `currencyCode` is **not** in `ENCRYPTION_FIELDS.expense` (either tier) so it stays plaintext — no re-encrypt on the E2EE push path; `expenseTotalsByCurrency`/`totalThisMonth` re-bucket for free via the `useExpenseStore.subscribe` on `expenses`; splits/items/trip-shares carry no currency of their own and inherit the relabel. **`app/income/[id].tsx` has the same chip (tech-debt `income-currency-not-editable`, closed)** — same `[amount input][currency chip]` row wired into `updateIncome()`'s `currencyCode` field, same relabel-not-convert rule; `incomeDetail.currency` i18n key added in all 9 locales.
- **Income detail screen decomposition** (ABA-496): `app/income/[id].tsx` was 634 lines with 11 `useState` hooks mixing read-only view and an inline edit form — the same shape `expense/[id].tsx` was already split out of via `ExpenseDetailsCard`. `app/income/components/IncomeDetailsCard.tsx` is now a `forwardRef` component mirroring `ExpenseDetailsCard`'s pattern: it owns the amount card + details card JSX, all 8 `edit*`/`show*Picker` fields, the `incomeTags` fetch, the debt-repayment banner, and the debt-info section, exposing `triggerSave()` via `useImperativeHandle` so the screen's Save button can invoke it through a ref. `income/[id].tsx` is now a 197-line thin coordinator (not-found guard, `isEditing` state, delete handler, action row). Pure structural refactor — no behavior/API/store change. Any new income-detail field or edit-form feature should extend `IncomeDetailsCard.tsx`, not grow `[id].tsx` inline again.
- **Web product telemetry (ABA-497)**: first-party product-usage analytics for the **web build only**. **Moved to the wiki** — `docs/wiki/features/web-telemetry.md`. Carried here: `telemetry.ts` (extensionless) is a real native **no-op**, NOT a stub re-exporting `.web` — do not add a `.native.ts`, which would make it dead code and pull `document`/`fetch` into the native bundle; the `screen` value is the route PATTERN off `navigationRef.getCurrentRoute()`, never a resolved path; and mobile's flow/status unions must stay equal to the server validator's lists, or a real event vanishes with nothing failing anywhere.
- **Bulk expense operations** (ABA-173): `PATCH /expenses/bulk` — accepts `{ ids: string[], categoryId?, tagIds?, isDeleted? }`, scoped to `accountId`, guarded by `ViewerBlockGuard`. Validates ownership (fetches only IDs matching `accountId + isDeleted:false`), runs a single `$transaction` (updateMany + per-expense `expenseTag.upsert` loop for tag append), then invalidates chat cache. `BulkUpdateExpensesDto` with 1–500 ID limit. Mobile: long-press any expense row opens the `TransactionActionSheet` (Edit / Duplicate / Delete + **Select multiple** for expenses, ABA-168 — long-press previously jumped straight to multi-select and hid Duplicate/Edit). **Select multiple** enters multi-select mode — checkboxes appear, header switches to count + Cancel + Select All, FAB hidden. Bottom action bar: Set Category (picker sheet), Add Tag (tag multi-picker sheet via `src/components/BulkTagPickerSheet.tsx` — append mode), Delete (confirmation alert). Multi-select state machine lives in `src/hooks/useExpenseMultiSelect.ts`. `expenseStore.bulkUpdateExpenses(ids, patch)` — optimistic in-memory update → SQLite persist (incl. tag links to local `expense_tags`, ABA-167) → fire-and-forget server call (failures log `console.warn` and stay `pending`). 13 new `expenses.bulk*` i18n keys in all 8 locales. **Two correctness fixes (ABA-166/167)**: (1) **route ordering** — `@Patch('bulk')` MUST be declared *before* `@Patch(':id')` in `expenses.controller.ts` (Express matches in declaration order; a `:id` route placed first captures `/expenses/bulk` as `id="bulk"` → 400 → bulk silently no-ops and rows reappear after the next sync pull); (2) **clientId resolution** — the mobile uses local clientIds as its row ids, so `bulkUpdate` resolves both expense ids and tag ids via `OR:[{id},{clientId}]` and writes the resolved server PKs to the junction — matching on `id` only silently no-ops every bulk op for synced (device-created) rows. Tests: `expenses.controller.spec.ts` (routing) + `expense-bulk.service.spec.ts` (clientId resolution) — logic lives in `ExpenseBulkService` (ABA-368, see **Expenses service split** below).
- **Move expense between accounts**: `POST /expenses/:id/move { targetAccountId }` (`ViewerBlockGuard` + `TripArchivedGuard` on the SOURCE via `AccountContextGuard`; `ExpenseCrossAccountService.moveToAccount(sourceAccountId, userId, id, dto)` validates the caller is a **non-viewer** member of the TARGET). Account-scoped associations do NOT travel across the boundary: the category is **remapped by (case-insensitive) name** into the target account (else cleared), while tags/project links/category splits are soft-deleted and trip expense shares deleted; amount/currency/description/merchant/notes/date/items/receipt stay. **E2EE expenses are rejected** (`BadRequestException`) — their `encryptedPayload` is sealed with the source account's key and can't decrypt under the target. On a `clientId` collision in the target (`@@unique([accountId, clientId])`) the moved row's `clientId` is reassigned to a fresh `randomUUID()` — **NOT** nulled: `Expense.clientId` is a required (non-nullable) column, so `clientId: null` throws `PrismaClientValidationError` (ABA-351; the mocked-Prisma unit test hid it). The moving device self-heals — the target-account pull soft-deletes the stale re-homed local row (old clientId no longer returned) and creates the new one. Invalidates chat cache for BOTH accounts + dismisses stale source anomaly alerts. **No migration** (reassigns the existing `accountId` column). Mobile: **online** action (server-authoritative — category remap + membership checks live server-side) via `expenseStore.moveExpense(id, targetAccountId)` — optimistic remove-from-list, `api.moveExpense`, then `moveExpenseAccountInDb` re-homes the local SQLite row (`account_id`→target, `category_id`→NULL, `sync_status:'synced'` — no push queued) so it reappears under the target account on next pull; the optimistic removal is rolled back if the server call fails. Expense-detail screen (`app/expense/[id].tsx`) shows a **swap-horizontal** action + a bottom-sheet account picker (targets = non-viewer accounts ≠ current); hidden when there are no eligible targets or for viewers. `MoveExpenseDto`/`MoveExpenseResponse` in `packages/shared-types/src/dto/expense.ts`. 5 `expenseDetail.move*` i18n keys × 9 locales. Tests: `expense-cross-account.service.spec.ts` (`moveToAccount` — remap/clear category, clientId clash, same-account/encrypted/viewer/non-member/not-found rejections; also covers `mergeExpenses`, see **Expenses service split** below).
- **Expenses service split** (ABA-368): `expenses.service.ts` had grown to 1,164 lines mixing CRUD, bulk ops, cross-account move, and merge. `ExpensesService` now only owns `create`/`update`/`remove`/`findOne`/`findAll`/items/receipt-image/splits and the create-time fire-and-forget hook chain (gamification, anomaly, family-feed, community-prices, shield tracking) — 832 lines. `bulkUpdate` moved to `ExpenseBulkService` (`expense-bulk.service.ts`); `mergeExpenses` + `moveToAccount` moved to `ExpenseCrossAccountService` (`expense-cross-account.service.ts`); `ExpensesController` injects both new services directly for the corresponding endpoints — no route/guard/DTO changes. Logic duplicated across the three classes was pulled into standalone utils: `invalidateExpenseChatCache` (`expense-cache.util.ts`) and `resolveExpenseCategoryId` (`expense-category-resolver.util.ts`); `ExpensesService`'s private `invalidateChatCache`/`resolveCategoryId` are now thin wrappers over these. Mirrors the "AI service split" (chat.service.ts) and `admin.service.ts` splits — do NOT let `ExpensesService` re-absorb bulk/cross-account logic; extend `ExpenseBulkService`/`ExpenseCrossAccountService` instead. Tests split 1:1 with the new services (`expenses.service.spec.ts`, `expense-bulk.service.spec.ts`, `expense-cross-account.service.spec.ts`). **Follow-up (regrowth fix)**: the file regrew to 1,056 lines as every new "what happens after create" feature kept adding another `@Optional()` sibling injection and inline fire-and-forget to `create()` (see `docs/tech-debt/expenses-service-regrowth-after-split.md`); that whole hook chain (plus `reconcileNotificationStub`) is now extracted into `ExpenseCreatedHooksService` (`expense-created-hooks.service.ts`, `onExpenseCreated(accountId, userId, expense, learnableItems)`), bringing `expenses.service.ts` to 952 lines — a new such feature should add a branch there, not a new constructor param on `ExpensesService`.
- **Tag clientId reconciliation** (ABA-167): tags created on a device got a server-generated `id` the client never learned (no mapping), so server-side tag links — single (`tags.service` `addToExpense/Income`) and bulk — matched nothing (`expense_tags` was empty across prod). Fix: `Tag.clientId String?` (migration `20260530000000_add_tag_client_id`, `@@unique([accountId, clientId])`); `tags.service.create` is **idempotent on `(accountId, name)`** and stores `clientId`; `findOne` + `add/removeTo{Expense,Income}` resolve the tag AND the expense/income by `OR:[{id},{clientId}]` and write the resolved server PKs to the junction. Mobile: `tagStore.createTag` sends `clientId = local id`; `syncFromServer` upserts under `clientId ?? id` and drops legacy `pending` duplicates by name; `(tabs)/expenses.tsx` calls `loadTags()` on mount (the bulk-tag picker was empty otherwise, ABA-167). **Tests + a second instance of the same mistake (ABA-419)**: `tags.service.spec.ts` covers the id-or-clientId resolution, the idempotent create, and the usage-count bookkeeping in all four link/unlink methods. Writing it caught `update()`/`remove()` making the identical error the ABA-167 fix exists to prevent — they called `findOne(accountId, id)` to resolve the tag but then used the raw (possibly-clientId) `id` param, not the resolved `existing.id`, in the Prisma `where: { id }` unique lookup; a mobile client editing/deleting a tag by its local clientId before its first sync round-trip would hit the wrong key. Fixed to use `existing.id` in both. **When touching any `resolve*Pk`/`findOne`-by-clientId helper anywhere in the codebase, always use the resolved server PK in the following write, never the raw incoming param** — the sibling tech-debt item `clientid-resolution-gap-investments-projects-debts-budgets` tracks the same bug class in other modules.
- **Merchant analytics** (ABA-174): `useAnalytics` returns `merchantSpending: MerchantSpending[]` — computed client-side from `filteredExpenses`, groups by `merchant` (null/empty excluded), sorts descending, top 8 entries + "Other" remainder; percentage relative to total merchant-tagged spending. Rendered by `MerchantBreakdown` component (`src/components/analytics/MerchantBreakdown.tsx`) — same `InteractiveDonutChart` + row-list pattern as `TagBreakdown`; shown in Analytics tab between CategoryBreakdown and TagBreakdown when `merchantSpending.length > 0`. i18n keys: `analytics.byMerchant`, `analytics.merchantOther` (all 8 locales).
- **Income category breakdown** (ABA-178): `useAnalytics` returns `incomeByCategory: IncomeCategorySpending[]` — computed client-side from `filteredIncomes` (same date/currency filter as `filteredExpenses`), groups by `categoryId`, sorts descending, uncategorized income labelled via `analytics.incomeCategoryOther`. Uses `INCOME_CATEGORY_COLORS` (green/teal palette, distinct from expense `CATEGORY_COLORS`). Rendered by `IncomeCategoryBreakdown` component (`src/components/analytics/IncomeCategoryBreakdown.tsx`) — same `InteractiveDonutChart` + row-list pattern, no vsAverage chip. Shown in Analytics tab between `AiInsightsSection` and `SpendingTrendChart` when `incomeByCategory.length > 0`. i18n keys: `analytics.byIncomeCategory`, `analytics.incomeCategoryOther` (all 8 locales).
- **Merchant management + capture reconciliation** (ABA-141): Settings → **Merchants** screen (`app/settings/merchants.tsx`, header registered in `app/_layout.tsx`) lists distinct merchants with counts (`expenseStore.getMerchantCounts()` → `getMerchantCounts` util) and supports **rename / merge / delete**: `expenseStore.renameMerchant(from, to | null)` (rename-to-existing merges; `null` clears) updates matching in-memory rows + one `bulkRenameMerchant` SQL `UPDATE` (account-scoped, marks rows `pending`) then fires `syncPendingExpenses()` (re-encrypts for E2EE). **Capture reconciliation**: receipt OCR (`expense/receipt.tsx`) and voice (`expense/voice.tsx`) pre-fill the merchant via `resolveExistingMerchant(input, getDistinctMerchants())` (exact case-insensitive/trim match snaps to the existing canonical value) and render the editable `MerchantInput` (suggest+confirm; no fuzzy auto-map). **Voice now stores its merchant in `merchant`, not `notes`** (legacy voice rows keep it in `notes`, not retro-migrated). All three reconciliation/management helpers live in `src/utils/merchant.ts`. **Multi-merge + grouping suggestions** (ABA-254): the Merchants screen has a **selection mode** (checkboxes + count header + bottom Merge bar) and a **merge modal** (editable canonical target + moved-expense count) backed by `expenseStore.mergeMerchants(sources, target)` → `bulkMergeMerchants(accountId, sources, target)` (one account-scoped SQL `UPDATE`, skips rows already named target, marks `pending`, re-encrypts on sync) — mirrors `renameMerchant`. Above the list, **suggest-only** banners from the pure heuristic `suggestMerchantGroups()` / `merchantFingerprint()` (first alphabetic token ≥4 chars that is NOT in `FINGERPRINT_STOPWORDS` — generic venue/category words like Restauracja/Apteka/Sklep, legal forms, PL cities — → brand key; groups variants sharing a fingerprint, ≥2 members; canonical = title-cased fingerprint) propose merges the user confirms (Merge / Dismiss). Visible banners are capped at the **top 3** by spend. **Dismissals persist across sessions** via `merchantSuggestionStore` (MMKV id `merchant-suggestions`, keyed by fingerprint; pure `resolveDismissed` helper unit-tested in `stores/__tests__/`). The heuristic **never auto-merges** — only the API dictionary normalizes automatically. All write affordances `canEdit`-gated; `merchants.*` i18n adds 11 keys (`select`/`selected`/`merge`/`mergeTitle`/`mergeInto`/`mergeCount`/`merged`/`selectToMerge`/`suggestionTitle`/`suggestionMerge`/`dismiss`) across 9 locales.
- **Local-first tab hydration**: list-bearing tabs (`(tabs)/index`, `expenses`, `analytics`) call `hydrateTransactions()` (in `src/stores/hydrateTransactions.ts`) from `useEffect([currentAccountId])`. It runs `loadExpenses` then `loadIncomes` **sequentially** (parallel contends on the single SQLite connection — reads went 65ms→65-602ms spiky) and exposes `useHydrationStore.isHydrating`. Its re-entry guard collapses the ~5 parallel call sites to one cycle. Pull-to-refresh and `settings/data.tsx` "Sync now" pass `{ force: true }`. `loadExpenses`/`loadIncomes` each also have a re-entry guard + 30s per-account skip window (`force` bypasses). Server-pull merge is 4 phases: A) dedup unique categories/projects, B) decrypt rows in parallel (`Promise.all`), C) build entities in JS, D) one `withTransaction` for ALL writes. The post-merge `loadCategories+loadProjects` cascade is **awaited**. `categoryStore.loadCategories` caches seeded accounts in a module-level `Set<accountId>` (first call seeds+patches ~60ms, later ~5ms re-read). Stores read SQLite first, set `isLoading: false` immediately, then server-pull — tab paints from cache instantly. Receipt-image fetch in `expense/[id]` deferred via `InteractionManager`. Empty + `isLoading` → spinner; empty + `!isLoading` → "Add your first…" empty state.
- **Viewer role — mobile UI**: `useAccountStore(s => s.canEdit())` returns `false` for `viewer` role. In all reference-data screens (categories, merchants, tags, projects) and any write-action UI: hide `+` / pencil / trash buttons by wrapping them in `{canEdit && ...}`; pass `onPress={canEdit ? handler : undefined}` and `activeOpacity={canEdit ? 0.7 : 1}` on touchable row content so the row gives no press feedback. API already blocks writes server-side — the mobile guard is UI-only.
- **Reference data screens** (ABA-158/159) — `settings/categories.tsx`, `settings/merchants.tsx`, `tags/manage.tsx`, `projects/index.tsx` all follow the same pattern: section header with title + optional `+` icon (inside ScrollView, same as categories), single `card` with rows and `divider` lines, row content is a `TouchableOpacity` (tap to edit → bottomsheet modal), trash icon on the right. `projects/[id].tsx` shows project expenses from `expenseStore` + edit/delete in Stack header. Modal style: backdrop + bottom-sheet with handle bar, Cancel + Save buttons. Merchants cannot be manually created (derived from expenses) so no `+` button.
- **Widget order** (ABA-189): `widgetVisibilityStore` exposes `order: WidgetKey[]` (MMKV key `widget-order`, defaults to `WIDGET_KEYS`), `reorder(newOrder)`, `resetOrder()`. Validation on load: unknown keys dropped; a widget **missing** from the persisted order (a newly-added `WidgetKey`) is inserted at its intended position, not appended to the end — see the shared-factory note (ABA-456) above; `reorder()` itself still appends any missing key, since a live drag session can only omit a key the user just removed. `settings/widgets.tsx` shows all 12 widgets in stored order with drag handles (PanResponder + `LayoutAnimation`); `scrollEnabled` disabled during drag; hidden widgets are grayed-out (opacity 0.45) but still reorderable; "Reset to default order" button at bottom. Home screen `(tabs)/index.tsx` renders widgets via `widgetOrder.map(key => renderHomeWidget(key, ctx))` (see **Home screen decomposition** below) — do NOT revert to sequential if-blocks. Investment portfolio card is above the ordered widget list and is not part of the order system. `'safeToSpend'` is a `WidgetKey` (ABA-293) — `SafeToSpendWidget` shows today's safe-to-spend number; tap opens breakdown bottom-sheet.
- **Configurable quick actions** (ABA-207): the home-screen quick-action strip is data-driven from `quickActionStore` (`src/stores/quickActionStore.ts`) — MMKV `id: 'quick-actions'`, same shape as `widgetVisibilityStore` (`visibility` + `order` + `setVisible`/`reorder`/`resetOrder`/`toggle`) but with **per-key default visibility** via `DEFAULT_VISIBILITY`: `voice_income` and `scan_invoice` default **off**, the other 7 (`add_expense`, `scan_receipt`, `voice_expense`, `exchange`, `converter`, `transfers`, `subscriptions`) on. The 9 `QUICK_ACTION_KEYS` define the default order. Visibility/order resolution lives in two pure exported helpers — `resolveVisibility(read)`, `resolveOrder(raw)`, re-exported from the shared `orderedVisibilityStore.ts` factory (ABA-456) — wired to the real MMKV getter in the store, so defaults are unit-tested without mocking MMKV (`__tests__/quickActionStore.test.ts`; the factory's own append/positional-insert modes are covered generically in `__tests__/orderedVisibilityStore.test.ts`). Per-key visibility is namespaced with a `vis:` prefix (`mmkv.set(\`vis:${key}\`, ...)`); order under `quick-action-order`. `HomeQuickActionStrip` renders `order.filter(k => visibility[k]).map(...)` — static route/label maps are module-scoped there; the icon renderer (`renderQuickActionIcon`) is keyed by action and closes over `theme`/`styles`. The strip wrapper AND the hero header's bottom padding (`heroHeaderNoStrip`) collapse when nothing is visible (`showQuickActions = canEdit && visibleQuickActions.length > 0`) so there's no empty bar; still `canEdit`-gated (viewer role unaffected). The drag-reorder + toggle UI is the shared generic `ReorderableToggleList` component (see below). **Shopping hub (ABA-332)**: the near-identical `shopping` + `purchase_request` cart actions were merged into one `shopping_hub` key (default visible) that, instead of navigating, opens a bottom-sheet menu with two rows — "Shopping list" (`/shopping-list`) and "Purchase request" (`/purchase-requests`). `HomeQuickActionStrip` owns the `Modal` sheet; removing the two old keys is handled gracefully by `resolveOrder` (drops unknown, appends the new). New i18n `dashboard.shoppingListFull` (menu-row label) in all 9 locales.
- **ReorderableToggleList** (ABA-207): `src/components/ReorderableToggleList.tsx` — generic (`<K extends string>`) presentational drag-reorder + visibility-toggle list extracted from the former inline PanResponder logic in `settings/widgets.tsx`. Props: `keys`/`order`/`visibility`/`labels`/`onReorder`/`onToggle`/`onDraggingChange`. Owns the local-order drag state machine (PanResponder + Animated `translateY` + per-key responder cache); reports drag start/stop via `onDraggingChange`. **Invariant**: `order` must be a referentially-stable array (read straight from the Zustand store, never a fresh `.slice()`/literal each render) or the during-render sync loops. `settings/widgets.tsx` is now a **two-section** screen — "Quick actions" (quickActionStore) + "Widgets" (widgetVisibilityStore) — each a `ReorderableToggleList` with its own reset button; the ScrollView `scrollEnabled` is false while EITHER list drags. Section headers `settings.quickActionsTitle`/`settings.widgetsTitle`; quick-action row labels reuse `dashboard.*` keys.
- **Home screen decomposition** (ABA-304): `(tabs)/index.tsx` was a 1,145-line god file (most-churned file in the repo) mixing ~15 store subscriptions, the widget switch, the quick-action strip, the hero header, and the Safe-to-Spend sheet. Split into a thin 169-line composition root plus: `src/hooks/useHomeScreenData.ts` (all Zustand store subscriptions, derived totals, the account-change + focus-effect data loading, `onRefresh`); `src/components/home/HomeHeroHeader.tsx` (orange hero — account/currency pills, alerts bell, settings, safe-to-spend hero row); `src/components/home/HomeQuickActionStrip.tsx` (quick-action grid, route/label maps, icon renderer — moved out of the screen); `src/components/home/SafeToSpendSheet.tsx` (breakdown bottom sheet); `src/components/home/HomeWidgetSwitch.tsx` (`renderHomeWidget(key, ctx)` — the thin dispatcher over all 13 `WidgetKey` cases). `DashboardScreen` now just calls the hook and composes these five pieces + the existing `NetProfitWidget`/`NetCapitalWidget`/`CalendarWidget`/`FinancialHealthWidget`/`FamilyFeedWidget`/`FatFinderCard`/`GoalsCard`. **Widget switch regrowth fix**: `HomeWidgetSwitch.tsx` itself regrew to 704 lines as each `WidgetKey` case's card component was written inline in it; the six that had their own layout — `InvestmentCard`, `GamificationCard`, `MonthlyBudgetCard`, `IncomeExpensesCard`, `DebtsCard`, `WalletsSection` — now each live in their own file under `src/components/home/widgets/`, each owning its own `createStyles(theme)`; the shared prop type moved to `src/components/home/HomeWidgetContext.ts` (imported by every widget file plus the switch, which still re-exports it for `(tabs)/index.tsx`). `HomeWidgetSwitch.tsx` is back to being only the `renderHomeWidget` switch + the `InvestmentCard` re-export (`(tabs)/index.tsx` renders `InvestmentCard` directly, above the ordered widget list). Any new home widget should get its own file under `home/widgets/` and a case in `renderHomeWidget` — do NOT write a new card's JSX/styles inline in `HomeWidgetSwitch.tsx`, and do NOT add cases back into `(tabs)/index.tsx`. Same rule applies to `HomeQuickActionStrip.tsx` for quick actions.
- **Account detail screen decomposition** (ABA-414): `app/account/[id].tsx` was a 796-line screen mixing account info edit/delete, member role management, pending invitations, trip-lifecycle actions, and the financial-month anchor-day sheet. Split into a 430-line thin composition plus three components in `src/components/account/`: `FinancialMonthSheet.tsx` (the anchor-day `Modal` + `ANCHOR_DAYS`, mirrors `home/SafeToSpendSheet.tsx`'s bottom-sheet pattern), `MembersSection.tsx` (member list with role-change/remove + pending invitations + invite button), and `TripSection.tsx` — which exports **two** components, `TripActionsCard` (settle-up/payment-settings/trip-map nav rows) and `TripArchiveButton` (owner-only archive action with the unconfirmed-settle-up force-retry alert), kept in one module because they share the archive-trip lifecycle logic but render in non-adjacent JSX slots (`TripActionsCard` in its own section, `TripArchiveButton` inside the Danger Zone alongside delete/leave). `AccountDetailScreen` now only owns account info edit/delete/leave and passes handlers down as props — mirrors the `(tabs)/index.tsx` decomposition above. Any new trip-lifecycle or member-management feature should extend `TripSection.tsx`/`MembersSection.tsx`, not `[id].tsx` directly.
- **Debts screen decomposition** (ABA-424): `app/debts/index.tsx` was a 722-line screen mixing tab/filter state with five inline render functions (`renderSummaryCards`/`renderSegmentedTabs`/`renderFilterChips`/`renderDebtItem`/list header/empty state — all closures recreated every render) and a full-screen add-debt `Modal`. Split into a 168-line thin composition plus `src/features/debts/debtDisplay.ts` (pure `getStatusColor`/`getStatusBackgroundColor`/`formatDueDate`, theme passed as a parameter so they're unit-testable independent of the screen) and components in `src/components/debts/`: `DebtSummaryCards`, `DebtSegmentedTabs`, `DebtFilterChips`, `DebtListItem`, `AddDebtSheet` (mirrors `home/SafeToSpendSheet.tsx`'s bottom-sheet pattern), `DebtsEmptyState`, `DebtsBottomNav` (the screen's own bottom tab bar). `DebtsScreen` now only owns tab/filter/refresh/add-sheet state and the `FlatList`. Any new debt-list feature should extend one of these components, not `debts/index.tsx` directly.
- **Fat Finder screen decomposition** (ABA-439): `app/fat-finder.tsx` was a 777-line screen with four inline render functions (`renderFinding`/`renderMonthPicker`/`renderHeader`/`renderFooter`, all closures recreated every render) plus the `TYPE_ICONS`/`SEVERITY_LABELS` lookup tables in the same file. Split into `src/features/fat-finder/fatFinderDisplay.ts` (pure, theme-parameterized `TYPE_ICONS`/`SEVERITY_LABELS`/`getSeverityColor`/`getSeverityBgColor`/`formatFatFinderDate`/`getMonthLabel`, mirrors `src/features/debts/debtDisplay.ts`) and components in `src/components/fat-finder/`: `MonthPicker` (nav row + `AiUsageBadge`, reused by the loading/error/empty states and by `FatFinderHeader`), `FatFinderHeader` (summary card + findings count), `FatFinderFooter` (generatedAt + regenerate button), `FindingCard` (the finding-card layout — expand/collapse moved from two id-keyed maps on the screen to local `useState` per card, safe because `FlatList`'s `keyExtractor` already keys each card by `finding.id`). `FatFinderScreen` is now a 262-line thin composition owning only data loading, month navigation, and the AI-cost-confirmation flow. Any new finding type or UI tweak should extend one of these components, not grow `fat-finder.tsx` inline again.
- **Receipt scan screen decomposition** (ABA-448): `app/expense/receipt.tsx` was an 868-line screen mixing the pre-scan capture UI, the post-scan confirm card, the whole category-split state machine (server-split seeding, manual reassignment, proposed-category bookkeeping), and the two save paths (create expense / hand off to the manual form) in one component body. Split into two hooks — `src/hooks/useReceiptCategorySplit.ts` (the `itemCategories`/`splitDropped`/`serverSplits`/`hasEditedCategories`/`showSplitSheet` state, the server-split seeding effect, and the `currentSplits`/`proposedNamesInPlay`/`proposedNamesToCreate`/`sheetItems` derivations — mirrors `src/features/debts/debtDisplay.ts`'s "pure logic, screen owns the JSX" split) and `src/hooks/useReceiptSave.ts` (`handleConfirmExpense`/`handleEditExpense`, plus the GPS-capture ref/effect that only that save path consumes) — and two presentational components in `src/components/receipt/`: `ReceiptCaptureView.tsx` (instructions, free-text prompt, processing spinner, camera/gallery/PDF buttons) and `ReceiptConfirmCard.tsx` (the receipt preview, editable fields, `CategorySplitChips`/`PriceFindingsCard`, the editable line-item list — `ReceiptItemsEditor.tsx`, added by ABA receipt-line-item-editing — confidence row, and the save-image/edit/confirm/retry actions), each with its own `createStyles`. The `compressAndEncodeImage` helper moved to `src/features/receipt/receiptImage.ts`. `ReceiptExpenseScreen` is now a 190-line thin composition owning only the simple screen-local UI state (`showConfirm`/`saveImage`/`userPrompt`/`merchant`) and the error-alert/scan-success effects; `ItemCategorySheet`'s modal stays screen-level since it renders as an overlay regardless of where in the tree it is mounted. Any new receipt-scan feature should extend one of these hooks/components, not grow `receipt.tsx` inline again.
- **Batch receipt-scanning session** (ABA-480): `app/expense/receipt.tsx` already looped back into capture mode on save — `useReceiptSave`'s post-save `Alert` has always offered `receipt.scanAnother` (→ `onReset`, no navigation away) alongside `common.done` (→ `router.back()`). What was missing for "clear a whole stack of receipts in one sitting" was a sense of progress and a natural checkpoint. `src/features/receipt/receiptScanSession.ts` (pure, unit-tested) exports `isReceiptSessionCheckpoint(count)` — true on every multiple of `RECEIPT_SESSION_CHECKPOINT_INTERVAL` (15), recurring, never blocking. `src/hooks/useReceiptScanSession.ts` tracks the count as **plain in-memory component state** (a `useRef` mirror lets `recordSaved()` return the post-increment value synchronously, since the caller needs it in the same tick) — deliberately not persisted or store-backed; leaving the screen (navigating away, or the screen unmounting) resets the count, which is the intended scope of "one continuous session," not a gap to fix. `useReceiptSave` gained an optional `onSaved?: () => { count, isCheckpoint }` called right after a successful save, before the result alert — when `isCheckpoint` is true the alert swaps its copy to `receipt.sessionCapTitle`/`sessionCapBody` ("Nice pace! You've scanned N receipts...") but keeps the exact same two actions, so the checkpoint only nudges, it never stops the loop. `ReceiptCaptureView` renders a small pill (`receipt.sessionCount`, pluralized × 9 locales) above the capture instructions once `sessionCount > 0` — visible exactly when the user is about to scan their next receipt. **Deliberately scoped to the expense flow only** — `app/income/receipt.tsx` shares `useReceiptScanner` but not `useReceiptSave`/`useReceiptCategorySplit`, already has its own separate scan-another alert, and income receipts are scanned far less often in practice, so it was left untouched.
- **Wallet transfer screen decomposition** (ABA-469): `app/wallet/transfer.tsx` was a 699-line screen with 11 separate `useState` hooks mixing create-mode form state, currency/amount-sync effects, the live FX-rate lookup, frequent-route chip prefill, and the per-account balance/`Max`-button logic. Split into `src/hooks/useTransferForm.ts` (all form state, the account-default/currency-sync effects with the `prefillFromRef`/`prefillToRef` guards, `fetchRate`, the amount↔rate cross-fill handlers, `applyFrequentTransfer`, `applyMaxAmount`, `handleSubmit` — pure logic, screen owns the JSX, mirrors `src/features/debts/debtDisplay.ts`'s split) and three presentational components in `src/components/wallet/`: `FrequentTransferChips.tsx` (the frequent-route chip row), `TransferAccountCard.tsx` (the account-picker + optional currency-picker + amount-input card, shared by both the "from" and "to" sides via a `footer` slot — the "from" side alone passes `TransferAvailableRow`), `TransferAvailableRow.tsx` (the available-balance text + `Max` button + insufficient-balance warning). `TransferScreen` is now a 279-line thin composition with zero local `useState`, owning only the exchange-rate/date/notes/count-as-income cards and the submit button (small enough not to warrant their own components). The edit screen `app/wallet/[id].tsx` was a separate file with its own form state and was NOT touched by this split — later split in ABA-479 (below). Any new transfer-form feature should extend `useTransferForm.ts` or one of these components, not grow `transfer.tsx` inline again.
- **Wallet transfer-detail screen decomposition** (ABA-479): `app/wallet/[id].tsx` was a 657-line screen mixing the read-only detail view with a full edit form — 9 local `useState` hooks, inline account/currency resolution, and two near-identical hand-rolled account-chip `ScrollView`s (from/to) that duplicated what ABA-469 had already extracted for the create screen. Split into `src/hooks/useTransferEditForm.ts` (mirrors `useTransferForm.ts`'s shape — `isEditing`/`isSaving`, account/amount/rate/notes/date state, the amount↔rate cross-fill handlers, `payableAccounts`/`otherAccounts`, `startEditing`/`cancelEditing`/`handleSave`/`handleDelete`; `fromCurrency`/`toCurrency` stay a pure derivation from the selected account rather than their own state, since edit mode never exposed a manual currency override) and reuses `TransferAccountCard` (from ABA-469) for the edit-mode account pickers in place of the duplicated chips. `[id].tsx` is now a 424-line thin composition. **Deliberately does NOT reuse `TransferAvailableRow`**: the create screen's available-balance/Max-button footer answers "how much is left in this account," but for an edit the transfer's own original amount has already been debited from that balance — showing it unadjusted would double-count the row being edited, so edit mode keeps its prior behavior (no balance display; `chipBalance={() => null}`, matching the old currency-code-only subtitle). `showCurrencyPicker` stays `false` on both cards while editing, for the same reason. Any new edit-form feature on this screen should extend `useTransferEditForm.ts`, not grow `[id].tsx` inline again.
- **Expense create screen decomposition** (ABA-477): `app/expense/new.tsx` was an 823-line screen with 23 `useState` hooks mixing basic expense fields with three independently-evolving sub-forms — recurring toggle, debt (lent/borrowed), and the manual category-split editor. Each sub-form got a hook (state, still owned by the screen since `handleSubmit` needs the values at save time) + a presentational component (props-driven JSX), mirroring the `useReceiptCategorySplit.ts`/`ReceiptConfirmCard.tsx` split from ABA-448: `src/hooks/useDebtExpenseFields.ts` + `src/components/expenses/DebtExpenseFields.tsx` (`isDebt`/`debtContactName`/`debtDueDate`/`showDebtDatePicker`, including the repayment-banner branch), `src/hooks/useRecurringExpenseFields.ts` + `src/components/expenses/RecurringExpenseFields.tsx` (toggle + period chips), `src/hooks/useCategorySplitEditor.ts` + `src/components/expenses/CategorySplitSection.tsx` (`showSplitEditor`/`pendingSplits` and the editor/summary/prompt three-way switch). `app/expense/new.tsx` is now 592 lines / 14 local `useState` hooks, owning only the basic fields (amount/description/merchant/category/tags/project/currency/date), trip-split state, and `handleSubmit` — matching the "screen owns simple local UI state" precedent from ABA-448. Trip splitting (`TripExpenseSplitPicker`) was already isolated before this pass and untouched. Any new sub-form on this screen should get its own hook+component pair, not another inline `useState`.
- **Products settings screen decomposition** (ABA-478): `app/settings/products.tsx` was 680 lines mixing three unrelated interaction modes — single-item rename (with its own inline `Modal`), AI re-analysis backfill, and multi-select merge (with its own inline `Modal` + selection state machine) — plus a live product search. Multi-select state (`selecting`/`selected`/`toggleSelect`/`enterSelect`/`exitSelect`) moved to `src/hooks/useProductMultiSelect.ts` (mirrors `useExpenseMultiSelect.ts`'s state-machine shape); the two modals became presentational components under `src/components/settings/` — `RenameProductModal.tsx` (rename input + save/cancel + the ignore-product destructive action) and `MergeProductsModal.tsx` (merged-name input + save/cancel, taking a precomputed `mergeLabel` string rather than the whole `products` array) — mirroring the shopping-list `ListNameModal.tsx` presentational-modal pattern. `app/settings/products.tsx` is now a thin composition owning only list rendering, search, and the AI-backfill button/handler (small enough to stay inline). Any new rename/merge-adjacent field should extend one of these two components, not grow `products.tsx` inline again.
- **Receipt image memory (ABA-463)**: Google Play enforces dynamic-memory (anonymous RSS + swap), bitmap and DEX thresholds from **February 2027** — an app over them loses Play visibility and publishing. The DEX half (>=25% optimization coverage) is already satisfied: `android/gradle.properties` sets `android.enableMinifyInReleaseBuilds=true` + `enableShrinkResourcesInReleaseBuilds=true` and `app/proguard-rules.pro` carries only two narrow `-keep` rules — **do not add a blanket `-keep class **`**, it would silently drop us under the bar. The memory half was all on the receipt path. **Rule: a receipt photo is downscaled before it is ever base64-encoded or handed to `<Image>`.** `src/features/receipt/receiptImageSizing.ts` is the pure, unit-tested home of the caps (`RECEIPT_OCR_MAX_WIDTH` 1600, `RECEIPT_STORED_MAX_WIDTH` 800) and `resolveResizeWidth`, which returns `null` to pass through rather than upscale a source already under the cap, and resizes when the width is **unknown** (the document-picker path reports none) — guessing wrong on a small image costs a modest upscale, guessing wrong on a 12 MP one costs tens of MB, so the memory-safe default wins. 1600 is chosen because the vision model downsamples to ~2048px on its longest side anyway; receipts are narrow, so capping WIDTH keeps the small print legible. `src/features/receipt/receiptImage.ts` is the only place that resizes (`downscaleForOcr` + `compressAndEncodeImage` over one private `toJpeg`) — it replaced a **third** inline copy that lived in `ReceiptSection` as `compressImageToBase64`; do not add a fourth. `toJpeg` runs the manipulation even when no resize is needed, since the pickers can return HEIC on iOS. `downscaleForOcr` **never throws** (falls back to the original URI — a scan that works but uses more memory beats a scan that does not work). `useReceiptScanner.processImage` downscales BEFORE encoding and sets `state.imageUri` to the downscaled file, so one shrunk copy feeds the OCR request, the preview, and the stored 800px copy; both pickers pass `asset.width` through. `app/income/receipt.tsx` shares the hook and inherits this. **Stored receipts render from a file URI, never a `data:` URL**: `src/features/receipt/receiptImageCache.ts` `materializeReceipt`/`releaseReceipt` writes the base64 to a cache file so Fresco can sample the bitmap down to the size actually on screen (a `data:` URL made the 200pt thumbnail decode at full resolution) and so the multi-MB string does not sit in component state; the filename carries a module-level revision counter because `<Image>` caches by URI and a reused path would show the previous photo after a replace. Web keeps the data URL (`expo-file-system/next` `File` has no working web implementation — same platform branch as `utils/fileBase64.ts`), which is fine: the threshold is a Play requirement. **Still open**: the **Restore Credentials API** (zero-tap sign-in on device migration) is enforced from **April 2027** — both stages have now landed (see the dedicated `Restore credentials` bullet above) but the round trip is unverified on real hardware; before this, sign-in was a JWT in `expo-secure-store` only, so a new device meant logging in again. Our real numbers against the memory thresholds are unmeasured — read them from Play Console's Android Vitals dynamic-memory breakdown rather than inferring them.
- **Financial Health Score** (ABA-193): `src/features/analytics/useFinancialHealthScore.ts` — pure `useMemo` hook computing a 0–100 score from four equally-weighted components (0–25 pts each): budget adherence (% of active budgets not over limit), savings rate (linear: 0%→0 pts, 20%+→25 pts, excluded when no income), goal progress (% of active goals on-track by deadline, excluded when no goals), debt health (always included; full 25 pts when no debts). Score normalises over included components only. `hasEnoughData=false` when <2 components have data. Color: red (<40), yellow (40–69), green (70+). Widget `FinancialHealthWidget` (`src/components/widgets/`) shows a circular gauge (pure `View`/`StyleSheet` border trick, no native deps) + a tap-to-open explanation `Modal` bottom-sheet. `WidgetKey` `financialHealth` is one of the `WIDGET_KEYS` entries (default on; `storeArrival` now leads the list — see below). Zero server calls, zero AI cost — entirely client-side.
- **i18n**: 9 locales in `src/i18n/locales/` — `en.ts` (source), `de.ts`, `es.ts`, `fr.ts`, `pl.ts`, `ru.ts`, `ua.ts`, `be.ts`, `nl.ts` (Dutch, ABA — added via `claude/dutch-language-support`). When adding keys, update ALL 9 files. Registered in `src/i18n/index.ts` (`SUPPORTED_LANGUAGES`, `intlLocaleMap` → `nl-NL`, `resources`).
- **Help system**: one markdown source, `user_docs/<lang>/NN-slug.md` × 9 locales, feeds both the in-app help screen and the public help center. **Moved to the wiki** — `docs/wiki/features/help-content-pipeline.md`. Carried here: NEVER edit `apps/mobile/src/help/content.ts` by hand (generated by `npm run generate:help`); extending an EXISTING section needs no registration at all, while a NEW one must be registered in THREE places — `scripts/generate-help-content.js`, `src/help/sections.ts`, and `docs/marketing/help/build_help.py`'s own list, whose omission silently drops it from the public site; and the landing rebuild that follows needs `LANDING_BASE= ROBOTS="index,follow,max-image-preview:large"`, since the env-less default emits a `noindex` preview over production.
- **Services**: `api.ts` (barrel), `http-client.ts` (base HttpClient), the domain api files (see **API client** above — do not restate their number here, the two places disagreed at 14 and 17 while the real figure was 25), `notifications.ts`, `secureStorage.native.ts` / `secureStorage.web.ts`, `widgetData.ts`, `appVersion.ts` (raw fetch wrapper around the public `/app-versions/check`, used pre-login)
- **Screens**: `(auth)/` login/register/forgot-password/reset-password, `(tabs)/` main tabs, `expense/`, `income/`, `budget/`, `account/`, `analytics/`, `calendar/`, `projects/`, `tags/`, `wallet/` (index, set-balance, exchange/index, exchange/[id], transfer, transfers, exchanges, [id]), `debts/`, `goals/`, `settings/` (index hub, profile, appearance, ai, widgets, notifications, security, categories, `reference` (ABA-153: reference-data hub linking categories/merchants/tags/projects; navigation-only, links to existing routes), data, about, ai-usage-details, `bots` (unified Telegram + WhatsApp connection screen), `import/` (transactions-import hub: index, preview, mapper, request-bank), `auto-capture` (Android notification auto-capture opt-in — linked from import hub; Android-only, shows "Android only" note on other platforms), change-email — 2-step flow with 6-digit code, pending state persisted in `secureStorage` under key `pendingEmailChange` with 30-min TTL so the screen resumes at step 2 after app restart), `subscription.tsx`, `referral.tsx`, `admin.tsx`, `story.tsx`, `fat-finder.tsx`, `scenario-simulator.tsx`, `subscriptions/` (index, new, [id]), `expense/merge.tsx` (ABA-296: two-expense merge screen, entry via `possible_merge` alert or import-preview)
- **Wallet balance history** (ABA-257 daily, superseded by ABA-258 monthly): two endpoints. (1) Daily `GET /wallet/balance-history?days=30` (capped 90; derives daily per-currency snapshots: current balance from `getSummary()`, back-calculate `startBalance = currentBalance – rangeDelta`, walk forward) → `WalletBalanceHistoryResponse { points, currencies }`. **Kept only for already-released app versions** — the mobile client no longer calls it. (2) **Monthly `GET /wallet/balance-history/monthly?months=6`** (ABA-258, `getMonthlyBalanceHistory`, clamp 1–12) sums **per-currency net change per calendar month** from income(+)/expense(−)/exchange(±)/transfers(±, inbound `countAsIncome:false`) → `WalletMonthlyHistoryResponse { months: WalletMonthlyDeltaPoint[] /* {month:'YYYY-MM', deltas} */, currencies }`, one entry per month incl. empty months. No new table. Mobile: `walletStore.monthlyHistory`/`selectedMonths`/`loadMonthlyHistory(6|12)`; `WalletBalanceCard` (`src/components/wallet/WalletBalanceCard.tsx`, replaced `WalletSparklineCard`) converts each month's deltas to a `displayCurrency` prop via `convertAmount` (current FX rates) and renders `WalletMonthlyChart` (`src/components/wallet/WalletMonthlyChart.tsx` — gifted-charts `BarChart` with **signed** bars, negatives below axis via `mostNegativeValue`/`noOfSectionsBelowXAxis`, per-bar green≥0/red<0, value on tap, NO per-bar labels — fixes the prior per-point label overlap), plus a 6M/12M chip toggle and the latest-month delta summary. **Wallet-local currency toggle** (ABA-258): `app/wallet/index.tsx` has a `SUPPORTED_CURRENCIES` chip row driving a local `displayCurrency` state (default `user.currencyCode`) for the total card + chart via `convertAmount` — **local-only, not persisted, never touches the global `user.currencyCode`**; resets to base on re-entry. Per-currency balance cards stay native.
- **Components**: `charts/` (Bar, Donut, Pie, Weekday, GroupedBar), `interactive-charts/` (drill-down with ChartRenderer; **`InteractiveLineChart`'s SVG wrapper (`chartClip`) must stay auto-height with `overflow:hidden` — NOT a fixed height** (ABA-352): gifted-charts draws the negative below-x-axis region *beyond* the `height` prop (`svgHeight = height + noOfSectionsBelowXAxis * height/noOfSections`), so a fixed-height clip hides all negatives; `noOfSectionsBelowXAxis` is proportional 1..4 to the most-negative point), `insights/`, `wallet/` (`WalletBalanceCard` + `WalletMonthlyChart` — monthly balance-change bars with 6M/12M selector + display-currency conversion, see **Wallet balance history** above), `widgets/` (NetProfitWidget, NetCapitalWidget, CalendarWidget, FinancialHealthWidget), `story/`, `investment/` (ABA-238: `PerformanceCard`, `AllocationCard`, `GainersCard`, `BenchmarkCard`, `FormulaModal` — sub-components extracted from `app/investment/analytics.tsx`; each owns its styles), `chat/` (ActionConfirmationCard, ActionResultCard, `ChatMessageItem` — single message bubble with markdown/action cards, `ChatHistorySheet` — conversation history modal bottom-sheet), `analytics/` (per-section components, each owns its styles, consumed by `(tabs)/analytics.tsx`), `expenses/` (ABA-176: `ExpenseListItem`, `IncomeListItem`, `ExpenseFilterBar` — extracted from the expenses tab screen; `ExpenseFilterBar` manages period chips, category dropdown, merchant picker modal, and month navigator; internalises `showCategoryPicker`/`showMerchantPicker` state and resets on `activeTab` change), `AccountSwitcher` (ABA-187: also the **global currency switcher** — its trigger pill shows the base-currency symbol after the account name (`Personal · $ ▾`, symbol never truncates), and its menu always opens (even with one account) with a "Display currency" chip section (one chip per `SUPPORTED_CURRENCIES`) that calls `authStore.setCurrency`. Currency is a user preference, not account-scoped → available to all roles incl. `viewer`, no `canEdit` gate. **ABA-243** (unified header across home + all tab screens): `showCurrency` prop (default `true`) hides the inline symbol. The home hero header AND every tab header (`(tabs)/_layout.tsx` `screenOptions`) render `<AccountSwitcher showCurrency={false} />` + the standalone `CurrencyPill` (same file, opens just the currency chips, also routes through `setCurrency`) so account and display-currency are **separate pills**; other consumers keep the combined pill. Tab headers use a **custom `header`** (the `TabHeader` component in `(tabs)/_layout.tsx`, not RN's default header — the default vertically centres the side controls, which left a long title overlapping them): a two-row layout — controls row (compact account + currency pills left; alerts bell with unread badge from `useAlertStore` + settings rounded buttons right), then a centered white divider, then the **page title BELOW the divider** (regular weight, own row — so a long name like "Транзакции" never collides with the pills). Home hero header has **no greeting** (removed) and the same style: controls row + centered white separator (`heroDivider`) above the quick-action strip. Both dividers are `width: '95%'` inside a 16px-padded container so they line up with page content. **ABA-244**: the home quick actions are a **centered wrapping grid** (fixed-width cells, `flexWrap` + `justifyContent: center`) — extra actions wrap to a new row that pushes page content down instead of hiding behind a horizontal scroll (the old `ScrollView` + `expo-linear-gradient` scroll-hint were removed). The first row overlaps up into the orange hero (`marginTop: -22`); each icon sits on a near-invisible `surface` backing so it stays readable on the overlap. Icons render via `QuickActionIcon` — brand outline SVGs from `assets/icons/*.svg` inlined as `react-native-svg` `SvgXml` strings (no svg-transformer / metro change); every symbol gets a uniform orange rounded-square frame (the 4 source SVGs that shipped with a built-in frame have it stripped + viewBox tightened), and each renders at its true aspect ratio so wide icons (e.g. transfers) stay centered; `color` recolors the green income/invoice variants), `AiUsageBadge`, `BulkTagPickerSheet` (standalone tag multi-picker sheet used from the expenses multi-select flow), `CreateCategoryModal`, `KeyboardAwareScreen` + `KeyboardAvoidingScreen` (pure-JS keyboard wrappers — see **Keyboard handling**), `HydrationProgressBar` (2.5px indeterminate slider mounted in `(tabs)/_layout.tsx`, driven by `useHydrationStore.isHydrating`), `Paywall` (ABA-265: mounted as a bottom-sheet `Modal` in `(tabs)/chat.tsx`, shown when an AI-limit **403** in `chatStore.sendMessage` flips the new `showUpgrade` flag; the catch maps 403 → `subscription.limitReachedBody` instead of the generic `errors.chatError`, and the card now shows a prominent `subscription.startTrial` badge above the plan cards — previously the component was a dead export, never mounted, so hitting the AI limit showed only a confusing error with no upgrade path), `ProjectPicker`, `SplitEditor`, `TagPicker`, `TagChip`, `TransactionActionSheet`, `UpdatePrompt` (Play/App Store update modal, mounted at root in `app/_layout.tsx`, force/soft modes, dismissal persisted per-version in `secureStorage` key `skippedUpdateVersion`; soft Update also writes the key before opening store; required prompts never dismiss locally, rely on `AppState`-active force version-check)
- **Hooks**: `src/hooks/usePortfolioAnalytics.ts` (ABA-238) — pure computation hook for investment analytics; takes `PortfolioAnalyticsResponse | null`, returns `performanceHistory`, `allocation`, `benchmarkComparison`, `topGainers`, `topLosers`, `latestValue`, `earliestValue`, `periodReturn`, `isPeriodPositive`; used by `app/investment/analytics.tsx`. `src/hooks/usePeriodNavigation.ts` — manages `selectedMonth`/`selectedYear` state with `isCurrentPeriod`, `getPeriodLabel`, `goToPrevPeriod`, `goToNextPeriod`, `resetToCurrentPeriod`; used by `(tabs)/analytics.tsx`. `src/hooks/useCalendarData.ts` — shared hook for calendar grid computation, date filtering, category breakdowns with multi-currency conversion. Used by `CalendarWidget` (home screen) and `app/calendar/index.tsx` (full-screen page). `src/hooks/useAiCostConfirmation.ts` — one-time confirmation dialog before expensive AI operations (cost >= 2.0), stores dismissal per feature in **MMKV** (`react-native-mmkv`, store id `ai-cost-confirmation`) — not AsyncStorage, which this said for months. `src/hooks/useAppVersionCheck.ts` — calls public `/app-versions/check` on mount and on `AppState` `active`, 6h in-memory cache, web no-op, fail-silent (never blocks app boot), exports `__resetAppVersionCheckCache()` test seam. `src/hooks/useOrientationLock.ts` — locks portrait on phones (`Math.min(width, height) < 600dp`) and unlocks on tablets/foldables, called once from `app/_layout.tsx`. App-level `orientation` is `"default"` so the manifest doesn't restrict large screens (Android 16 ignores manifest orientation lock on large screens anyway); the JS hook restores phone-only portrait UX. Web is a no-op. `src/hooks/useExpenseMultiSelect.ts` (ABA-176) — encapsulates expenses-tab multi-select state machine (`isMultiSelect`, `selectedIds`, bulk pickers, `isBulkProcessing`) and the three bulk action handlers; called from `(tabs)/expenses.tsx`. `src/hooks/useChatPolling.ts` (ABA-198) — wraps `startPolling`/`stopPolling` + `useFocusEffect` for the chat tab; pure side-effect, no return value. `src/hooks/useMentionBar.ts` (ABA-198) — manages `pendingMentions`, `@`-detection in input text, member lookup; returns `{mentionCandidates, insertMention, getActiveMentions, resetMentions}`; used by `(tabs)/chat.tsx`. `src/hooks/useTelegramBot.ts` / `useWhatsAppBot.ts` / `useSlackBot.ts` (ABA-240) — each encapsulates one bot's state, `useEffect`/`useFocusEffect` reload, and all action handlers; types (`WhatsAppStatus`, `WhatsAppLinkCode`, `SlackStatus`, `SlackLinkCode`) live in the respective hook file; `API_ORIGIN` derivation lives in `useSlackBot` (Slack OAuth install URL only); `BotsSettingsScreen` is a thin coordinator that calls all three.
- **Features**: `src/features/analytics/useAnalytics.ts` — analytics computations hook. Returns `AnalyticsSummary` (incl. `vsAverage: number` — signed % vs trailing 3-month rolling average, computed locally from SQLite expense store). `CategorySpending` items include `vsAverage: number | null` — per-category delta vs 3-month trailing avg shown as green/red chip below the percentage in the category list (month view only; `null` = new category or non-month view, chip hidden; ±5% noise threshold, computed offline from SQLite). `src/features/scenario/useScenarioProjection.ts` — scenario simulator projection hook (pure client-side, reads expense/income stores, projects savings over 3/6/12 months)
- **Scenario persistence**: `src/stores/scenarioStore.ts` — MMKV-backed store (`scenario-storage` id). Persists `SavedScenario[]` (id, name, expenseAdj, incomeAdj, extraIncomes, horizon, createdAt). Free tier: 5 scenarios max; Pro/Business: unlimited. Actions: `saveScenario(name, snapshot, isPro) → 'ok'|'limit_reached'`, `deleteScenario(id)`, `canSave(isPro)`. `scenario-simulator.tsx` has Save modal, load bottom sheet, and native Share button (text summary via `Share.share()`). No API endpoint — device-local only.

### Admin (Next.js)
- **Pages**: Dashboard (`/`), Login (`/login`), Investor Metrics (`/metrics`), Acquisition (`/acquisition`), Users (`/users`, `/users/[id]`), AI Usage (`/ai-usage`), Subscriptions (`/subscriptions`), Communications (`/communications`), App Versions (`/app-versions`), Referrals (`/referrals`), Audit Log (`/audit-log`), Settings (`/settings`)
- **App Versions page**: per-platform tabs (Android/iOS); list of past releases with "Current" badge on the most recent; "New release" Dialog form with semver-pattern inputs and 8-locale release-notes textareas (EN required); delete-confirm via `Dialog` (codebase has no `AlertDialog` primitive). Each release row's content is a clickable button (ABA-202) opening a read-only detail `Dialog` (latest/min version, clickable store URL, full release notes per locale) — the trash button stays separate. Hooks: `src/hooks/use-app-versions.ts`.
- **Users page**: filters (search debounced 300ms, tier, status) + paginated table. Sortable column headers (ABA-202) on Name, Email, Registered (`createdAt`), Last Login (`lastSyncAt`) via a `SortableHead` component with chevron indicators — clicking toggles asc/desc, switching column resets to asc, both reset to page 1. `sortBy`/`order` flow through `useUsers` → API `admin/users` (allow-list `name`/`email`/`createdAt`/`lastSyncAt` in `admin.service.ts`). Tier/Status/AI-Requests are not sortable (relation/computed fields).
- **Tech**: Next.js 16 App Router, React Query 5 (data fetching), shadcn/ui components, Recharts (charts), Socket.io-client (real-time), ky (HTTP client)
- **API client**: `src/lib/api-client.ts` — ky instance, auto-injects Bearer token, 401 → logout. Base URL from `NEXT_PUBLIC_API_URL` (default `http://localhost:3000/api/v1`)
- **Auth**: `src/lib/auth.ts` — login via `POST /auth/login`, tokens in localStorage (`admin_token`, `admin_refresh_token`)
- **Real-time**: `src/lib/socket.ts` — Socket.io namespace `/admin`, events: `new_user`, `ai_request`, `error`, `subscription_change`
- **Dashboard features**: KPI cards, subscription distribution pie chart, registration trends, AI cost by feature, live activity feed, top AI spenders
- **Communications page** (ABA-420): 5 tabs — Send Push, Send Email, Broadcast, Scheduled, History. `apps/admin/src/app/communications/page.tsx` is a thin `Tabs` shell (45 lines); each tab is its own component under `apps/admin/src/components/communications/` — `SendPushTab.tsx`, `SendEmailTab.tsx`, `BroadcastTab.tsx`, `ScheduledTab.tsx`, `HistoryTab.tsx` — so each tab's form/filter/pagination state is local to its own component instead of one god component tracking all five at once (the admin app's first per-page decomposition, mirroring the mobile app's repeated screen splits e.g. `(tabs)/index.tsx`/`app/account/[id].tsx`). History tab has summary stats cards, type filter (push/email/broadcast), expandable rows with recipient details (name/email), body preview, broadcast filters, delivery success bar, and paginated list with relative dates — all state (page/typeFilter/expandedId) owned by `HistoryTab.tsx` alone.
- **User detail page decomposition**: `apps/admin/src/app/users/[id]/page.tsx` (646 → 387 lines) had its five independent user actions each pulled into `apps/admin/src/components/users/` — `ChangeTierDialog.tsx`, `AiLimitCard.tsx` (an inline, non-modal control — it was never a `Dialog`, unlike the other four), `SendPushDialog.tsx`, `SendEmailDialog.tsx`, `DeactivateDeleteDialog.tsx` (bundles Deactivate + the Delete confirmation dialog, both "danger zone" actions). Each owns its own local state + mutation hook, taking `userId` (plus `userName`/`userEmail`/`isActive`/`customAiLimit` as needed) as props. `page.tsx` stays a thin composition: `useUserDetail`/`useUserNotificationHistory` + the profile/subscription/actions cards, accounts table, AI usage chart, recent expenses, notification history. Any new user-detail action should get its own component under `components/users/`, not another inline dialog in `page.tsx`.

### Shared Types
- Entities: `packages/shared-types/src/entities/index.ts` — 30+ domain interfaces
- DTOs: `packages/shared-types/src/dto/index.ts` — API request/response shapes
- API types: `packages/shared-types/src/api/index.ts` — API endpoint types
- Types use `PascalCase` interfaces, enums use string literal unions (e.g., `type AccountRole = 'owner' | 'editor' | 'viewer'`)
- Key enums: `Currency` (USD/EUR/PLN/GBP/UAH/RUB/**BYN**), `AccountRole` (owner/editor/viewer), `AccountType` (personal/business/shared/investment), `ExpenseSource` (manual/voice/ocr/import/telegram/whatsapp/slack), `BudgetPeriod` (daily/weekly/monthly/yearly/custom), `SubscriptionTier` (free/pro/business), `SyncStatus` (pending/synced/conflict/error)
- **Sync DTOs** (ABA-177): `packages/shared-types/src/dto/sync.ts` — `SyncChange` is a **discriminated union** keyed on `entityType`. Each entity type has its own payload interface (e.g. `SyncTagPayload`, `SyncProjectPayload`, `SyncPortfolioHoldingPayload`). `sync.service.ts` handlers take `Extract<SyncChange, { entityType: '...' }>` for compile-time payload validation. Do NOT add a local `SyncChange` interface in the API — always import from `@budget/shared-types`.

### Shared Utils
- Validation: `packages/shared-utils/src/validation/index.ts` — Zod schemas for auth, expenses, incomes, budgets, categories, tags, projects, sync
- Formatting: `packages/shared-utils/src/formatting/index.ts`
- Constants: `packages/shared-utils/src/constants/index.ts`

## Dependency Order for Changes

When modifying features that span multiple packages, follow this order:

1. `packages/shared-types` — entity interfaces and DTOs
2. `packages/shared-utils` — Zod validation schemas (if needed)
3. `apps/api/prisma/schema.prisma` — database schema, then `npx prisma migrate dev --name <name>` + `npx prisma generate`
4. `apps/api/src/modules/*` — API services, controllers, guards
5. `apps/mobile/src/db/schema/index.ts` — SQLite schema (independent from API DB)
6. `apps/mobile/src/db/*Repository.ts` — data access layer
7. `apps/mobile/src/stores/*` — Zustand stores
8. `apps/mobile/src/services/api.ts` — API client methods
9. `apps/mobile/app/*` — screens and UI
10. `apps/mobile/src/i18n/locales/*.ts` — translations (all 9 files)

Mobile SQLite changes (step 5-6) are independent from API Prisma changes (step 3-4) and can run in parallel.

## Commands

```bash
npm install                    # Install all dependencies
npm run dev                    # Start all dev servers (turbo)
npm run build                  # Build all packages
npm run lint                   # Lint all code
npm run typecheck              # TypeScript check all code
npm run test                   # Run tests
npm run format                 # Prettier format
npm run clean                  # Clean all builds + node_modules
npm run dev:web                # Start mobile app in browser (Expo web on http://localhost:8081)

# API specific (run from apps/api/)
npx prisma generate            # Regenerate Prisma client
npx prisma migrate dev --name X  # Create DB migration
npx prisma studio              # Visual DB editor

# Mobile specific (run from apps/mobile/)
npx expo start                 # Start Expo dev server
npx expo start --web           # Start web preview

# Admin specific (run from apps/admin/)
npm run dev                    # Start admin dashboard on port 3001

# Help content (run from project root)
npm run generate:help          # Regenerate apps/mobile/src/help/content.ts from user_docs/
```

## Environment Variables

See `.env.example`:
- `DATABASE_URL` — PostgreSQL connection string
- `REDIS_URL` — Redis connection string
- `JWT_SECRET`, `JWT_EXPIRES_IN` — JWT token config
- `GOOGLE_OAUTH_CLIENT_IDS` — comma-separated Google OAuth client IDs (web,ios,android) accepted as ID-token audiences for `POST /auth/google` (ABA-282). Client-side IDs are baked into the app via `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` / `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` / `EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID`
- `OPENAI_API_KEY` — OpenAI for AI features (Whisper, GPT)
- `FIREBASE_PROJECT_ID`, `FIREBASE_PRIVATE_KEY`, `FIREBASE_CLIENT_EMAIL` — push notifications
- `EXPO_PUBLIC_API_URL` — API URL for mobile app
- `STRIPE_SECRET_KEY` — Stripe API key (apiVersion pinned to `2026-01-28.clover`, must match SDK in `package-lock.json`)
- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` — the **user-facing assistant bot** (`MiCodeAIBudgetAssistantBot`, `TelegramBotService`): expenses, chat, voice, photo. `TELEGRAM_CHAT_ID` is now legacy/unused by the API (ops moved to the ops bot below).
- **Ops bot split (ABA-319)**: `OPS_TELEGRAM_BOT_TOKEN`, `OPS_TELEGRAM_CHAT_ID` — a **separate** MiCode ops bot + channel. ALL operational Telegram alerts go here, NOT the assistant bot: `TelegramService` (the ops notifier in `modules/telegram/`, injected by auth/subscriptions/referrals/import-bank) reads these for new-registration / new-subscription / referral / request-a-bank alerts, and `uptime-check.yml`/`backup-db.yml`/`infra-watch.yml` use the same-named **GitHub Actions secrets** for downtime/backup/infra alerts. **No fallback** — if `OPS_*` is unset, ops alerts are skipped (never leak to the assistant bot). Optional `OPS_PROJECT_NAME` prefixes every message (`<b>[name]</b> ` in the API, `*[AI Budget]*` in workflows) so a shared MiCode ops channel aggregating multiple projects can tell them apart. Prereq: create the ops bot + channel, set `OPS_TELEGRAM_BOT_TOKEN`/`OPS_TELEGRAM_CHAT_ID` in `.env.production` (force-recreate `api`) AND as GitHub secrets.
- `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_BUSINESS_ACCOUNT_ID`, `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_APP_SECRET`, `WHATSAPP_BUSINESS_PHONE_NUMBER`, `WHATSAPP_API_VERSION` — WhatsApp Business Cloud API. Token scope `whatsapp_business_messaging` only. `WHATSAPP_APP_SECRET` is the HMAC key for inbound webhook signature verification. `WHATSAPP_BUSINESS_PHONE_NUMBER` is shown in the mobile app as a `wa.me` deep link. Webhook URL is `https://api.ai-budget.pl/whatsapp/webhook` (no `/api/v1` prefix — excluded in `main.ts`).
- `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_APP_ID`, `SLACK_BOT_USER` — Slack bot. `SLACK_SIGNING_SECRET` is the HMAC key for inbound webhook signature verification (`v0=` scheme). Bot is a no-op when these are unset. `POST /slack/events` and `POST /slack/interactivity` are excluded from the `/api/v1` prefix in `main.ts`. `main.ts` also registers `express.urlencoded({ verify })` so that `rawBody` is available on the urlencoded interactivity endpoint for HMAC verification. Prod event URL: `https://api.ai-budget.pl/slack/events`.
- `SLACK_CLIENT_ID` — OAuth app Client ID (from Slack App Settings → Basic Information); required for multi-workspace install.
- `SLACK_CLIENT_SECRET` — OAuth app Client Secret (same page); used server-side in the `oauth.v2.access` code exchange; never exposed to clients.
- `SLACK_OAUTH_REDIRECT_URL` — must match exactly what is registered under OAuth & Permissions → Redirect URLs in the Slack App dashboard (prod value: `https://api.ai-budget.pl/slack/oauth/callback`).
- `SLACK_TOKEN_ENC_KEY` — 32-byte (256-bit) random hex key for AES-256-GCM encryption of `SlackInstallation.accessToken` at rest; generate with `openssl rand -hex 32`; rotation requires re-encrypting existing rows.
- `SENTRY_DSN` — optional. When set, `apps/api/src/instrument.ts` initializes `@sentry/node` (must be imported FIRST in `main.ts`, before any other module). When unset, Sentry is a no-op.

## Production

Hetzner VPS (Hetzner cloud), Docker Compose. Stack defined in `docker-compose.prod.yml`:
- `budget-db-prod` (postgres:16-alpine, 512M), `budget-redis-prod` (redis:7-alpine, 320M — `maxmemory 256mb allkeys-lru`), `budget-api-prod` (1G, `NODE_OPTIONS=--max-old-space-size=768`), `budget-admin-prod` (256M), plus shared `accounting-nginx` (separate stack) reverse-proxying `api.ai-budget.pl` and `admin.ai-budget.pl`. API container has **no host port mapping** — reach it only through nginx or the docker network. **API memory (ABA-163)**: without an explicit `NODE_OPTIONS=--max-old-space-size`, Node auto-caps the V8 heap at ~half the container limit (~256M at 512M), which OOM-crashed the backup export. Compose sets heap to 768 and `deploy.resources.limits.memory` to 1G — keep them in sync (heap + ~150M non-heap must fit under the limit).
- Volumes: `ai-budget_postgres_data`, `ai-budget_redis_data` (preserve across deploys; never `docker volume prune` without filters).
- **DB backups (ABA-163, Phase 1)**: `.github/workflows/backup-db.yml` — nightly cron (`0 2 * * *` UTC, off-host so it survives a VPS outage) + `workflow_dispatch`. SSHes to the VPS, runs `pg_dump -Fc` inside `budget-db-prod`, streams it to the runner, validates it (size ≥10 KB + `pg_restore --list` object count), `age`-encrypts (asymmetric — runner holds only `AGE_PUBLIC_KEY`; the private key is **offline**, never in CI), and publishes it as a Release asset (`ai_budget-YYYY-MM-DD.dump.age`, tag `backup-YYYY-MM-DD`) in the private repo `BACKUP_REPO` via `BACKUP_REPO_TOKEN` (PAT, `contents:write`). Logic lives in `scripts/backup-db.sh` (dump+sanity+encrypt; mockable via `LOCAL_DUMP`) and `scripts/prune-backups.sh` (GFS retention 7 daily/4 weekly-Sunday/6 monthly-1st; mockable via `RELEASES_FILE`/`DRY_RUN`). Failure → Telegram alert (same pattern as `uptime-check.yml`). Restore: `docs/ops/restore-runbook.md` (decrypt → scratch-restore → verify → prod `pg_restore --clean --if-exists`). RPO ≤24 h. Redis is **not** backed up (cache/ephemeral). Receipt images are in-DB `Bytes`, so the dump captures them (and grows with them — revisit object storage if a dump exceeds a few hundred MB). Phases 2–4 (infra fault tolerance, HA, mobile local backup) deferred — see `docs/superpowers/specs/2026-05-29-postgres-backup-phase1-design.md`.
- Deploy: push to `development` triggers `.github/workflows/deploy.yml` → SSH → `scripts/deploy.sh` (`git reset --hard`, `npm install` with lock, build, `prisma migrate deploy`, `up -d --force-recreate api admin`, then `docker image prune -f` + `docker builder prune -af`). Verify-step polls `https://api.ai-budget.pl/api/v1/health` for up to 120 s. **Pre-deploy guard (ABA-317)**: `deploy.yml` runs `scripts/check-no-shared-utils-runtime-import.sh` on the runner *before* the SSH step — a runtime `import`/`require` of `@budget/shared-utils` in `apps/api/src` fails the deploy (the API has no build step for workspace packages, so it loads the shared-utils TS barrel and prod Node ESM crash-loops on `ERR_UNSUPPORTED_DIR_IMPORT`). Mirrored by an `apps/api` ESLint `no-restricted-imports` rule (`allowTypeImports`). `import type` is fine; copy runtime values into an API-local util. **Build-cache pruning matters**: images are built on the VPS each deploy and the build cache accumulated ~4 GB → disk hit 89% (ABA-165); the per-deploy `builder prune` is NOT enough on its own — **`deploy.yml` is path-filtered** (`apps/api/**`, `apps/admin/**`, `packages/**`, `docker/**`, `docker-compose.prod.yml`, `scripts/deploy.sh`), so a stretch of mobile/marketing/docs work means `deploy.sh` never runs and the cache grows untouched. That is how ABA-165 recurred as **ABA-432** a week after the last API deploy: 3.35 GB of cache + a 1.8 GB `buildkit/metadata_v2.db` put `/` at 88%. The scheduled **`.github/workflows/docker-gc.yml`** (`scripts/docker-gc.sh`, Sunday 01:00 UTC an hour before `backup-db.yml`, plus `workflow_dispatch`) is what actually bounds it; it no-ops below 70% and alerts only when the disk is **still** above 85% after cleaning. `.github/workflows/infra-diagnostics.yml` (manual `workflow_dispatch`) dumps a `docker system df` + `du` breakdown over SSH when disk needs investigating. **One Docker daemon is shared with `marketing-ai-*`, `accounting-*`, `legalka-bot` and `shared-nginx`, so `docker volume prune` (any filter) and `docker image prune -a` are both forbidden here** — the volumes include `ai-budget_postgres_data`, and unused-but-tagged images are another project's rollback target. Runbook: `docs/ops/disk-pressure.md`.
- **Snap-installed Docker is held and disabled** (`snap refresh --hold docker`, `snap disable docker`) after the 2026-04-27 incident where snap auto-refresh hijacked `/var/run/docker.sock` from the apt-installed daemon. Do not re-enable snap docker; only the apt-installed `dockerd` (system data root `/var/lib/docker`) owns prod state.
- Adding env vars: `docker restart` does NOT reload `env_file`. Must `docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate <service>`.
- **CORS (ABA-151)**: `main.ts` builds `corsOrigins` from `process.env.CORS_ORIGIN.split(',')` (falls back to `localhost:8081`/`:3001` when unset). It must be an **explicit comma-separated origin list** in prod `.env.production` — e.g. `CORS_ORIGIN=https://admin.ai-budget.pl,https://ai-budget.pl`. Do **NOT** use `CORS_ORIGIN=*`: it becomes the array `["*"]`, which Express/Nest `cors` matches as a literal origin (not a wildcard), so the admin browser gets no `Access-Control-Allow-Origin` and login silently breaks (and `*` is invalid with `credentials:true`). Native mobile is unaffected (no `Origin` header). After changing it, force-recreate the `api` service (see env-var note above).

## Mobile releases (Android)

- **Bare Expo workflow** — `apps/mobile/android/` is committed, so EAS builds from the native project and does **not** run prebuild or read `app.json` for the version. The version shown in **Google Play comes from `apps/mobile/android/app/build.gradle` `versionName`**, not `app.json`. `eas.json` `appVersionSource: "remote"` + production `autoIncrement: true` only manage the integer `versionCode` (build number) remotely; they never touch `versionName`. (Historically this caused every release to ship as `1.0.0` while `versionCode` quietly incremented — ABA-131.)
- **Before each release**: bump `versionName` in `android/app/build.gradle` (source of truth for Play) AND `version` in `app.json` (kept in sync for the in-app update gate) to the same value. Do not hand-edit `versionCode` — EAS remote autoIncrement owns it.
- **One semver, two cadences — the web carries a build id and native does not (ABA-514).** Native and web are ONE codebase and ONE `app.json` version, and they must stay that way: a different scheme would invent a distinction the source does not have and break the only thing the number is for, tying a bug report to code. What they do not share is a release cadence — `web-deploy.yml` deploys on every push to `development`, while a phone carries whatever last cleared Play review (weeks here, and once frozen outright by the foreground-service declaration, ABA-438). So a bare shared number claims two different builds are the same software, and it already did: the deployed web app read `1.25.0` while carrying commits that were in no store build. **The web therefore shows `<version>+<short sha>`** — `EXPO_PUBLIC_BUILD_SHA`, set by `web-deploy.yml` from `github.sha`, composed by pure `src/features/about/displayVersion.ts` and rendered in the **existing** version row on the About screen (no new i18n key — a second row would need nine locales for a developer-facing string). `+` is semver **build metadata and is explicitly ignored for precedence**, so nothing that compares versions can see it: not `UpdatePrompt`/`useAppVersionCheck`, not `minSupportedVersion`, not the store-review throttle. Native passes no sha and keeps the bare version, because Play owns that string and `versionCode` is its own build identity. `displayVersion` **drops a non-hex build id rather than showing it** (the value comes from an env var, which can hold anything — including an un-expanded `${{ github.sha }}`); 7 characters, matching git's own abbreviation. **Do NOT add a `web` platform to the app-versions gate**: that gate exists because a native install can be arbitrarily old and needs forcing forward, whereas a web reload is always current — it would be a mechanism with no failure mode to prevent.
- **CI guard**: `apps/mobile/scripts/check-release-version.js` runs as a production-only step in `mobile-build.yml` and `mobile-eas-build.yml`. It fails the build if `build.gradle` versionName ≠ `app.json` version, and (best effort) if the version isn't strictly greater than the latest `v*.*.*` git tag (skipped when no release tags exist).
- **Publish**: run the *Mobile Build & Publish* workflow (`mobile-build.yml`, profile `production`, submit `true`) → builds AAB `--local` → `eas submit` to the Play production track. After release, also add the new version in **admin → App Versions** so the in-app `UpdatePrompt` knows about it.
- **Foreground-service declaration blocks the submit, and no amount of retrying fixes it (1.18.0; unblocked on `development` since ABA-438)**: while the app declares `FOREGROUND_SERVICE_LOCATION` (Shopping Mode, ABA-408), `eas submit` fails at the *track update* step — the AAB uploads fine — with `Google Api Error: Invalid request - You must let us know whether your app uses any Foreground Service permissions.` fastlane retries it five times and every attempt fails identically, because this is a **Play Console form, not an API field**: Android 14+ requires a per-FGS-type declaration under **Play Console → the app → Policy → App content → Foreground service permissions**, stating what the feature does, why it must run as a foreground service, and (for the `location` type) usually a short video of the in-app journey. It is declared **per app, not per release**, so once filled in it stops blocking future submits. Recovery without rebuilding: the workflow already uploaded the AAB as the `app-release-production` GitHub artifact (30-day retention) — fill in the declaration, then upload that same file by hand in Play Console. A re-run of the workflow also works but burns another remote `versionCode`. Not to be confused with the much heavier `ACCESS_BACKGROUND_LOCATION` declaration, which this app deliberately does not need. **`development` no longer declares either permission** — ABA-438 moved Shopping Mode to `feature/shopping-mode` precisely so releases stop being hostage to this form — so a submit from the mainline works again without it. The declaration still has to be filled before that branch can re-land, and it is per app, not per release, so filling it early costs nothing.
- **Samsung Galaxy Store is a second, signature-separate channel (ABA-495)**: runbook `docs/ops/samsung-galaxy-store-rollout.md`. Two facts that decide everything else. (1) Since **2026-09-02** Samsung blocks *every* Seller Portal submission whose binary is not ADV-approved (unconditional — not the country-scoped install block of 2026-09-30), so the certificate signing the Galaxy Store build must first be added under **Play Console → Android developer verification → `com.budget.assistant` → Add key**; that package is registered with **1 key** today (Play App Signing, 2026-04-10). (2) Play App Signing holds our app signing key and will not export it, so a Galaxy Store build is signed with the **upload key** instead — same package, different signature, therefore **mutually exclusive installs** with the Play version and no updates crossing between stores, permanently. Verified as *not* signature-bound, so nothing breaks: Google sign-in uses the **web** OAuth client via the `ai-budget.pl/oauth/callback/` relay (`EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID` is dead config, read nowhere), and `google-services.json` carries no `oauth_client`/`certificate_hash` at all. The binary needs no code change — *Mobile Build & Publish* with `profile=preview, submit=false` already yields a production-API release APK (`app-release-preview` artifact), since every submit step is gated on `profile == 'production'`. Listing copy cannot be pasted from `docs/marketing/copy/google-play-listings.md` unchanged: Galaxy Store counts **bytes**, and `shortDescription` is **40 bytes** against Play's 80 chars, so all 9 locales are over (ru/ua/be by 63–67 B — UTF-8 Cyrillic leaves ~20 letters); screenshots must be ≤ **2:1**, and ours are 1080×2340 (2.17:1), so they pad to 1170×2340. Registration is free, but commercial-seller validation needs a company-domain e-mail (not the gmail Play login) plus MICODE documents, at up to 10 business days per stage. F-Droid is permanently ineligible — `LICENSE` says proprietary, not open source.

## Observability

- **Health**: `GET /api/v1/health` (public, no auth) — runs `SELECT 1` on Postgres, returns `{status, db, uptimeSeconds, timestamp}`. 503 if DB fails. Used by Docker `HEALTHCHECK` (requires HTTP 200) and CI verify-step.
- **Uptime**: `.github/workflows/uptime-check.yml` runs every 5 min via cron, hits the public `/api/v1/health`. On non-200, sends a Telegram alert via `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` GitHub Secrets with HTTP code, response body, and a link to the failed run.
- **Backup alerts (ABA-163)**: `backup-db.yml` posts a Telegram alert on any failed nightly DB backup (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`), same channel as the uptime check. A silent backup failure is the classic way to discover at restore time that there are no backups — so failures, not successes, are alerted.
- **Infra watch (ABA-165)**: `.github/workflows/infra-watch.yml` — cron every 30 min, SSHes to the VPS and pipes `scripts/infra-check.sh` (`ssh … 'bash -s' < script`) to check root-disk usage (> 85% alerts) and that all 4 prod containers are running and not `unhealthy`; non-zero exit → Telegram alert. Complements `uptime-check.yml` (5-min `/health`) with disk pressure + per-container granularity. Reuses SSH + Telegram secrets (no new ones).
- **Log rotation (ABA-165, gap found in ABA-432)**: `docker-compose.prod.yml` sets `logging` `json-file` `max-size: 10m` / `max-file: 3` on postgres/redis/api/admin (~30 MB/container) so logs can't fill the disk. **That covers only 4 of the 16 containers on the box.** Everything created outside compose has no limit — including **our own** `ai-budget-web-prod` and `ai-budget-app-prod` (hand-created on the VPS in ABA-213/ABA-269) and `shared-nginx`, whose json.log had reached **244 MB** by 2026-08-24. Recreating them with `--log-opt max-size=10m --log-opt max-file=3` is the fix and is **not yet done** (ABA-432); `shared-nginx` needs a window because it briefly interrupts every site on the box. Also left alone deliberately: **`/root/.gradle` 4.3 GB + `/opt/android-sdk` 2.9 GB** of Android toolchain on a server that only runs API/admin/nginx — mobile builds happen on EAS and GitHub runners, so it is almost certainly a leftover, but it may belong to another project.
- **Disaster recovery**: `docs/ops/restore-runbook.md` (DB-only restore) and `docs/ops/disaster-recovery-runbook.md` (full server rebuild after total VPS loss). Both need the offline `age` private key; full rebuild also needs an **offline copy of `.env.production`** (prod secrets live only on the VPS). Redis is cache-only and intentionally not persisted.
- **Errors**: `@sentry/node` v8 captures unhandled exceptions and Express 5xx errors. Init must remain at the top of `main.ts` via `import './instrument'` — moving it after other imports breaks instrumentation.
- **No Prometheus / Datadog / external APM** at this time.
